# 已确认支付延迟入账调查与恢复

## 已验证事实

- B06 达摩12年 ¥1380：支付 P8fd40f533e854367a49bb411f2692eb3，原订单 order-befaa55c46042d8cb84cde34，商户单号20260912MM6el8oh。渠道时间2026-09-12 00:39:11 CST，最早成功查单观察00:39:22.623、成功通知00:39:22.654；这些及后续九次查询观察均未消费。08:19:36.381消费新的查单观察，08:19:36.402首次写入¥1380到账流水。幂等键标明manual-reconcile，不能称为后台自动恢复。
- VIP1 ¥88同样曾有成功通知及九次成功查询未被消费；此前通过现有核对命令恢复。9月12日本轮只读检查已显示refunded，未在本轮发起退款。¥20已处理历史问题排除。
- 两笔异常都是order_batch；同期普通order成功通知通常约20ms完成。这是关联证据，不足以单独确定某条SQL或合并付款代码必然有错。
- 历史callback的handleRoute只返回映射错误，后台循环catch丢弃异常；日志无原始异常。因此最初事务失败的具体触发原因尚不能复原。不能将9次成功查单误写为9次渠道失败。
- 已保存成功凭证没有被自动恢复链路优先使用，每次仍调用渠道。应用失败被记为普通query error，再进入频道退避，延长了营业页面的不一致。

## 修复范围与边界

在原OnlinePaymentService查询入口优先读取同门店、同付款、postar-active-query已验真的未消费成功观察；由既有authority重新核对金额、币种、交易号、状态和归属并原子消费。稳定观察恢复键防并发重复，到账日从凭证时间及门店营业日配置计算，避免第二天恢复计入第二天。

成功应用失败保留已观察成功状态、一分钟进行本地恢复；原七日财务窗口保留，未恢复对渠道的高频查询。回调与后台分别记录安全错误码和处理阶段，不记录渠道密钥、原始报文或SQL。普通渠道处理继续原有退避。

明确没有绕过账务事务，也没有伪造成功状态、删除旧支付或发起第二次退款。原始异常缺证部分保持开放，不能宣称完全根治。

## 验证与交付

精确工作树 /Users/jingda/mbox/mbox-member-growth-reliability-20260909；分支fix/payment-confirmation-apply-20260912；基线b1af40e包含此前Cursor临时热修，现网声明仍rc.187/d31f8fe/schema196。拟发布rc.188，不新增数据库迁移或小程序代码。

新增真实PostgreSQL故障回归：合并付款已有成功凭证，在账务事务写事件阶段故意抛错，验证全部回滚且观察仍未消费；下次渠道离线仍恢复成功、仅一笔138000分流水、零渠道调用。隔离库支付极端场景28项通过。首次重复使用测试库产生历史夹具主键冲突，新建独立库后通过；合并付款断言原先误用仅适用普通订单的helper，已改按原支付ID独立核对流水。

完整常规检查1662项通过；独立数据库1886项通过、1项既有跳过；支付API42项通过。PR212已合并，发布v1.0.0-rc.188，对应0dde87b4986559f0b1056ac2026761ecc5a360a9。标签CI34682902378和发布34682902425均成功。

2026-09-12 16:32 CST正式部署完成，schema196无迁移，production/ready，后台任务healthy。镜像sha256:75f3c75999f6563b9c01f481d982d1c6655d407f9e17380cba3941ad3efd604f。正式入口验证发布包、备份与OSS上传读回、候选服务及切换后的首页/顾客/预约/员工页面和浏览器渲染均通过。备份mbox-20260912T083036Z-meBgPY.dump，保留旧版回滚容器。证据位于.runtime/deploy/v1.0.0-rc.188/deployment/deployment-manifest.json和.runtime/payment-sync-20260912/deploy.log。

部署后只读回查：VIP1 ¥88 refunded，B06 ¥1380 succeeded；近七天已有成功观察但付款尚未成功入账的记录为0。该查询不等于对所有未知支付完成渠道核对。本轮未发起收款或退款。最初独立查询遗漏mbox schema而报relation不存在，补齐只读search_path后完成，不是线上服务错误。原始触发异常依旧缺失证据，不能将恢复机制上线写为完全根因闭环。

## 附加复核：SYS-175 同一次收款重试编号不稳定（P0，目标rc.188，负责人Codex）

主线8e5d970将默认付款号恢复为随机P加UUID，满足星驿适配器1—40位字母数字约束，但同一个idempotency-key重试时又生成随机publicId，进而改变命令指纹。新增回归在修改前实际失败：相同操作取得不同P编号；该证据能证明重试冲突缺陷，不能证明它造成B06成功通知入账失败。

修复为同门店、同幂等键生成稳定的P加32位摘要；新操作换键生成新编号。显式指定编号及测试注入编号行为保留。线上和现金/POS记录入口一起修复，避免把原先56位带连字符的非法星驿编号重新引入。完成标准：相同操作的默认编号和指纹一致，新操作编号不同，格式满足适配器限制，支付API与真实账务回归通过。
