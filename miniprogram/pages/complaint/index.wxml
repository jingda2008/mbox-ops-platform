<view class="page">
  <view class="top-band complaint-band">
    <view class="brand-row"><view><text class="eyebrow">{{tableCode}} · 独立处理通道</text><text class="complaint-title">由值班负责人接手</text></view></view>
    <text class="complaint-subtitle">投诉会进入经理关注队列，当前服务员不能自行关闭。</text>
  </view>
  <view wx:if="{{error}}" class="error-banner">{{error}}</view>
  <view wx:if="{{success}}" class="success-banner">{{success}}</view>
  <view class="panel complaint-form">
    <label class="field">
      <text class="field__label">问题类型</text>
      <picker mode="selector" range="{{categories}}" value="{{categoryIndex}}" bindchange="onCategoryChange">
        <view class="picker-field">{{categories[categoryIndex]}}<text>选择</text></view>
      </picker>
    </label>
    <label class="field">
      <text class="field__label">发生了什么</text>
      <textarea class="field__textarea" maxlength="300" value="{{details}}" placeholder="请描述事实和您希望如何解决" bindinput="onDetailsInput" />
    </label>
    <button class="danger-button" disabled="{{submitting}}" bindtap="submitComplaint">{{submitting ? '正在提交' : '提交给值班经理'}}</button>
  </view>
  <view class="complaint-policy"><text>处理边界</text><text>本页负责通知值班经理到桌，不公开员工处理记录，也不会在客户端伪造“已解决”状态。</text></view>
</view>
