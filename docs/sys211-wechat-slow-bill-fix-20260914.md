# SYS-211 微信小程序慢桌账阻塞加菜修复

更新时间：2026-09-14 11:38 CST。范围：仅微信小程序。工作目录：`/Users/jingda/mbox/mbox-system-audit-20260914`；分支：`fix/wechat-slow-table-bill-20260914`；代码基线：`3f645374e1337021e835789f9df931276c662b88`。

## 行为变化

原页面等待菜单、购物车、桌账全部结束后才允许加菜。现在菜单与权威购物车返回后即可选品，桌账并行读取；提交新订单前仍要确认桌账，查询失败则保留购物车并提示重试。同一页面提交复用正在进行的桌账查询，避免重复请求。

付款恢复逻辑从页面初始化中提取，保留本人订单、付款未知状态、已到账记录与原结账幂等键。初始化先恢复本地结账锁，再开放选品；桌账回读只更新付款状态，不重置客人刚加的购物车。切桌、同桌重载、离开页面或已开始重试结账时，旧桌账响应不能覆盖当前付款状态。

服务人员冻结购物车、菜单/购物车身份检查、服务端金额与库存校验及支付接口均保持原有约束。支付宝代码未修改。

## 验证证据

| 检查 | 结果 | 证据 |
|---|---|---|
| 原码反证 | 在临时目录使用 HEAD 原微信页面运行新增慢桌账用例，按预期失败：选品仍等待桌账 | [baseline-regression.log](../artifacts/sys211-wechat-fix-20260914/baseline-regression.log) |
| 定向回归 | 68通过、0失败，包含新增12项微信场景 | [targeted.log](../artifacts/sys211-wechat-fix-20260914/targeted.log) |
| 小程序发布回归 | 273通过、0失败、0跳过；与定向用例有重叠，不累加计数 | [release-miniprogram-tests.log](../artifacts/sys211-wechat-fix-20260914/release-miniprogram-tests.log) |
| 微信静态检查 | 151文件通过 | [check-miniprogram.log](../artifacts/sys211-wechat-fix-20260914/check-miniprogram.log) |

新增12项覆盖：桌账悬挂仍可加菜及并发提交、延迟失败与恢复、换桌旧响应、同桌重载旧响应、旧付款核验不得跨桌复用、支付中、结果未知、已付款、原结账重试与晚到空账单、购物车冻结、购物车失败、离开页面。

慢响应通过保持请求未完成来复现，而非立即返回失败。用例中的200毫秒是本地测试超时保护，不是生产响应指标。测试使用隔离页面及接口替身，证明客户端控制逻辑，不代表真实微信支付或真机网络速度。

## 交付状态

本地修复完成，未提交、推送、部署、上传或发布。原SYS-211登记保留；微信待正式包与真机弱网验收，支付宝按用户范围保持现状。本轮未改后端、资金数据或库存，不包含其他审计问题修复。

## 开发者工具打开记录

2026-09-14 11:40 CST：用户要求打开最新小程序。微信开发者工具 Stable 2.02.2608060 的项目路径已核对为本修复目录 `/Users/jingda/mbox/mbox-system-audit-20260914/miniprogram`；执行普通编译后显示 `pages/home/index` 首页，调试器0错误/4警告。未进行加菜、付款、上传或正式发布；此记录不代替慢网或真机验收。
