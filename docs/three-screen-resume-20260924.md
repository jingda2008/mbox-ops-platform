# 三屏竖屏与恢复操作补查

更新时间：2026-09-24 21:57 CST。沿用本地独立工作树，未提交、合并、部署或访问生产。保留SYS-379/380/381修复。

## 确认的问题与修复

1. 取餐详情在可见视口缩小时，底部确认仍按页面布局高度定位。修复前模拟可见高度480px，768/600/390宽下按钮底部分别为1009/933/817px，均越界。增加只影响屏幕布局的VisualViewport适配，三屏跟随可见高度和偏移，忽略手动捏合缩放；取餐内容内部滚动，确认栏留在底部。后厨可见高度低于420px时暂时隐藏已选批次的列表，保留详情与操作。
2. 取餐进入后台后仍保留可操作状态，唤醒发起读取期间也可能显示旧队列可操作；制作屏仅靠轮询，缺少同样的立即恢复流程。现在三屏在隐藏/离线时锁定，恢复可见、网络连接或pageshow后先重新读取。旧的普通读取被取消并用序号隔离，进行中的业务命令继续沿原幂等记录核对，不自动重发命令；未确认的原操作仍阻止新操作。后台返回的读取不能解除暂停。
3. 取餐窄竖屏原把整个工作台滚动，详情最小高度350px进一步挤出按钮。改为队列、详情及历史内部滚动；全宽导航、长备注、份数调整和撤回入口保留，主要按钮不低于44px。
4. 入口测试原假定整个隔离门店没有其他制作/待取记录，与部分完成测试合并运行时产生误报。现在核验本次新建批次及取餐屏有效加载，不再把其他批次视为本次失败。

## 验证与证据

最终联合浏览器运行16项全部通过（23.6秒）：
- 取餐768×1024、600×960、390×844，8品项长备注、少取1份、横竖旋转保持7份、480px可见视口及按钮点击命中；未读完出品时不能确认，滚动查看后可确认。模拟队列的业务写请求被拦截，并断言没有提交。
- 后厨三个竖屏尺寸，320px模拟键盘可见高度、实际隔离环境开做2份、旋转、只备齐1份、保留另1份制作中。
- 后厨、酒水和取餐，隐藏/恢复、离线/重连时读取被延迟，主操作持续禁用，读取成功才恢复；不发送业务命令。
- 此前三屏密度/全屏/错误提示3项和真实隔离账号入口4项回归。

相关单测7文件95项通过；前端类型检查与正式normalized服务端/网页构建通过。Lint退出0，保留仓库既有警告；git diff --check通过。

证据目录：`/Users/jingda/mbox/outputs/three-screen-resume-20260924/`。
- before.log及before-artifacts：初次复现。三项视口越界及取餐后台未锁定为有效缺陷；两个制作测试错误使用管理员而被岗位权限拒绝，已换成对应隔离岗位账号，不改权限。
- after.log及combined-artifacts：首次联合14通过/2失败，失败为共享测试数据假设；entry-fresh.log全新数据库4通过确认入口无回退。
- final.log：修正测试隔离假设后的16项联合全部通过；unit/typecheck/lint/checklist.log保留对应检查结果。

这些是隔离环境与浏览器事件/可见视口模拟，不能替代现场Android键盘、锁屏、设备绑定及真实实物取餐验收。qucan生产设备绑定没有读取或变更；SYS-379/381及本轮SYS-382现场验收仍开放。
