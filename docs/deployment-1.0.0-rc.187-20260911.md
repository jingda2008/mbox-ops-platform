# rc.187 开发与交付记录

本轮范围为最终需求 DEV-01—13。代码实现和自动测试完成；现场实物、真实资金和平台发布证据单独列明。

## 源码与检查

- 工作树：`/Users/jingda/mbox/mbox-member-growth-reliability-20260909`。
- PR #210：`https://github.com/jingda2008/mbox-ops-platform/pull/210`，合并提交 `d31f8fe784b8f77cbd9e8122632942df0f9abf3f`，标签 `v1.0.0-rc.187`。
- PR CI `34560725413` 全部必需检查通过；完整本地 check 为1655通过，隔离数据库1878通过/1原有跳过。
- 本次不重做原支付幂等、后台退避、实际到账与多收基础；¥20已处理退款不查询、不补账、不再次退款。

## 客户端

- 微信官方工具上传 rc.187 成功，AppID `wxdb9f2dc413484f2d`，包963981 bytes（941.4 KB）。候选149文件哈希全部回读匹配。
- 微信候选源码 `ae291802f8c178b598ebf309d97f2f5c81652797`；到合并提交的微信源码无差异。上传不代表平台审核上线或已选择体验版。
- 支付宝官方IDE发现省略catch参数编译不兼容，已改成显式参数并补静态检查；重新编译、首页渲染成功。候选153文件与 `96cd33a` 源码匹配（生产配置按候选明确注入）。当前登录账号无可关联应用，检索AppID `2021006196615276` 仍无结果，因此无法上传，未创建替代应用。
- 支付宝既有身份/手机/支付/通知开关保持失败关闭；未将编译成功写成渠道上线。平台适配和商户配置属于原有外部验收范围。

## Windows和现场

- 打印桥1.0.3升级包已生成，包含大字排版、保留本地日志和安全升级路径。
- 独立生产回读：桥版本仍1.0.0，最后心跳2026-09-11 02:13:49 CST；不能从active推断当前在线。没有可用Windows远程安装入口，本轮未完成终端升级或实体纸票验收。
- iPhone相机/输入法、扫码换桌、实际收款、多员工操作、实物退库和两站点打印需真机/现场验收，自动化不替代。

## 发布过程保留记录

- 主分支CI `34561088450` 在合并提交d31f8fe通过。
- 标签CI首次浏览器功能43通过/10原有跳过，唯一失败为顾客启动P95 542.5ms、重试584.8ms，高于既有500ms门槛；两次60个员工/顾客启动样本均无失败，关键API P95低于500ms。未调低门槛或跳过用例；对相同提交重跑失败任务，结果另记。不能据此确定网络或运行器是唯一原因。
- 标签浏览器第二次任务103145920219通过；员工启动P95 243.2ms、顾客P95 376.2ms，门槛仍500ms，各30个成功样本、零失败。首次失败归档SHA256 `e7e2bdf0769d3eafc4d92373bf7103aee1830afe874d5fdf3dfc3dd31af92872`，原CI失败附件仍保留。
- 发布流程首次等待标签CI超时（12:25:48），未进入部署；同一发布流程重新启动，没有修改标签或源码。

## 正式部署与业务收尾

- 标签CI34561136089（第二次）和发布34561136073（第二次）均成功；部署使用既有正式路径，经chaohai-app及独立证据中继，无验证绕过。
- 生产切换2026-09-11 12:31:11 CST，schema196，commit d31f8fe，normal/writeEnabled=true，worker healthy；独立公网readiness及 `/`、`/guest?table=W01`、`/reserve`、`/staff/live` 的HTML与真实浏览器通过。
- 镜像摘要`sha256:4dbd0f202404334f5fd3a25641da38b1a2b1216fe373cc1c4dc56b21ce66064f`；平台摘要`sha256:efd38f0baf717a4d0e4d58ee7848ef2b642f107576b2fda1d5bd042cee39765f`。
- 备份`/opt/mbox/backups/mbox-20260911T042947Z-TMeVXF.dump`；回滚容器`mbox-app-rollback-d31f8fe-20260911-123109`，前版7d789f5。应用回滚不等于数据库回退。备份、镜像、发布及切换材料均经OSS上传/回读。
- 五笔修复先事务预演回滚，后提交并独立回读；5条取消订单的payment_status从pending收尾为unpaid，5条唯一审计。原支付状态/释放标记不变，五笔财务核对仍stopped/finance_review_required。原支付所有旧字段哈希与部署前一致；新增order_batch_id逐条确认为null后按同一旧列比较。初次直接全行哈希因新增空列不同，已明确归因，未掩盖业务数据差异。退款、账务、库存关联记录均无新增，再次预演零修改。
- 12:33:13 CST启用cashier_payment=true/copies=1，后续实际付款自动生成一张收据；另外8类策略逐项与部署前一致，再次运行不变更。旧事件早于策略启用时间不自动补打。未为了验证而在生产发起收退款或打印测试票。
- 打印桥升级包版本附件已上传，远端摘要与本地一致：`c96fc866564188bc23cf5f3cab7c0297027c56bfa2e3789d4c0c879da82a2b07`。

[版本与打印桥升级包](https://github.com/jingda2008/mbox-ops-platform/releases/tag/v1.0.0-rc.187)。原始部署材料位于`.runtime/deploy/v1.0.0-rc.187`；候选与限定回读记录位于本工作树`.runtime`。不提交数据库、密钥或原始运维日志。
