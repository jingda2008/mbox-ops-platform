# M-BOX rc.68 历史日志与运行质量审计

审计日期：2026-08-09

## 结论

历史日志证实“页面感觉慢”不是单纯主观感受。`rc.64`在员工启动、心跳、任务和KDS动作上均存在秒级长尾，并出现数据库连接池超时；`rc.67`错误信号下降，但关键路由仍超过候选SLO，任务与KDS还缺足够线上样本。因此`rc.68`必须重新采样，不能沿用旧版本的压力测试结论。

## rc.64长周期历史

| 路由 | 成功样本 | P50 | P95 | P99 | 结论 |
|---|---:|---:|---:|---:|---|
| 员工启动 | 1682 | 604ms | 5167ms | 23367ms | 失败 |
| 员工心跳 | 533 | 1648ms | 8332ms | 12804ms | 失败 |
| KDS动作 | 425 | 6786ms | 15879ms | 19188ms | 失败 |
| 任务动作 | 113 | 3440ms | 14153ms | 16529ms | 失败 |

同期记录134次数据库连接池超时、36次规范化读取回退、21次连接提前关闭和2次DNS错误。日志中未解析到应用5xx，但这不能抵消数据库等待和业务请求长尾。

## rc.67近期日志

| 路由 | 成功样本 | P50 | P95 | P99 | 结论 |
|---|---:|---:|---:|---:|---|
| 客户会话 | 118 | 297ms | 1269ms | 1352ms | 失败 |
| 预约列表 | 32 | 289ms | 781ms | 907ms | 失败 |
| 员工启动 | 23 | 288ms | 1913ms | 1954ms | 失败 |
| 员工心跳 | 9 | 1469ms | 2560ms | 2560ms | 样本不足，观察值异常 |

近期日志未出现5xx、连接池超时或规范化读取回退，出现1次DNS解析错误。任务和KDS没有满足指标门禁的样本，不能判定通过。

## 根因判断

以下为代码与日志交叉验证后的工程判断：

1. 员工心跳曾读写约7MB整店聚合状态，连接池和变更队列在高峰互相放大。
2. 受保护接口鉴权会读取并克隆整店状态，心跳和启动轮询重复支付相同成本。
3. 工作区投影循环中反复创建`Intl.DateTimeFormat`，单次投影产生明显CPU阻塞。
4. 客户会话轮询虽有数据库幂等，但每次仍建立事务并尝试写事件。
5. 任务和KDS仍是聚合写路径；`rc.65`已增加串行队列和revision缓存，但缺少`rc.67`线上样本验证实际改善幅度。

## rc.68整改与验证点

- 心跳迁移到独立员工租约表，鉴权与续租合并为一次轻量事务，正常心跳不得改写整店revision。
- 中国时间格式化器复用；本地2500次调用从旧线上同类路径约1036ms降至约7ms。此数字只证明CPU热点被消除，不代表完整接口延迟。
- 客户会话到访事件在进程内合并，并继续由数据库幂等约束兜底。
- 预约接口去除重复聚合读取，员工首屏延后非关键心跳。
- 增加规范化路由直方图、事件循环延迟、连接池、变更队列和投影就绪指标。

## 未关闭风险

- 任务/KDS聚合写路径必须在候选镜像上重新压测。
- 单实例进程内缓存只能减少重复工作，不能代替数据库幂等或跨实例一致性。
- 阿里云当前资源较小，真实300人高峰仍需观察CPU、内存、连接池和事件循环，而不仅是HTTP成功率。
- 真实支付、打印机、微信容器、门店弱网和连续3个营业晚班不在日志模拟范围内。

## rc.68当前候选复测状态

PR #62首次提交级性能运行`31306680108`未通过：员工/客户读取、任务、点单与KDS均存在超限，事件循环P95约165.0/158.7ms。

第二次不可变提交级运行`31316732711`把问题进一步拆清：员工API启动旅程120/120成功、P95 101.6ms，但浏览器页面内双帧P95 701.4ms，主要时间落在API就绪357.5ms至工作区就绪665.7ms之间，证实工作区懒加载资源瀑布；一个实例的登录聚合写队列等待P95 133.97ms，超过100ms门槛。KDS开始在持续5次/秒下P95 3469.7ms，两个实例写队列等待P95约2599.7/2601.4ms，而连接池获取P95只有约0.32/0.31ms，说明当前瓶颈是整店聚合JSON串行改写，不是数据库连接池。下一阶段因固定PostgreSQL端口冲突未启动，属于测试基础设施缺陷；因此该运行整体失败，且商业发布结论不变。

同一阶段的后续脏工作区本地诊断曾在5次/秒下得到KDS开始P95 96.5ms、300/300成功，但最大并发仅1，而上述不可变CI最大并发达到17。两组证据不是同一提交、同一构建和同一运行环境，不能相互覆盖；它们共同说明结果对构建身份和写入服务时间高度敏感。CAP-018继续以不可变CI失败为准，必须由启用完整构建来源证明的新提交CI重新裁决。

随后审计发现旧负载脚本把多条写路由按固定顺序放在同一个持续增长的数据集上，并使用闭环单工作者；这会混入状态体积增长并隐藏固定到达率下的排队。当前工作区已经改为七个独立阶段，每阶段使用全新PostgreSQL数据库和两个新API进程，写路由采用开放到达率，`all`模式被标记为无发布证据资格。

早期脏工作区的隔离诊断曾得到员工浏览器P95 398.7ms和客户P95 377.1ms，但不可变CI没有复现员工结果，因此不能再把该本地数字当作候选结论。代码已经在应用入口预取员工工作区，重复增加预加载不能解释或修复CI长尾；必须继续拆分API、资源下载、解析和渲染，并增加弱网与低端移动设备档位。

修正动态端口并把源码、构建环境、构建产物和来源工具摘要全部绑定后，最新本地诊断重新构建了当前源码：员工API旅程120/120成功，P95 97.1ms；员工新浏览器上下文页面内部双帧绘制30/30成功，P95 391.1ms。相同样本从Playwright外部导航到可见工作区的观测P95为1096ms，说明391.1ms只能表示页面内部就绪，不能替代完整设备启动指标。客户首次会话API的前一轮诊断为300/300成功、P95 24.8ms；客户新上下文首屏双帧绘制30/30成功、P95 373.9ms。读取阶段1812次测量无失败，Bootstrap 200/304 P95分别为30.9/15.6ms。员工复测两个实例连接池获取P95约0.04ms，写队列等待P95最高51.1ms。以上仍是绑定当前脏工作区构建来源的诊断证据，尚未替代不可变CI；浏览器进程启动、网络条件、员工登录准备和真实设备均未纳入完整冷启动验收。

最新脏工作区KDS诊断使用双实例、独立PostgreSQL和300个样本：在2次/秒下300/300成功，P95 200.7ms、P99 210.7ms；连接池获取P95约0.07ms、写队列等待P95约0.03ms，业务不变量与跨实例检查均通过。该结果只证明提交回归档位；5次/秒容量失败仍保留，且脏工作区结果不能将对应TC改为正式通过。

本地点单不变量阶段300/300通过，P95 114.9ms、P99 120.5ms；订单、审计、库存销售流水、打印任务、KDS关联和幂等重放均验证为一次。修正门禁后重新执行KDS完成阶段：准备300单、准备300次制作，再测量300次完成，P95 153.7ms、P99 169.6ms；完成审计、配送任务、跨实例状态、5xx、连接池和投影均通过。种子最初没有商品库存，曾造成“库存扣减0/300”的真实测试失败；补充确定性入库基线后转绿，证明测试必须核对业务前置条件，不能只看接口成功。

第三次不可变提交级运行`31320627313`已经证明多个旧瓶颈明显下降：员工API启动旅程120/120、P95 98.7ms；客户首次会话300/300、P95 31.9ms；完整Bootstrap 200与ETag 304的P95分别为37.0ms和8.8ms；预约读取P95 28.9ms。连接池获取在关键阶段远低于50ms，因此此前58.0ms和189.6ms的连接池观察值已不代表当前提交。浏览器仍未达标：员工页面内双帧P95 608.2ms，客户561.5ms，目标500ms；员工API只占268.7ms，后续主要是资源解析与绘制，客户API就绪P95 448.2ms仍是首要优化点。

该运行也发现三项测试设施缺陷，不能把它们误写为产品通过或失败：任务测试持续时间越过60秒SLA后仍强制期待`pending`，实际合法进入`escalated`；KDS完成在300次制作准备之前就清零指标，导致准备写入污染完成动作的队列与事件循环窗口；阶段合并器把每阶段本应不同的`workload.phase`纳入共同环境身份，造成七阶段必然不一致。当前本地候选已分别改为时间语义断言、在所有准备动作后重置全栈指标、忽略阶段名但继续核对其余工作负载身份，并给三类错误补充回归测试。未形成新不可变CI前，`CAP-008/CAP-015/CAP-016`仍保持失败。

修正测量窗口后，本地候选又执行了三轮同条件、双实例、300次KDS完成诊断。第一轮300/300成功、客户端P95 387.3ms，但一个实例的前台写队列等待P95 207.5ms、P99 265.7ms，连接池P95仍低于0.1ms，证明两个API实例各自运行SLA定时任务会与前台KDS写入发生相位竞争。第二轮增加PostgreSQL session advisory lease、300ms空闲窗口和10秒最大延后后，客户端P95降至259.2ms，队列等待P95降至69.8ms，但一个实例事件循环P99为104.5ms，仍超过100ms门槛。代码复查随后发现热写路径先`structuredClone`、再由`parseState`内部再次克隆整个聚合状态；去掉重复拷贝后的第三轮300/300成功，客户端P95 247.4ms、服务端P95 246.3ms，两个实例队列等待P95仅0.03/0.02ms，连接池获取P95为0.09/0.07ms，事件循环P99为95.6/95.0ms，全部本地门禁通过。分布式租约还增加了解锁失败即销毁连接的保护，避免带锁连接返回连接池。上述结果来自未提交本地候选，只能证明修复方向和测试门禁有效，`CAP-019`仍须由新提交的不可变CI裁决。

浏览器依赖审计进一步发现，`/guest`虽懒加载客户门户，静态入口仍先加载并解析员工`App`、离线队列、员工API和运营样式。当前候选把最外层入口按URL拆分，客户、会员、公开预约与员工应用分别启动；公共入口JavaScript由约51.3KB降至10.6KB，约98.7KB员工样式不再进入客户首屏。双实例本地复测中，员工API旅程120/120成功、P95 102.9ms，员工30个新浏览器上下文双帧绘制P95 405.4ms；客户读取阶段1812次请求无失败，客户会话P95 22.2ms，客户30个390x844新上下文双帧绘制P95 69.2ms。两端均低于500ms候选门槛，且计时仍从导航开始到可见后两帧绘制。客户读取前置负载为了缩短诊断时间使用10次/秒，不代表经营峰值结论；浏览器测量在前置负载结束后独立执行。以上仍是未提交本地候选，`RPF-015`必须等待不可变CI复现，真实微信、Android WebView、iPhone和门店网络仍需现场验收。

提交`3bd32fa3ee7b2e8dd4524e401f05b10b3945d844`的不可变CI `31325130109`中，质量、数据库和浏览器商业流程通过；客户页面双帧绘制P95为213.7ms并通过，但员工页面为558.4ms，仍超过500ms。性能套件随后在KDS开始和完成阶段与30秒员工心跳发生重置竞争，`/api/metrics/reset`返回`409 METRICS_RESET_BUSY`，因此两阶段没有进入有效测量；准备数据污染后出现的队列P99和状态增长不得解释为KDS产品性能。合并器因缺少两个阶段拒绝生成完整证据，属于正确的失败关闭。候选修复暂停并等待在途心跳、对忙碌窗口做有界重试，同时取消员工首屏对非首屏工作台模块的等待。本地双实例回归中，员工页面30个新上下文双帧P95为408.5ms；KDS开始300/300、P95 200.9ms；KDS完成300/300、P95 249.3ms，两实例队列等待P95为0.04/0.02ms、连接池获取P95为0.11/0.07ms、状态增长约4.09/4.07KB每写，均通过候选门禁。以上仍须新提交CI裁决，不能提前改为正式通过。

提交`ea52af39275cd1ba5f1ca7b944a6de01df99d1e2`的不可变CI `31327038745`已修复上述测试窗口缺陷：七个独立路由阶段全部执行，负载请求、业务不变量、日志门禁和环境合并均完成，KDS开始与完成各300次业务动作本身达到路由SLO。性能作业仍正确失败，原因变为三个可归因的产品指标：员工页面双帧P95为649.6ms；KDS开始两个实例事件循环P99约107.5/107.9ms；KDS完成事件循环P99约149.7/155.1ms，且一个实例写队列等待P95为352.5ms。各实例连接池获取P95仍低于0.11ms，不能把问题归因到连接数或通过扩大连接池解决。资源瀑布显示员工工作台仍在Bootstrap完成后才加载；调度代码则每2秒用`structuredClone`复制整店聚合状态判断是否有工作，即使无到期任务也阻塞事件循环。当前候选将员工工作台下载与Bootstrap并行，并为无到期营业日、待点单、KDS、任务和SOP状态增加只读快速判定；本地结果只能作为修复诊断，仍须下一不可变CI裁决。

上述候选的本地定向复测中，员工API旅程120/120成功、P95 106.3ms，30个新浏览器上下文双帧绘制P95 442.7ms。KDS开始在2次/秒、300次业务动作下300/300成功，客户端P95 190.6ms、P99 231.4ms，两个实例事件循环P99为80.5/79.8ms；KDS完成同样300/300成功，客户端P95 256.1ms、P99 271.8ms，事件循环P99为93.5/92.7ms。两阶段写队列等待P95最高约0.05ms，连接池获取P95最高约0.11ms。它说明资源并行加载与无工作调度快速判定值得进入提交级复测，但仍是脏工作区诊断，不等于CI通过。快速判定只优化无到期工作；启用SOP、已到期KDS、最长延后、租约故障接管和聚合写容量仍需独立场景验证。

提交`efd32100fea1a1e6ba19f5bebfb7505c52d3d05b`对应的PR合并候选CI `31329190196`没有复现上述本地绿灯。员工API旅程120/120、P95 99.0ms，但员工岗位首页页面内双帧P95为701.7ms；Bootstrap就绪P95为363.0ms，角色首页进入DOM为670.9ms。客户岗位同一轮双帧P95为318.2ms并通过。KDS开始300/300、客户端P95 251.4ms，但两个实例事件循环P99为104.53/104.07ms；KDS完成300/300、客户端P95 417.7ms，但事件循环P99为155.98/148.24ms，一个实例写队列等待P95为131.45ms、写服务P95为550.93ms。连接池获取仍低于0.1ms，继续证明当前问题不是连接数不足。该轮七阶段、数据库和浏览器商业流程均完成，性能和最终发布门禁正确失败。

最新CI还证明“失败证据留存”的代码分支已被实际执行：运行制品55个文件、质量总账3个文件都尝试上传，但GitHub返回`Artifact storage quota has been hit`，API中没有可下载制品。因此状态必须记为`artifact_missing`，不能记成测试未执行，也不能宣称证据链闭合。释放旧Actions制品额度属于账号级删除动作，未获明确授权前不执行；也可另行配置阿里云OSS等不可变对象存储。

本轮继续拆分员工首屏：Bootstrap到达后先渲染同一套真实`RoleHomeView`，完整运营工作台在后台继续加载，首页按钮会把操作请求转交给加载完成后的工作台。本地30个全新浏览器上下文双帧P95为92.1ms，最慢131.6ms；该数字来自未提交工作树，只说明结构修复方向有效。聚合写路径新增`clone/domain/serialization/state_write/projection`分段P95和最大值指标，用于下一次KDS复测确定3.6MB活跃聚合状态下究竟是主线程序列化、数据库JSONB写入还是规范化投影占主导；在该证据形成前不调整连接池或服务器规格。

首次分段诊断中，KDS完成300/300成功、客户端P95 238.6ms；两个实例连接池获取P95低于0.1ms，写队列等待P95约0.02ms。聚合写P95分段为：克隆9.8至16.0ms、领域逻辑0.7至1.0ms、序列化22.5至28.1ms、整店状态写库72.6至76.7ms、规范化投影15.5至17.6ms，而完整mutation服务P95为212.6至288.3ms。可见单纯JSONB写入不是全部耗时，剩余主要候选是revision行锁等待、状态回读、事务提交和调度器并发写。该轮运行期间又加入了写入来源指标，旧脚本未检测源码漂移，因此本轮只保留为诊断，不构成当前工作树或发布证据。测试启动器现已增加结束前源码指纹复核；下一轮同时采集`revision_lock`及`kds/scheduler/other`来源，才能给出根因结论。

冻结源码后的来源复测再次完成300/300次KDS，客户端P95为296.6ms且业务动作本身通过；源码漂移为0。两个实例的`revision_lock` P95仅0.77/1.31ms，连接池获取P95仅0.08/0.12ms，排除数据库连接和行锁为主因。实例一承担150次KDS写且无调度写，队列等待P95为0.02ms；实例二承担150次KDS写和45次调度写，KDS/调度写服务P95分别为211.1/214.0ms，应用队列等待P95升至195.2ms并使运行门禁失败。根因是300ms连续空闲要求与500ms到达间隔、约211ms写服务形成相位冲突。候选将调度空闲判定改为25ms、单次等待250ms，优先利用前台写后的早期空档，持续繁忙时仍由10秒最大延后兜底；须按原条件复测后才可判断修复有效。

按完全相同的双实例、300次、2次/秒条件复测后，KDS完成300/300成功，客户端P95/P99为266.6/291.1ms；两个实例事件循环P99为98.0/89.9ms、连接池获取P95均低于0.1ms、应用写队列等待P95为0.02/58.1ms，运行指标与业务不变量全部通过。调度器仍执行45次权威写入（分别25/20次），没有通过停用SLA升级换取性能；KDS写P95为217.3/261.4ms，调度写P95为137.3/201.1ms。一个实例`revision_lock` P95为97.7ms，说明跨实例锁竞争仍存在，但早期空档让它未再把前台队列推过100ms。该结果证明最小调度相位修复在本地有效，但仍是脏工作区诊断；`CAP-019`维持失败，等待提交级CI复现。

质量证据链同时发现一个独立P0问题：旧工作流只在性能作业成功时解析路由指标，PR也跳过运行制品和质量总账上传，导致真实失败可能被二次表述为“缺少证据”。当前候选让失败作业继续携带已生成的路由明细，将`failure/cancelled/skipped`分别映射为`fail/blocked/not_run`，PR和主分支均上传带SHA256的原始运行制品及质量总账。该规则已增加自动化测试，但仍需新CI实际失败/通过两条路径验证。

KDS阶段的聚合状态由约80KB增长到约3.29MB，倍率约41倍，但约916次集群业务写入的净增长约3.5KB/写，低于5KB门槛且未超过10MB绝对上限。旧门禁单凭倍率阻断属于低基数误判，已改为倍率预警、绝对上限和显著体量下每写斜率联合阻断。该结果仍暴露结构风险：`runtime_states.state`仍是兼容性权威写模型，规范化表主要承担投影和读取；连续3个营业日的有界增长、归档和查询退化尚未验证。

提交级单路由回归默认KDS写入2次/秒。该值建立在“300位客人、每人估算3个出品、每个出品4次状态流转、营业5.5小时”的显式估算上，约为估算夜间均值0.182次/秒的11倍；它不是实测经营事实。5次/秒约为27.5倍，保留为容量探针且当前失败。后续应由真实订单与KDS事件校准模型，不能用2次/秒通过覆盖5次/秒容量缺陷。

任务写入、5次/秒容量阶梯、三轮完整重复、多营业日稳定性、完整App冷启动、全部阶段合并、全量CI、固定候选环境和阿里云资源门禁仍待执行。完成前`rc.68`性能与商业发布结论继续为`DENY`。历史20并发聚合写冲击和`rc.67`双容器部署约6分钟资源饱和仍保留为扩展风险，不能据局部本地结果宣布无需扩容。

提交`17f4138aa2841eeed3a9f2bf884e6fb79da8078a`的不可变CI `31332057352`中，代码质量、数据库和浏览器商业流程通过，但性能作业正确失败。员工启动两个实例事件循环P99为103.3/102.0ms，略高于100ms；KDS完成300/300业务请求成功、客户端P95 491.5ms，但一个实例事件循环P99 146.1ms、写队列等待P95 125.4ms、写服务P95 591.0ms，另一实例事件循环P99 143.5ms。连接池获取P95仍低于0.1ms。分段指标显示高尾延迟主要来自跨实例revision行锁等待和调度聚合写竞争，而不是连接池。该运行还再次触发GitHub Actions制品额度耗尽；执行失败与`artifact_missing`分别记录，不能互相替代。

代码复查发现`waitForMutationIdle`只观察本进程队列，因此实例A可能在实例B刚写完或仍持锁时误判全店空闲。下一候选使用`runtime_states.updated_at`作为跨实例共享空闲事实，要求全店连续750ms无提交后才运行聚合调度；持续高峰下最长避让15秒后强制执行，防止后台SLA永久饥饿。聚合克隆后增加一次事件循环让出，避免克隆与规范化序列化合并成同一长同步任务。

冻结源码的本地双实例复测中，KDS完成300/300成功，客户端P95/P99为235.6/242.1ms；两个实例事件循环P99为96.9/89.3ms，写队列等待P95约0.02/0.02ms，连接池获取P95约0.12/0.07ms，业务不变量全部通过。调度器仍完成5次权威写入，没有通过关闭后台任务换取性能。员工启动API旅程120/120成功、P95 98.2ms；30个新浏览器上下文首屏绘制P95 94.8ms；两个实例事件循环P99为26.9/27.6ms。独立复审随后发现普通时间戳读取不能排除兄弟实例未提交事务及检查后竞态；候选已进一步改为前台写持有共享事务门、后台到期写必须立即取得独占事务门并在同一事务内复核750ms安静窗口，通知和SOP的首次认领也进入同一门控。该改动必须由真实PostgreSQL双仓储持锁测试和新提交CI复现后才能改变CAP状态。

事务门改动后的冻结源码复测中，2次/秒KDS完成300/300成功，客户端P95/P99为231.5/239.0ms；两个实例事件循环P99为96.0/87.5ms，写队列等待P95约0.02/0.02ms，连接池获取P95约0.11/0.07ms，后台调度仍执行5次。5次/秒容量探针同样300/300成功，实际发起和完成吞吐均为5次/秒，客户端P95/P99为171.9/177.4ms；两个实例事件循环P99为83.5/82.7ms，写队列等待P95约0.01ms，连接池获取P95约0.03ms，状态、审计和库存不变量全部通过。5次/秒轮没有到期调度写，因此它只能裁决`CAP-018`容量档位；`CAP-019`仍需真实持锁和故障接管CI。

## 证据

- 原始日志：部署服务器Docker历史日志，分别保存为`/tmp/mbox-rc64-history.log`和`/tmp/mbox-rc67-server-audit.log`。
- 历史日志SHA256：`rc.64`为`08aab755da430a3c01b8f32a302236a83871a78743edce74ba541e2873296c42`；`rc.67`为`b2f487216aa19423a9993f0f01f9e32bd152de3e83f225a2104fe65b0bec1ba0`。
- 解析工具：`scripts/analyze-runtime-logs.mjs`。
- 指标门禁：`docs/runtime-quality-standards.md`。
- 当前本地分阶段证据：`artifacts/runtime-quality-current/`，不纳入Git且不替代CI制品。
- 调度隔离与单次拷贝本地候选证据：`artifacts/runtime-quality-kds-complete-scheduler-lease/`及`artifacts/runtime-quality-kds-complete-single-clone/`，不纳入Git且不替代CI制品。
- 路由级首屏拆分本地候选证据：`artifacts/runtime-quality-staff-route-split/`及`artifacts/runtime-quality-guest-route-split/`，不纳入Git且不替代CI制品。
- 岗位首页即时壳层本地候选证据：`artifacts/runtime-quality-staff-fallback/`，30个新浏览器上下文双帧P95为92.1ms；只作诊断。
- 当前证据来自未提交本地候选，不得冒充不可变镜像、CI或上线结果。
- GitHub工作流正在改为七个独立数据库阶段，按“每阶段恢复基线、启动双实例、负载、指标、日志、停止、合并、SHA256封存”的顺序执行；该改动尚未形成提交或CI运行，不能冒充已通过。
- 聚合缓存正常写入、RLS和revision锁路径未发现状态串改；仍需后续增加低频全量checksum巡检及“同revision、不同checksum”回归，避免为了该P2风险在每次热写中重新序列化1MB以上状态。
