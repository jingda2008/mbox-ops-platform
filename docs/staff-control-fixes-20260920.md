# 员工后台逐项修复与核对记录（2026-09-20）

本轮范围为逐控件审计确认的 **SYS-287—303 共17项**，以及 **60条文案处置决定**。代码已落实，逐项本地验证通过（2026-09-20 23:44 CST）。原审计保留，修后证据单独归档。

基线提交：`5c4bde7de5ed154ab3dd9227a64751dcc65293eb`；分支：`audit/staff-every-control-20260920`；目录：`mbox-staff-every-control-20260920`。本候选含未提交修改，不能用基线SHA冒充修后不可变版本。

## 逐项修复

| 编号 | 问题 | 实际修改 | 核对内容 |
| --- | --- | --- | --- |
| SYS-287 | 损耗登记丢失返回后重试，再次扣库存 | 冻结损耗内容和原请求编号，按员工隔离持久保存，关闭页面仍可恢复；已成功但列表读取失败时只刷新结果。 | 丢返回→刷新页面→关闭页面再进入→恢复原登记，流水编号相同且仅一次扣减；恢复模块7项单测，导航/北京时间2项单测。 |
| SYS-288 | 新物料损耗限额为0，超限后没有审批办理路径 | 零限额及超限损耗生成待复核申请；申请人不能自批；独立通过时重新核对库存，驳回不扣库存。 | PostgreSQL提交、重放、自批拒绝、无权限拒绝、独立批准、驳回、库存不足；浏览器默认0限额。 |
| SYS-289 | 前往配置中心审批按钮没有可见目的地 | 原审批按钮导航到专用审批页，携带规则类型和同一草稿编号并自动选中。 | 浏览器真实创建等级草稿后，以另一岗位一次点击定位；六类旧入口均接同一导航函数。 |
| SYS-290 | 核对库存成本打开的是隐藏分区中的表单 | 先切到库存分区再打开成本核对；返回原商品时仅刷新成本，保留商品未保存内容并拒收旧商品的迟到成本。 | 具有界面能力的隔离夹具验证入口可见、返回保留编辑；服务端原成本更正权限/数据库用例保留。 |
| SYS-291 | 只读概览、权益待办和异常页的说明与实际内容不符 | 概览只读，规则起草/审批/发布各自限定动作；待交付与异常页仅呈现对应办理动作和说明。叠加试算、券日历与赠礼仍在规则与条款，并在草稿页给出直达入口。 | 浏览器检查概览零写表单、零保存/审批/发布入口；待办和异常无等级/目录编辑表单。 |
| SYS-292 | 输入校验抛错后页面没有提示 | 把倍率、数量和日期校验纳入错误反馈，非法值不产生未捕获异常；金额精度由输入控件直接校验。 | 空倍率、负数/非整数边界约束、金额第三位小数、非法库存与终身上限；浏览器与精确数值单测。 |
| SYS-293 | 快速切换规则时旧响应覆盖最后选中的规则 | 每次选取规则递增读取序号，迟到响应不能覆盖最后选择；失败保留目标并提供重读。 | 浏览器延迟A响应后选择B，最终仍为B；原草稿切换输入有效性测试继续运行。 |
| SYS-294 | 不限库存改为数量时提交成文字，保存被拒绝 | 可空库存、日库存和终身上限使用明确整数输入，清空恢复null；提前提醒单独解释为空的含义。 | 浏览器总量10、每日0、终身5及清空回null；终身0被阻止，后端数据库字段仍为整数/null。 |
| SYS-295 | 草稿保存成功提示被随后的刷新清空 | 区分已完成写入和随后读取失败；成功信息不被列表刷新清空，发布结果也保留明确反馈。 | 真实保存后回读；注入503仅显示读取失败，仍保留草稿已保存；七类发布结果可见。 |
| SYS-296 | 有活动配置权限的副店长打不开小程序弹窗表单 | 弹窗及会员卡接入按各自业务权限读取最小商品选项，采用分页；一个依赖失败不清掉已读配置。 | 原副店长账号弹窗加载无403且可选择商品；原商品编辑/价格权限没有放宽。 |
| SYS-297 | 窄屏会员通知页出现横向溢出 | 微信接入移到管理员分区；长文字换行，容器及表单允许收窄，手机单列。 | 320/360/390/844浏览器无横向溢出；真机软键盘与原生壳仍需设备验收。 |
| SYS-298 | 同类经营设置混用元、分、基点和万分比 | 界面金额用元、比例用百分比；仅在接口边界精确换成分和基点，不使用浮点四舍五入掩盖额外精度。 | 真实保存回读1元=100分、1%=100基点、0.01元=1分、0.29元=29分；升级及推荐草稿。 |
| SYS-299 | 仍有内部状态值、类型值和记录编号直接显示 | 按存酒、消费、报名、联系方式和微信处理分别翻译状态；未知值给出核对入口；诊断编号折叠；报表导出同步。 | 11种报表状态界面夹具；实际PostgreSQL报表生成XLSX并解析；币种/非实收口径保留。 |
| SYS-300 | 面向员工的说明仍混入大量实现术语 | 按60项处置表替换或保留文案；中文选择替代代码输入，规则/登记项目/会员卡编号自动生成，技术接入参数保留在管理分区。 | 60/60源码锚点核对，详见独立CSV/JSON；保留观察事实与推测、收入与应付、受理与发送完成的区别。 |
| SYS-301 | 会员发布页未承载其他规则的发布动作 | 统一中心提供全部七类规则发布，显示生效时间及后续调整路径；补齐受保护的微信通知发布命令，三人分离和原请求重放。 | 七类界面路由/北京时间/反馈夹具；既有六类真实数据库发布测试，加新增通知发布/防自批/重放集成测试。 |
| SYS-302 | 酒水上架步骤编号互相矛盾 | 以现有原子收货发布流程统一四步：建物料/收货草稿→销售规格与配方→生成完整预览→确认入库并发布。 | 浏览器主指引四步，无第五步；商品说明对应第2—4步；原数据库收货与发布原子性测试保留。 |
| SYS-303 | 月度演出表折叠入口触控高度偏小 | 月度演出表折叠入口触控高度44像素，保留原生details/summary键盘与展开语义。 | 浏览器实测44px，Enter展开、Space收起；真实手机触感与读屏另验。 |

## 文案逐条处置

[60条文案修复表（CSV）](staff-copy-fixes-20260920.csv)和[结构化记录（JSON）](staff-copy-fixes-20260920.json)逐条列出原表达、处理决定、修后表达、验证方式及代码行。已逐条确认60个修后锚点；涉及流程的行同时连接专项浏览器或数据库验证。

必要信息继续保留：业务订单号、实收与应付的区别、存酒登记价值并非收入、原营业日、观察事实与推测的区别、独立审批、真实补偿凭据、微信受理与发送完成的区别。AppID、模板ID、密钥与消息字段保留在管理员接入分区，未为了消除术语而删除接入能力。

两项采用更符合实际业务的表达：COPY-07按既有“核对预览后原子入库发布”统一四步，不能写成提前入库再核对；COPY-30使用已有权益名称和适用等级识别规则，自动生成编号，不增加重复名称字段。

## 验证结果

| 检查 | 最终证据 |
| --- | --- |
| `npm run check` | 通过；通用测试1961通过、758条件跳过（数据库场景另跑）；小程序308通过；支付宝一致性、类型、lint和构建通过。 |
| 完整PostgreSQL回归 | 2320通过、1跳过，迁移至223。跳过的是需额外启用容器参数的独立备份恢复演练，本轮未冒充已完成该演练。 |
| 107个浏览器用例 | 首轮、受影响流程复测及可选夹具增补共四批，107个不同用例均取得通过结果，无未解决失败；不是单次107项全绿。 |
| 最终12项专项操作 | 全部通过；最后的持久保存补丁完成后再次全跑，覆盖丢回执、刷新、关闭页面再进入仍复用原操作编号和同一库存流水。 |
| 恢复与导航/时间单测 | 9项通过；涵盖员工隔离、并发点击、确定拒绝与未知结果、关页恢复、北京时间和发布路径。已包含在通用检查中，不重复计总数。 |
| 文案逐条核对 | 60/60修后锚点匹配，CSV和JSON保留处置决定及准确源码行。 |

[结构化验证汇总](quality/evidence/staff-control-fixes-20260920/validation-summary.json)、[107项逐用例结果](quality/evidence/staff-control-fixes-20260920/browser-case-results.json)、[12项专项证据](quality/evidence/staff-control-fixes-20260920/browser-fix-results.json)、[源码与构建指纹](quality/evidence/staff-control-fixes-20260920/source-candidate-manifest.json)、[17项修复矩阵](staff-control-fix-matrix-20260920.json)。

最后补丁只改变浏览器原操作保存及对应测试，没有改变已通过2320项回归的服务端或数据库；通用检查和12项专项已在补丁后重跑。七类发布页、全状态报表与成本入口含明确标记的界面响应夹具，不能冒充真实渠道发布、真实库存更正或真实资金操作。

数据库发布证据对应：基础积分 `loyalty-accrual-repository.test.ts`；等级/兑换 `loyalty-redemption-repository.test.ts`；等级权益 `loyalty-tier-benefit-repository.test.ts`；促销 `promotional-loyalty.integration.test.ts`；条款 `membership-terms-service.test.ts`；微信通知新增 `membership-configuration-draft-repository.test.ts`。这些服务端证据与七类界面入口验证配合使用。

## 首次失败与修正记录

- 首轮数据库：2317通过、1跳过、2失败。新增223迁移未写入原迁移清单断言，已补齐；既有售后恢复用例超过5000ms，原失败保留，后续复测未修改它的超时或业务断言。
- 第二轮数据库：2319通过、1跳过、1失败。新增通知测试把“7个目标配置”误当成“夹具全部配置仅7行”；同文件还有2个配置，已改为核对7个目标编号。通知发布、防自批和重放断言原本已通过。
- 第三轮数据库：2319通过、1跳过、1失败。新增导出测试误把同会员既有39份夹具当成仅2份，改为精确核对本用例的2份金额测试单，保留50.00/70.00、业务状态、币种及非收入口径断言。
- 首轮常规检查发现返回商品成本时未判空，已补齐并阻止迟到成本覆盖新商品。8个旧文案/来源契约文件的10个断言更新到新的业务表达，保留原权限和业务约束。
- 浏览器脚本中的多匹配选择器、文本换行、统一错误转译和确认弹窗角色已修正；不是通过删掉结果或延长超时消除失败。修前与修后日志分开保存。

- 首轮全量浏览器：89通过、13项因可选夹具未开启而跳过、5失败。三项来自旧控件文字定位；另外两项叠加/券日历测试仍走已拆分职责的旧路径。补上草稿页的直达入口，更新名称与路径，并保留原叠加金额、三人审批发布、打印恢复及滚动恢复断言后复测。

- 可选会员卡增补首轮18通过、2失败：旧测试仍要求手填项目编号，改为检查自动编号确实进入保存请求；两种宽度复测通过。其余三项默认未开启的权益复核/离店重做夹具也已开启并通过。
- 关闭页面恢复补验先失败：原sessionStorage会随标签页关闭而丢失，浏览器实际找不到恢复按钮，新增单测也失败。改为按员工隔离的localStorage后，9项单测和最终12项专项全部通过；没有删掉或放宽原库存唯一流水断言。

## 发布与现场边界

新增数据库迁移223，用于损耗独立复核。没有改生产数据库，没有扩大生产员工授权，没有真实发送微信、支付或退款。本轮没有提交、推送、合并、部署或上传小程序。

本轮完成的是已知17项候选修复与本地验证，不能证明所有状态、角色、网络故障的任意组合都无缺陷。正式发布后的门店岗位操作、真实手机软键盘/读屏、原生App权限与推送、实际微信送达、真实资金和实体打印仍是独立验收；[商业化清单](commercialization-pending-checklist.md)不因本地测试通过而关闭这些项目。

原审计：[报告](staff-every-control-audit-20260920.md)、[原问题清单](staff-every-control-findings-20260920.json)、[原功能闭环核查](staff-workflow-closure-20260920.md)。
