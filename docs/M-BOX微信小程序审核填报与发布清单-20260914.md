# M-BOX微信小程序审核填报与发布清单

适用AppID：wxdb9f2dc413484f2d  
运营主体：上海超嗨文化传媒有限公司  
客服电话：17621392152

本清单用于把协议正文、微信公众平台隐私保护指引、后台发布和真机验收对齐。仅上传代码不等于协议正文已经发布。侧面会话材料记录曾发现线上隐私政策为空、用户协议仅269字；本轮无会话直接GET隐私接口返回401，不能据此验证上述数据库状态。需在已授权会话或只读后台查询中核实，再决定创建新稿或使用已发布版本。

## 一、需要发布的文件

1. 《M-BOX微信小程序用户服务协议》：`MBOX-TERMS-20260914-V2`
2. 《M-BOX微信小程序隐私政策》：`MBOX-PRIVACY-20260914-V2`
3. 《M-BOX微信小程序第三方个人信息处理清单》：`MBOX-THIRD-PARTY-20260914-V2`
4. 内部个人信息保存规则：`MBOX-RETENTION-20260914-V2`

用户服务协议和隐私政策应在“我的”页面提供常驻入口，并在需要同意的入会或授权页面提供可点击入口。不得只在一次性弹窗内展示，不得出现空白、加载失败或仅有标题。

## 二、微信公众平台“用户隐私保护指引”填报文案

平台字段名称可能随后台版本调整，应选择与以下内容最接近的正式信息类型，不要用一个笼统目的覆盖所有功能。

### 1. 手机号

用途文案：

> 用于用户主动加入或找回M-BOX会员、核验会员身份、更换已验证手机号及主动提交需要联系方式的服务。通过微信获取时须由用户主动点击手机号授权按钮；新入会另需勾选协议。活动、预约和更正联系方式存在主动手填路径，应分别说明。拒绝不影响不依赖手机号的公开浏览和点单服务。

调用位置：`getPhoneNumber`组件。  
触发方式：用户主动点击。  
是否必要：仅对入会、会员找回、更换手机号和需要联系的活动报名必要。

### 2. 微信昵称、头像

用途文案：

> 用于用户自愿完善会员展示资料。昵称由用户主动填写后保存；微信头像由用户主动选择，当前仅保存在用户设备本地用于识别会员页，不上传M-BOX服务器。拒绝不影响基础服务。

调用位置：昵称输入框、`chooseAvatar`组件。  
触发方式：用户主动填写或选择。

### 3. 摄像头或扫码能力

用途文案：

> 用于用户主动扫描M-BOX门店桌面二维码，将菜单、订单和服务请求绑定到正确桌台。仅接收二维码识别结果，不上传或保存扫码照片、视频。

调用位置：`wx.scanCode`。  
触发方式：用户主动点击“重新扫码”等扫码入口。

### 4. 设备运动信息或加速度传感器

用途文案：

> 用于用户主动使用“摇一摇推荐”时判断是否完成摇动操作。原始加速度数据仅在设备本地即时处理，不上传或保存；用户可改为直接点击获取推荐。

调用位置：`wx.startAccelerometer`、`wx.onAccelerometerChange`。  
触发方式：用户主动点击摇一摇推荐入口。

### 5. 订单和交易信息

用途文案：

> 用于创建和履行用户提交的桌台订单，展示单价、数量、优惠、应付和实付金额，核对微信支付、退款、出品和售后状态，并防止重复扣款或错误出品。

信息范围：商品、数量、价格、优惠、桌号、订单号、支付和退款编号及状态。  
触发方式：用户提交订单、付款、退款或查询账单。

### 6. 预约和活动报名信息

用途文案：

> 用于处理用户主动提交的门店预约、候位和活动报名，包括安排座位或名额、联系确认、处理变更、到场和退款。

信息范围：称呼、联系方式、人数、时间、区域偏好、活动、备注和处理状态。  
触发方式：用户主动填写并提交。

### 7. 会员和偏好信息

用途文案：

> 用于用户自愿建立会员身份、查询积分和权益，并根据用户主动保存的生日月日、座位、服务、酒水、口味、音乐和饮食偏好改善门店服务。所有偏好均可不填、修改或清空。

饮食说明提示：可能包含健康相关内容，应在保存前显著说明用途、必要性和拒绝影响，不得用于无关营销。

### 8. 用户提交内容

用途文案：

> 用于处理用户主动提交的服务请求、投诉、订单说明、点歌和祝福语，并由相应门店员工跟进。提示用户不要填写身份证号、银行卡号、支付密码等与服务无关的信息。

### 9. 网络和安全日志

用途文案：

> 用于维持安全会话、防止错桌、重复提交、冒用和支付风险，以及定位服务异常。可能记录IP地址、请求时间、请求结果、接口路径、匿名设备标识和安全校验结果，不用于跨应用广告追踪。

## 三、微信授权触发规则

1. 协议复选框默认不勾选。
2. 用户可以分别打开并完整阅读用户服务协议和隐私政策。
3. 未同意时不得调用手机号等需要隐私授权的接口。
4. 用户拒绝后应返回原页面并保持普通浏览、扫码和点单功能可用；不得循环弹窗或阻断无关功能。
5. 当前代码已设置`__usePrivacyCheck__: true`。如不实现自定义隐私弹窗，应验证微信官方隐私授权弹窗在体验版和正式审核环境均能正常出现并回传结果。
6. 微信公众平台申报的信息类型、页面实际文案、代码调用能力和隐私政策必须一致。删除某项功能后同步撤下不再使用的申报；新增能力先更新告知和平台指引再上线。

## 四、后台发布参数

### 隐私政策

- `policyVersion`：`MBOX-PRIVACY-20260914-V2`
- `operatorName`：`上海超嗨文化传媒有限公司`
- `contact`：`M-BOX陆家嘴店，电话17621392152，企业微信客服入口位于“我的—联系我们”`
- `dataRetentionPolicyVersion`：`MBOX-RETENTION-20260914-V2`
- `thirdPartyRegisterVersion`：`MBOX-THIRD-PARTY-20260914-V2`
- `content`：完整隐私政策正文，不含本发布清单
- `contentSha256`：对最终正文原始UTF-8字节计算SHA-256；正文任何字符变更后必须重算

隐私政策草拟人与发布人必须是不同的授权员工。发布前确认数据保存规则和第三方清单已经由经营负责人认可，不能只填写版本名而没有实际规则。

### 用户服务协议

- 标题：`M-BOX微信小程序用户服务协议`
- 摘要：`适用于公开浏览、扫码桌台、点单、支付退款、预约活动、会员权益、服务请求和争议处理。`
- 正文：完整用户服务协议正文
- 版本：使用系统生成的下一版本号，不覆盖已发布历史版本

发布新版本时保留旧版及历史同意记录。新协议对既有用户是否需要再次确认，应根据变更是否实质影响其权利另行决定，不能一律强制，也不能对重大变更静默生效。

## 五、送审前验收

- 未登录的新用户可以从“我的”直接打开两份协议。
- 用户服务协议显示完整正文，不再只显示会员积分条款。
- 隐私政策显示运营主体、电话、版本、生效时间、第三方清单版本和完整正文。
- 断网或后台无政策时明确停止隐私授权，不展示空白页。
- 协议复选框默认未勾选；拒绝后仍可浏览菜单和普通点单。
- 同意后再点击手机号授权，允许和拒绝两条路径均有明确结果。
- 扫码、头像、加速度传感器、订阅消息、微信支付和企业微信客服均只在用户主动触发时调用。
- 自主点单和协助点单都能在付款前查看商品、数量、单价、优惠和合计。
- 退款申请金额不超过实际支付且尚未退回的余额；套餐部分退款可解释核算依据。
- 微信开发者工具、体验版真机和审核员冷启动路径各验证一次。
- 微信公众平台隐私保护指引已审核生效，内容与本包一致。

## 六、当前未闭环事项

1. 复核线上已发布隐私政策。侧面会话的“为空”尚未在本轮通过授权读取确认；如为空，使用现有草拟与发布流程，不绕过发布人权限。
2. 复核线上用户协议正文、版本及历史同意记录。侧面会话记录为269字会员条款；如仍为旧文案，以完整用户服务协议建立新版本，不覆盖旧记录。
3. 复核已发布联系方式保存规则和清理执行结果；内部保存期限仍为待确认方案，不能把文档版本名等同于规则已经发布或清理已运行。
4. 星驿付法定主体名称应以门店当前有效签约文件再次核对；如与清单不同，发布前同步修改隐私政策和第三方清单。
5. 饮食说明可能包含健康信息，正式上线前应确认页面是否需要单独明示同意或收窄为非健康偏好；政策文字不能代替产品端必要授权。

## 七、本轮整合边界

积分仅继续积累及原退款赠分冲回；积分用途和优惠券返还扩展暂缓。V2送审稿已对齐该范围。本地两端“我的”新增常驻协议入口，复用原读取与授权流程；正文仍通过后台已有版本化发布，不硬编码到客户端。本文不证明后台已发布、微信平台已填报或审核通过。
