# 1.0.0-rc.237 桌台名单与旧二维码兼容

按确认名单配置68桌，新增普通桌4人、VIP6人，D区室内。配置v27为28个改名桌指定原编号：发布同步识别原桌ID并保留旧内部编号，避免兼容前端上线前影响旧H5；新旧编号同时存在时整笔回滚，不自动合桌或移单。

H5初次扫码和等待刷新仅对明确的28组旧新编号兼容，服务器继续按二维码凭证认证，不放宽错桌校验。兼容前端正式切换并通过全部发布检查后，审计名单脚本原位改号。schema248，无新增数据库迁移，不更改账目、桌次及既有二维码凭证。

2026-09-25 03:18 CST已正式部署，03:19完成28个改名并读回。68张目标桌可用；11张未结束旧桌保留，因此可用总数79。29桌固定桌码及地图坐标仍待现场配套，SYS-385开放。回退rc.236前必须恢复28个旧内部编号，不能直接假定旧H5兼容新编号。详见[正式发布证据](production-release-1.0.0-rc.237.md)。
