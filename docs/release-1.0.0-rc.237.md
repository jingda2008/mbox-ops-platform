# 1.0.0-rc.237 桌台名单与旧二维码兼容

按确认名单配置68桌，新增普通桌4人、VIP6人，D区室内。配置v27为28个改名桌指定原编号，部署同步按原ID改名；新旧编号同时存在则失败回滚，不自动合桌或移单。H5的初次扫码和等待刷新仅对这28组旧新编号兼容，服务器仍按二维码凭证认证，不放宽不同桌校验。

生产已增加27桌、停用15张空旧桌，11张未结束旧桌保留。发现H5旧编号兼容风险后，28个内部编号已暂时恢复旧值，显示名称保留；本候选部署后再完成编号同步。新27桌和G1/G2的固定桌码及地图坐标尚缺；不伪造现场位置，不关闭SYS-385。schema248无新增迁移；不操作订单、支付、退款或桌次。本记录创建时未部署。

发布顺序：配置同步识别renameFrom并保留旧code，不提前创建或改号；兼容前端正式切换且全部发布检查成功后，用审计名单脚本原位改号。切换前失败时旧程序和旧编号保持兼容。改号后若人工回退旧镜像，须先审计恢复旧内部编号。
