# M-BOX 双任务并行开发开工说明

状态：用户已转发协调说明并授权开工；双方已确认独立工作区及文件分工。本任务正在修复 SYS-310—313、316—320，三屏任务已开始正式开发，SYS-314/315 首轮实库专项通过，完整集成验收尚未完成。当前没有合并或变更生产。

## 共同基线

- 2026-09-21 已重新 fetch origin/main，基线为 `77744bc4e9ed308546b630590bdfa5dfe5ce5b7d`，schema 224。
- 生产业务代码仍以审计冻结的 rc.216 为核对基线；本轮没有重新采集生产运行状态。
- 本任务：`检查代码并部署合并`。修复工作区 `/Users/jingda/mbox/mbox-audit-remediation-20260921`，分支 `fix/audit-remediation-20260921`。
- 审计来源单独保留 `/Users/jingda/mbox/mbox-comprehensive-audit-20260921`，已将审计报告和 SYS-310—320 清单副本放入修复工作区。
- 另一任务：`梳理后台界面与展示逻辑`，已建立 `/Users/jingda/mbox/mbox-three-screen-workflow-20260921`，分支 `feat/three-screen-workflow-20260921`，同一基线；原型复测后已进入正式实现，范围见该工作区 `docs/three-screen-development-20260921.md`。

## 拟定分工

| 范围 | 主要负责人 | 边界 |
|---|---|---|
| 吧台/后厨制作、待取、取走、撤回、设备与责任交接；SYS-314/315 | 三屏流程任务 | 正式界面与实际后台一并实现；按用户最新流程，不恢复已取消的姓名选择和手机二次确认 |
| 数据库运行权限；SYS-310 | 审计修复任务 | 先本地验证受限运行身份及所有worker权限；迁移/备份管理员分离；新取走表和接口的授权在整合时共同复核 |
| 付款/退款/会员奖账；SYS-311/312/313 | 审计修复任务 | 使用既有原份、应收调整与实际成功收退款事实；不改制作/取走状态，不改历史生产余额 |
| 损耗持久恢复、权限保存恢复、普通呼叫服务显示与确认、结账恢复；SYS-316—320 | 审计修复任务 | 修改前核对是否涉及共享命令执行器、服务任务或公共契约；交叉部分先约定后再编辑 |
| 清单编号、迁移编号、合并与发布 | 一个固定集成负责人，建议本任务承担 | 两边先各自交付提交SHA和验证证据，再集成；本文件不是生产发布完成或新的发布执行记录 |

## 首批顺序

1. 另一任务完成当前方案复测，输出最终流程、状态定义、接口/迁移需求、正式工作区及文件范围。
2. 首批优先做 SYS-318/319 的普通呼叫服务显示及确认读回，并行开展 SYS-310 本地运行身份门禁与授权清点；修改前确认它们没有与新取走接口共享状态含义。
3. SYS-311—313 合成一组会员资金闭环修复，先复跑最小故障矩阵；SYS-317、320 独立交付；SYS-316 优先采用损耗域持久回执，避免同时改通用命令执行器。
4. SYS-314/315 由三屏任务统一负责，本任务复用原探针独立验收。SYS-314 是已有 P1，必须单列接班修复交付，不能无限等待原型；新流程正式接入前先消除该阻塞。

## 双方先统一的业务事实

- 制作完成、可领取、已取走保留独立事实。用户最新决定为“取走视为送达”，替代此前“取走不算送达”的约定：取走按业务送达完成既有依赖，保留吧台取走确认来源，不声称已测量实际到桌。领取撤回、制作撤回和售后撤回必须区分。
- 原料依确切份数仅扣一次；取走、撤回不重新制作或重复扣料。
- 撤回绑定原取走动作和原份数；后来新增出品不能并入旧动作；需要核实实物仍在取餐区。
- 一块共享取餐屏不要求选择姓名；仍需要受信任设备权限、可追踪操作身份和审计，不能开放匿名公网写入。
- 暂停、重做、换品、转桌、关桌、退款分别保留原事实；新流程要适配旧未完成计数/关桌条件，不能仅改显示文案。

## 文件与数据协作规则

- 共享重点：`commerce-kds-api.ts`、`kds-repository.ts`、`kds-authorization-policy.ts`、`item-quantity-fulfillment-repository.ts`、`quantity-remake-fulfillment-repository.ts`、`item-quantity-projection.ts`、服务任务仓库、公共契约、`command-executor.ts` 与迁移目录。
- 一个共享文件同一轮由一个任务主改；另一方提交接口要求/补丁建议，待合入后再基于新主线修改。文件不冲突也要检查状态含义是否冲突。
- 每边独立测试数据库、端口和证据目录；复用PostgreSQL容器可以，但不能共享测试库或任意删停其他任务资源。
- 迁移预约：225 支付积分修复；226 报损永久回执；227 员工权限发布回执与修订号；228 三屏取走/撤回/交接（另一任务）。已经正式部署的001—224不得改写。228测试须使用225—227真实冻结SQL，不得伪造占位或绕过连续编号检查。
- 225的混合退款积分政策仍待用户决定。为不阻塞三屏开发，已授权复制225—227真实文件作为未提交、未发布的本地依赖快照v1，复制前后核对哈希；最终集成必须换成正式SQL，在新库重跑完整迁移和回归。
- 本任务共享文件范围：`normalized-app.ts` 仅运行身份/ready及报损事务注入；`normalized-contracts.ts` 仅员工权限接口；`guest-service-repository.ts` 仅普通服务状态与顾客确认读回；`guest-commerce-service-api.ts` 仅结账终态恢复。另一任务优先新增 `src/shared/pickup-workflow.ts`，接口注册优先复用其负责的 KDS 插件。
- `normalized-runtime-config.ts`、`normalized-app.ts`、`scripts/generate-normalized-runtime-config.ts`由三屏任务仅增加 `MBOX_THREE_SCREEN_WORKFLOW_ENABLED=false` 对应配置段，集成时逐段合入本任务身份修复，禁止整文件覆盖。三屏任务另负责 `normalize-runtime-env.sh` 新键白名单。
- 年卡撤回投影由三屏任务负责：`annual-daily-snack-claim-service.ts`、`customer-experience-repository.ts`、`AnnualBenefitManagementPanel.tsx`及两端member-center状态文案；原已履约claim和领取占用不回退，仅显示当前制作/领取状态。本任务的 `customer-experience-service.ts` 仅处理会员奖账补发。
- 复用本机PostgreSQL端口55443；本任务独立库前缀 `root_fix_20260921_`、`financial_fix_20260921_`、`frontend_fix_20260921_`，三屏任务 `three_screen_20260921_`，后者浏览器/API端口18921/18922。
- 只提交明确的文件范围，不把其他任务工作目录、生产数据、运行密钥或原型输出整包加入仓库。

## 合并与发布门禁

- 两边交付：提交SHA、实际变更文件、迁移/接口差异、专项结果、已知限制。
- 在最新main的独立集成工作区合入两边，解决代码与业务冲突；任何修正后的最终SHA重新验证。
- 联验：重复点击/丢回执、部分领取/撤回、新出品晚到、设备交班/账号停用、多代重做、转桌/关桌、付款退款与库存、受限数据库角色下API和所有worker。
- 通过相应完整CI后，由一个负责人从最终合并SHA构建/部署，并核对线上SHA、迁移、权限与健康。两个任务不得各自部署自己的分支。
- 原生上传、真实平板、手机同步、实物出锅/取走和现场验收另留证据，原型模拟不能替代。

## 可发给三屏任务的协调说明

请继续完成当前三屏优化模拟，再在已建独立工作区统一负责吧台/后厨制作、待取、取走、撤回及接班流程，同时纳入SYS-314（员工停用后批次/设备无法交接）和SYS-315（重做送达后原批次残留）。回报拟改文件、状态/API和迁移需求；共享接口先对齐，不自行合并发布。审计修复任务处理其余9项，最后统一集成验证。流程以用户已确认的不选姓名、手机不二次确认、取走视为送达且可撤回为准；保留操作来源，不重复扣料。
