# M-BOX 日志、并发与凭证安全专项 TC 1.0.0-rc.65

本专项新增 16 条 TC，不改写历史 213 条经营 TC 的编号和既有状态。自动化通过只能证明候选代码和隔离环境，不能替代部署后观察或真实支付联调。

| TC | 级别 | 场景与步骤 | 预期结果 | 当前状态 | 证据 |
|---|---|---|---|---|---|
| OBS-001 | P1 | 读取线上容器健康、重启、OOM、修订和投影状态 | 容器健康且无异常重启；修订与投影一致 | 通过 | rc.64 线上只读审计 |
| OBS-002 | P0 | 模拟数据库连接超时或仓储队列背压 | 返回 503 与可恢复提示，不记成普通业务 400 | 通过 | `error-classification.test.ts` |
| OBS-003 | P0 | 同时提交多项聚合状态写入 | 同一实例最多一个写事务进入；无连接池争抢 | 通过 | `postgres-repository.test.ts` |
| OBS-004 | P1 | 队列达到上限或等待超过时限 | 请求在占用数据库连接前失败；健康端点显示队列状态 | 通过 | 队列上限与超时自动化 |
| OBS-005 | P0 | 写入高峰同时读取最新状态 | 读取仍可获得连接；无 pool timeout；最终修订连续 | 通过 | 隔离 PostgreSQL 并发验证 |
| OBS-006 | P1 | 两台设备用不同幂等键重复完成同一快速服务 | 返回同一已完成任务，不新增第二组任务事件 | 通过 | `domain.test.ts` |
| OBS-007 | P0 | 两台设备用不同幂等键重复完成并送达同一 KDS | 返回同一 delivered 任务；订单不重复履约 | 通过 | `order-domain.test.ts` |
| OBS-008 | P1 | 写入全部客户行为事件类型 | 数据库约束全部接受且应用契约集合完全一致 | 通过 | 迁移 022 与契约集合测试 |
| OBS-009 | P0 | 请求 URL 含 token、微信 code、刷新令牌 | 应用日志只记录 `REDACTED`，不出现原值 | 通过 | `log-redaction.test.ts` |
| OBS-010 | P0 | Caddy 接收含敏感查询字段的请求 | 代理访问日志脱敏；路由和桌号仍可观察 | 通过 | Caddy 2.10.2 配置验证 |
| OBS-011 | P0 | 扫描新固定桌码并交换客户会话 | 地址只含桌号，凭证在片段；会话通过 POST 正文交换 | 通过 | QR、客户 API 与浏览器测试 |
| OBS-012 | P1 | 客户在响应完成前关闭页面或断网 | 记录客户端断开警告，不触发系统故障告警 | 通过 | 错误分类自动化 |
| OBS-013 | P1 | 300 人、60 并发访问客户入口、页面和健康端点 | 900 请求无 5xx，错误率低于 1%，P95 低于阈值 | 通过 | k6：错误率 0，HTTP P95 33.75ms |
| OBS-014 | P0 | 5 连接 PostgreSQL 下执行 50 写、20 强制新鲜读并启用投影 | 全部完成；连接等待 0；投影一致；写 P95 < 2s | 通过 | 写 P95 731.66ms，读 P95 112.06ms |
| OBS-015 | P0 | rc.65 部署切流后连续观察 30 分钟 | pool timeout、应用 5xx、Caddy 5xx 均为 0；投影一致 | 未执行 | 必须在阿里云验证环境切流后执行 |
| OBS-016 | P0 | 微信/星驿真实小额支付、查单、退款、回调乱序和 POS 联调 | 不重复扣款/退款/出品；渠道与账本一致 | 阻塞 | 缺真实生产渠道完整联调证据 |

## 当前专项结论

- 通过：14 条。
- 未执行：1 条。
- 阻塞：1 条。
- 候选代码可以进入验证环境发布流程；`OBS-015` 通过前不能宣布线上问题已关闭，`OBS-016` 通过前不能宣布商业支付正式可用。
