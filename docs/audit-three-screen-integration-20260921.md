# 审计与三屏集成最终本地交付

最终业务源码：`c87a01430fb3ef2e55e2d27b33250f5e4c69ac37`，分支 `fix/worker-health-integration-20260921`，工作区 `/Users/jingda/mbox/mbox-audit-three-screen-healthfix-20260921`。继承审计df7734f和三屏6d8446e的独立合并99ddeb，另外修复通用worker健康误报。原A/B、227编译基线、99ddeb及失败证据都保留。后续若增加纯文档提交，业务验证仍绑定本SHA。

**结论：本地集成及必要修复完成；尚不能主线合并部署，也没有已合格可投入生产的受限恢复业务目标。** 原75文件外发确认没有扩展为本次137文件业务候选；未push、创建PR、合并主线、构建正式发布镜像、部署或上传小程序。

## 实际修复

1. 保留双方脚本、配置与迁移测试，完整包含225–228，最低就绪schema提高到228。
2. SYS321：会员公开日历引用不存在的claim.order_id，真实低权PG复现42703后改为gift_order_id。真实公开日历的待取、取走和撤回投影测试通过，历史权益核销不逆转。
3. SYS323：共用调度器在跨周期保留每个worker未恢复失败。调度跳过/未配置返回null不算恢复；只有对应worker实际成功且没有原有部分批次失败才清除，空批和数值0可成功。原打印非阻断例外、启动/停止、并发共享单次执行及过期判定保持。不新增自动停写。

最终编译/Web649项中，相对99ddeb仅 `dist-normalized/server/normalized/background-worker-coordinator.js` 变化，其余648项逐字相同；原99ddeb全部产物和源码保持。见 `candidate-source-build-manifest.json`、`build-delta-verification.json` 和 `frozen-predecessor-verification.json`。

## 验证及来源

| 验证 | 本次结果 | 边界 |
|---|---|---|
| 新4项调度回归 | 旧99调度器2失败/2通过，修后4通过 | 测试先抓住独立worker及partial失败被跳过清空；原99工作区未改 |
| npm run check | exit0；2099通过/838环境条件跳过；双端320、运行器9、类型/架构/构建通过 | 有既存lint/体积告警，不声称零告警；组间重叠不累加 |
| 最终标准PG | 2447通过/1专用恢复条件跳过，exit0 | 随机独立库与真实低权限LOGIN，001–228迁移完整 |
| SYS323真实入口 | 11条有界断言通过 | 同PID13936持锁503→4个skip仍503→解锁后约30秒实际worker提交审计→200；原份/重做/取走撤回不变量不变，KDS_ACTIVE1保持 |
| 数据库恢复工具 | 前序99ddeb独占容器1/1通过 | 4个工具/测试文件及228迁移均未变，复用已封存证据，没有假称在新SHA再次执行 |
| React/API/PG三屏 | 前序99ddeb 20桌80份1/1通过 | 测试、入口与全部Web资产未变，复用证据；不是现场四员工吞吐或真机弱网验收 |

相关继承证据235个源文件逐字相同，见 `inherited-evidence-verification.json`；没有重复计数或用单元测试代替正式入口锁故障。SYS323实际测试在本机production模式启动正式normalized-server，外部网络被限制为本地PG；不是真实生产切流。资源清理和哈希见runtime报告及manifest。

## 恢复资格：227已明确不合格

前序三组同库228→227真实接续证据在 `../audit-three-screen-integration-20260921/`，本轮不重跑已明确不合格的旧候选。此次仅改worker健康，相关履约代码/SQL仍与前序逐字相同。

- 旧227新增制作HTTP500/PG42P10；接班后旧版拒绝现任B，却允许原人A实际释放设备。
- 新版启用pickup后手机deliver被409阻止；旧版200把份数改delivered，却没有新pickup回执且revision仍2。共享历史和恢复接口不可用。
- 旧closure漏掉撤回后未取重做；实际worker被数据库55000拒绝、事务回滚，未实证误关桌或数据丢失。
- 年卡/金融限定样本通过：原核销重放，新键核销/预留拒绝；普通80元退款20后积分/成长各60、库存不变。经济应收仍80且部分退款；未验证新权益申请发放入口，不能据此宣称全部金融/三屏可回退。

准确恢复目标表和两路线见 `bootstrap-design-228.md`。历史rc216、仅身份补丁旧版、完整227均未取得本轮恢复资格；c87a014是本地候选，缺正式镜像与bootstrap资格。后续稳定恢复目标需完整228业务语义，不能只修索引、恢复旧接口、压掉健康告警或换镜像名称。

## 尚待决定和验收

- 混合可计分/不可计分商品与真正超收退款的分配政策，以及待核记录的审批关闭流程。现有阻断记录不是逻辑闭环。
- 首次运行身份过渡：真正224/228 bridge或经批准维护后向前恢复。两路线未选择；writer归零、外部在途支付/退款/回调、不可被restore抹掉的epoch/journal、备份/OSS读回与故障演练均须正式完成。
- 原具体GitHub外发确认、后续统一候选的明确外发范围、远程CI、最终版本/镜像、部署及平台/门店验收。

自动审批此前拒绝推送原75文件候选到GitHub，理由是内部审计与实现外发的具体目的地/完整内容授权不足；没有换工具或任务绕过。此次本地新增修复与证据不自动包含在原确认范围。
