# 1.0.0-rc.228 客人下单恢复与发布历史核验

包含rc.226/227微信身份权限与内建SHA256修复，业务源码相同。rc.227全量CI通过（数据库2711、浏览器99通过/17跳过），正式包、备份及OSS回读完成；切流前普通发布入口将已明确撤回的旧维护记录误判为未结束，因此未上线，生产仍rc.225。

普通发布现在通过只读核验承接已完成维护计划明确绑定的撤回记录：完整日志哈希链、原计划摘要、撤回凭据、已实际provisioned的schema、现运行镜像和公网ready必须一致，旧守护与入口必须停止，回调队列为空。未知未结束记录或证据变化继续拒绝。后续同schema普通发布也可核验；schema变化仍走显式维护。没有删除/重写旧日志，没有新增数据库迁移或权限。

维护控制器21项及相关发布流程测试通过；生产只读核验确认现有完成记录覆盖旧撤回记录，productionWrites=0。固定SHA CI和正式切换仍待执行。详见[故障记录](guest-checkout-incident-20260923.md)。

## 正式生产结果

2026-09-23 22:51 CST，提交`7e0b457a8c96752126d5ac472d5e8a53ef29dfc4`已通过原发布入口上线，状态completed。标签CI35872773662及发布35872773544成功；PG2711通过、浏览器99通过/17跳过。schema242未变化，备份及OSS回读通过，保留rc.225回退镜像。公网ready与四入口浏览器渲染均核对新SHA，受限运行身份保护仍成立。

现场已观察到真实微信登录200；截至22:55:03尚未看到新的客人订单与付款，实际下单/实付待回读，不据此关闭现场验收。排障、一次自动回退及部署机兼容处理详见[故障记录](guest-checkout-incident-20260923.md)。无需上传微信小程序，受影响匿名会话应重新扫本桌二维码。
