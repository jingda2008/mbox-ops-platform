# 1.0.0-rc.192 桌台售后、库存、打印与高峰点单统一发布

合并近期未提交的 REQ-01—19 及关联修复：扫码换桌归属和旧响应隔离、桌台消费明细、员工单品暂停/停止和一次退款审核、数量制作与送达、换品/补送/重做、实付退款上限、原价明确单套餐拆退、退款与库存实物处置、日期范围扎账与小票表达、打印路由、支付错误分类，以及 REQ-15-A 优先菜单/点单和按需缩略图。

## 版本与范围

- 以 origin/main 5ea213cc1b6d05e4ca9545e1fd1089e4b1281ced 为基线；近期本地和远端已提交分支均已包含在主线。稳定目录的13处修改是本次开发目录的子集，保留原目录。
- 新增 normalized schema198—202；旧迁移不修改。门店配置2026.09.13-v24，服务人员可申请退款，已支付仍由另一人一次审核，退款不超过原实付剩余额度。
- 数量售后正式配置 MBOX_QUANTITY_AFTER_SALES_ENABLED 默认false，验收候选通过后本店显式启用；同版本设false停止新增、保留原申请和制作/退款/实物恢复。不得在已有新数量事实后直接退回不认识schema202业务事实的rc191；故障优先同镜像关闭新增并恢复原流程。
- 积分仅保留积累及原退款赠分冲回。积分用途、优惠券自动返还和部分优惠重算继续暂缓。缺历史单点价、多套归属不足保持人工核价，不能自行分摊。
- 两端小程序代码纳入版本控制；微信包上传、审核和发布分别记证据。支付宝原真实支付关闭状态保持。服务端发布不代表小程序平台发布。
- 保持已验收打印链路；Windows升级/诊断/恢复工具源码一并存档，不在本次后端部署时执行终端升级。被撤回1.0.3工具保持历史撤回标记，不能重新当最新安装包发放。
- 五份微信协议V2保持送审稿；主体、第三方及保存期限等事实未核实的内容，不通过本次代码发布写入正式协议版本。

## 验证与回退

此前本地全量数据库、浏览器、两端编译和480次性能模拟证据分别见 requirements-current-status-20260913.md 与 req-15-a-order-performance-20260914.md，仅证明各自源码快照。发布目录重新固定版本，执行完整CI、数据库与浏览器回归，并补配置开启/关闭验证。不得把模拟性能视作真机或高峰结果。

新迁移曾以有数据197/201数据库演练；正式发布仍必须读取现网迁移谱系并创建、上传和回读备份。先起隔离候选，验证后由标准发布脚本切流。最终提交、CI、镜像、备份、正式入口和小程序上传身份在实际完成后追加，当前准备中。

### 2026-09-14 01:29 CST 候选修订

首个候选dfa2a8f全量手机回归65通过/11条件跳过、1失败：出品搜索框高24px，低于现有44px触控标准。本次局部修复出品搜索和数量输入触控区，原失败记录保留，新提交作为新的候选重新完整CI，不沿用旧候选完成状态。首轮本地check、数据库2145通过/1跳过、配置28项、支付适配32项、发布链路可移植34项及支付宝23页编译通过；Linux发布锁由新CI核实。

### 2026-09-14 01:44 CST 关桌后售后窗口刷新竞态

第二候选a27da7c的PR CI34771754430中，质量、数据库/HTTP和性能通过，浏览器65通过/11条件跳过/1失败。根因：TableOrderStatusPanel挂在当前活动桌次详情下，5秒桌台轮询观察到关桌时卸载详情，连带卸载正在处理原商品的售后窗口。本地快速操作可能早于轮询完成，不能用原成功结果忽略云端失败。

增加确定性回归：关桌后立即触发现有可见性刷新，确认旧桌详情消失，再验证原售后窗口保持，完成撤回、分份实物处置、丢回执幂等恢复和岗位知悉。修复前稳定失败（窗口不存在），现改为复用现场工作区的独立售后窗口；服务器金额、库存、权限和轮询频率不变。新候选重新运行完整CI，失败记录位于artifacts/release-consolidation-20260914。


## 2026-09-14 02:17 CST rc192实际交付与配置缺口

PR220已合并，发布提交98e93a83453e2fcda2a3bf543455b26ae207d7d2；PR CI34772520647、标签CI34773114456及发布CI34773114471均通过。本地完整浏览器67通过/10条件跳过，配置28项通过，Linux发布检查由同次云端质量验证。北京时间02:13:42，rc192后台正式部署完成；schema202、镜像摘要sha256:02ce9179b4e423563ad20f32f06e9735276f288219e880501a905915d8c5ef79，API与首页/扫码/预约/员工入口通过，数据库备份和部署完成证据均经OSS上传回读。

微信rc192开发版已通过官方工具上传，149文件前后哈希一致，包1003009字节；最新独立包已在微信工具打开，首页和184款菜单可读。尚非微信审核/正式发布或真实手机验收。支付宝23页正式配置候选已编译，当前工具关联列表及精确AppID搜索为空，上传按钮不可用；需要有现有应用权限的账号，真实支付继续关闭。

最终线上回读发现数量售后开关缺失：生产规范化脚本的配置保留名单遗漏MBOX_QUANTITY_AFTER_SALES_ENABLED，并在完成部署时把过滤结果写回正式配置。rc192运行正常，但不能声称数量售后已开启。新登记SYS-206，修复保留名单并做真正规范化回归：修复前失败，修复后validation/production两环境下missing/true/false/typo各重复两次均通过，既有支付参数保留；28项配置解析测试通过。准备rc193小修版继续标准发布，其他独立功能不回退。

原目录、历史备份和旧候选记录均保留；积分用途/券扩展暂缓、协议V2送审稿、已验收Windows打印及现场高峰验证边界保持。
