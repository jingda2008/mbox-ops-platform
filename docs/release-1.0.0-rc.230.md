# 1.0.0-rc.230 受限账号配置与预约闭环

修复受限运行账号在会员配置影响审批、叠加规则发布、优惠报价消费及预约政策读取中的行锁错误。schema244以不可变事实读取、可变购物车锁及共享门店事务锁保持并发语义；不授予历史表UPDATE。

schema245提供固定租户/门店范围的通知规则管理、仅草稿子项替换和赠品商品KEY SHARE锁函数，运行账号不能直接更新政策或商品映射表。原影响审批事实、三人分离、员工权限、准确修订版、未来排期及原治理触发器保留。新版最低schema245，迁移限锁等待1秒/单语句5秒，不修改正确收退款账目。

五组真实受限LOGIN43项通过，包含跨范围拒绝、审批事实/贡献事实不可绕过、已审批子项保护、直接表写入拒绝及赠品映射并发删除阻塞。最终源码检查、旧新应用兼容、固定SHA完整CI与正式发布仍需完成。本候选不等于生产已上线；成本来源和实物打印/出品/取走不虚构验收。

隔离从实际rc.228/schema242新建基线后按标准迁移升至245：旧rc.228与新程序均ready/writeEnabled=true，六处既有函数OID/属主/ACL保持，新增限定函数满足运行身份检查；本机迁移耗时36ms不能当作生产时延保证。全量npm run check通过（既有分包警告保留）。固定SHA CI、原生镜像和正式部署仍待完成。
