# rc.222 财务、吧台及三屏发布验收记录

更新时间：2026-09-23 13:55 CST。rc.222未部署；原生演练在恢复验证失败，转rc.223最小修复候选。

## 版本与范围

- [PR279](https://github.com/jingda2008/mbox-ops-platform/pull/279) 已合并，标签 `v1.0.0-rc.222` 指向 `198b96ccd337cbfdee19649c1c9a2e3776e14ba7`。
- 受验候选 `f49f2b4a0b6ff2e329a5302a8e595077a33a9bce` 与合并提交 Git tree 完全相同。
- 包含 rc.220 完整财务、吧台及三屏实现、原字节迁移225–242，并保留rc.221兼容修复。
- 镜像清单摘要 `sha256:172c6e58e913d6b639f39861df59ee3319c068d984fc07819cd02ec01ae1d4c5`；平台镜像摘要 `sha256:535e68cb51ef0961333cb54545027af1ba539cf78be73f537eb496a4051387e8`。
- 发布清单SHA256 `7bdaf1db25f21902baefa4b51d79f9b2999a56cbd107d67907649a1587936bf6`。本地核验镜像归档、15个部署脚本、2个配置，共18个文件。

## SYS-340 根因与最终验收

第一轮完整候选重现30秒看板超时，实际执行计划显示120单造成任务扫描864000次。来源查询修正后，343项压力检查虽通过，最大耗时仍有23.7秒；继续定位到制作批次查询约140万次重复扫描。两处均改为保留门店scope的逐层主键关联，未改业务过滤、排序、数量、权限或超时。

最终固定候选在独立PostgreSQL及真实受限LOGIN下，通过原120人脚本：

| 验证 | 结果 |
|---|---|
| 压力及业务阶段 | 2647次HTTP，343检查通过，0错误 |
| 厨房/吧台刷新 | 480次全200；P99 41.57ms，最大47.17ms |
| 全部HTTP长请求 | 超过2秒的请求为0 |
| 正常收款、制作、领取、关桌接续 | 793检查通过；未用SQL强写终态 |
| 独立低权只读核账 | 18检查通过 |

合成事实：30桌正常关闭；150笔确认现金款合计4704000分，12笔服务补偿退款1200分，净额4702800分；480份实物均有未撤回领取，逐桌欠款0；两原料各9760、预留0。30笔原simulation pending保留，不能算实际收款。36个虚拟打印任务不证明实体出票。

来源查询新增回归在旧代码失败、修复后通过。批次回归在已统计的fixture上旧代码也通过，不能声称该用例单独复现25秒故障；批次根因以原压力执行计划证明。保留全部失败数据库、初始日志及中间版本。观察器后段事件达到上限，不声称SQL事件覆盖全程；HTTP观察完整。

## CI与发布包

- 候选CI [35819233367](https://github.com/jingda2008/mbox-ops-platform/actions/runs/35819233367) 全部通过。
- 标签CI [35820397159](https://github.com/jingda2008/mbox-ops-platform/actions/runs/35820397159) 第一次2701项PG及HTTP通过，但15分钟作业时限中断，边界检查未执行；第一次不能算通过。原代码原条件重跑第二次完整成功。
- 正式发布 [35820397085](https://github.com/jingda2008/mbox-ops-platform/actions/runs/35820397085) 第二次成功；首次因等待CI未成功结束，未启动生产切换。
- 原生维护演练 [35820460006](https://github.com/jingda2008/mbox-ops-platform/actions/runs/35820460006) 第二次在恢复验证失败，原容器与临时凭据清理完成；首次仅等待正式包，未进入LAB。诊断复跑35823933100及SYS-356后续见商业化清单。

## 生产准备与尚未完成项

实际生产仍为rc.221/schema224，写入开启、后台健康。已有受限应用账号启用LOGIN，准备配置补齐数量售后、制作批次、三屏开关；服务容器和数据库schema未切换。

实际RDS对9月21日冻结schema224备份的隔离恢复通过，临时库删除，未替换源数据库。受限应用身份符合最终源码验证器，应用/备份/管理员实际LOGIN指向同一物理库；管理员及集群维护服务能读取同一集群身份且具备所需维护属性。官方入口dry-run通过。

新计划绑定当前源版本、目标镜像、系统服务、容器、不可变适配器以及上次未迁移撤回的journal/receipt哈希。旧记录保留。正式切换仍须新备份、恢复验证、OSS读回、242迁移、受限运行服务及公网验证，不能由上述准备代替。

## 保留边界

- 积分已消耗、其他批次后来抵欠、原单补款恢复的到期规则仍为人工待核；没有代替经营方决定有效期。
- 本次后台、员工网页和数据库发布不代表原生小程序平台上传、审核或真机验证。
- 真实支付渠道、实体打印、门店三屏设备和现场峰值体验，按各自已有证据及未完成项记录。
