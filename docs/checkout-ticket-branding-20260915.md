# 结账单名称与陆家嘴中心抬头

2026-09-15，SYS-229，目标1.0.0-rc.202（包含rc.201）。开发Codex，实体票验收为门店打印负责人。

## 事实和需求

用户提供的W03票据对应00:37:01整桌消费账单，原票种order_summary，标题“订单汇总单（非制作指令）”。只读查询匹配原始单号和明细：原订单金额108元及300元，实际收款/净收108元、退款0元。金额并不等于已经结清；不改金额、不虚构付款方式。

标题改为“结账单”，去掉标题中的“非制作指令”。加入“陆家嘴中心 L+MALL”商场字样，保留M-BOX品牌、桌台、人数、单号、经营日期、时间、经办、品名/单价/数量/小计和真实收款/退款。参考图中的测试标记不进入正式票。

## 实现与兼容

新整桌快照增加可选documentRole=checkout，保持schema1和order_summary收银路由；旧快照无此标记仍用原标题。网页入口改为“打印结账单（整桌）”。分页和补打保留用途字段及原明细，不重新计算历史金额。

后台通过现有title和subtitle传递名称与商场。HTML商场字样置于标题上方；门店现有1.0.9桥在标题下方居中打印副标题“陆家嘴中心 L+MALL · 本桌次完整消费账单”。沿用现有Logo、字重和分列，无需安装新桥；没有修改GBK/RAW、驱动、服务、切纸或队列。本次不宣称与参考图像素一致。

00:44只读心跳显示在线桥1.0.9；已找到原1.0.9-r1包供离线兼容复核。软件版本心跳不能证明文件哈希或实际纸张效果。后台渲染与离线测试通过后，仍由门店实际新结账单核对中文、标题、商场字样、金额和出纸。

## 验证与交付

模板/来源/原桥20项、隔离数据库打印/历史/补打14项通过，完整check1800通过/710条件跳过。1.0.9-r1包21文件哈希校验通过，包SHA256为4a8d72f736d26a26af5c406f81510534cdfbd4b0e99a4cafb03f746157213e00。离线布局比较确认仅标题/副标题变化及原标题括号行移除，Logo抬头、正文和金额不变；58/80mm网页预览无横向溢出并已目视核对。证据在开发工作树artifacts/checkout-ticket-20260915。提交合并及标准生产部署待执行。未生成真实打印任务、收款、退款或库存交易。

## 追加编号范围（2026-09-15）

用户明确要求手动整桌结账单也使用新编号规则。新手动账单使用YYYYMMDD-HHMMSS-六位数字：日期/时间为开票北京时间，六位数字沿用既有SHA256模1000000算法，以源打印请求UUID参与生成。票据号一次写入新快照ticketReference，旧桥可直接打印，无需替换门店1.0.9。后台作业sourceReference仍保留原桌次UUID，纸票尾部备注也记录原始桌次追溯码。重复请求、分页及补打同号；主动生成新账单使用新请求。六位尾码不承诺全局唯一，不能用作资金/核销去重主键。

自动关桌生成的整桌结账归档单已经采用此前规则，本次不改；其他自动票种仍为原编号。旧结账票补打保留旧号，不回写历史。rc.201于00:54:40合并PR239但未部署，追加编号后使用rc.202候选，保留旧标签不覆盖。

编号专项34项（模板/来源/桥/隔离DB）通过，覆盖实际生成、重试、原始桌次追溯、补打保持与新请求新号。1.0.9离线单号、源追溯、Logo和财务行比较通过；58/80mm新编号预览无横向溢出。最终check及CI/部署随后记录。

最终39项专项通过，覆盖连续补打长备注后原桌次追溯码仍完整；完整check1800通过/710条件跳过。补打从不可变作业sourceReference恢复追溯，避免递归备注截断。准备提交合并rc.202并执行精确版本CI与标准部署。

## 2026-09-15 01:03 CST 暂停部署

按用户最新指令暂停部署。PR239/240已合并，最终功能提交72b463ef8ad143aa2f6f913edcbad1f801713eaa，rc.201/rc.202标签保留；未执行这两个版本的标准部署入口、数据库迁移或生产切换。线上仍为rc.200（b4d34ed）。CI及GitHub候选产物与生产部署分开；恢复部署须有用户新的明确指令。

用户随后明确要求检查退款审核权限侧会话并一起提交、合并、部署，暂停指令解除；集成候选改为rc.203。原暂停事实保留，新的部署结果后续另记。

## 2026-09-15 01:28 CST 正式部署完成

PR239/240/241已合并，rc.203于01:25:16 CST标准部署完成；线上6775481、schema203、四条公网及浏览器验证、OSS备份和完成证据通过。数量售后及支付/服务号配置保持；原岗位权限、员工例外和实际审批额度未变化（额度表仅部署更新时间刷新）。[完整交付记录](rc203-delivery-20260915.md)。原申请未代审核或退款，人员具体额度及实体票仍待门店验收。
