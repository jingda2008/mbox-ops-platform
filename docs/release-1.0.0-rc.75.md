# M-BOX 1.0.0-rc.75 本地候选说明

日期：2026-08-13
状态：工程候选验证中，尚未部署
线上基线：`1.0.0-rc.71`

## 本轮范围

- 完整保留`rc.72`已经验收的固定桌码、预约申请、操作提示、权限配置与规范化授权行为。
- 保留`rc.74`的OCI索引、Linux/AMD64平台清单与镜像配置摘要链校验。
- 将候选容器和切流后的健康门禁改为规范化服务真实契约：提交号、`normalized-core-v1`架构标识与数据库迁移版本。
- 启动候选时显式绑定精确提交号；镜像摘要继续由不可变发布清单和OCI摘要链校验，不依赖应用自行回显。
- 保持数据库备份、迁移、候选容器、健康检查、切流和自动回滚门禁不变。

## 自动化证据

- 发布脚本Shell语法检查：通过。
- 发布与云证据专项测试：16项通过。
- 规范化发布健康契约静态回归：通过，旧整店JSON投影字段不得重新进入发布门禁。
- `npm run check`、规范化测试和镜像构建将在不可变合并提交上重新执行并随发布证据归档。

## 发布结论

本候选仅用于开发验证。GitHub托管执行器受账户计费门禁影响时，不得伪报CI通过；允许记录精确提交上的本地替代证据，但商业发布门禁继续关闭。真实支付与退款、打印/耳机/摄像头硬件、备份恢复和员工现场演练完成前，不得标记为商业正式版。
