# 1.0.0-rc.223 恢复验证扩展属主权限修复

rc.222在隔离原生维护演练的恢复验证阶段失败，未切换生产。诊断演练35823933100确认`btree_gist`原属主缺少临时恢复库CREATE权限；原日志与清理凭据保留。独立真实PostgreSQL回归进一步确认：当临时恢复库归维护管理员所有，归档中的可信扩展属于非超级用户且无数据库CREATE时，按原属主建扩展被拒绝。

本版本只在临时恢复库的同一事务中，按需授予原扩展属主CREATE、创建扩展并收回该权限。若创建失败，事务回滚；源营业库、角色成员关系和原ACL不放宽。保留原扩展属主和最终完整证据比较。

增强恢复回归在rc.222失败，修复后通过，覆盖成功恢复、故障清理、源活动连接不受影响、原属主保留及无新增永久CREATE权限。真实RDS对9月21日schema224冻结备份的复验已通过，临时恢复库删除、营业源库未替换；恢复脚本SHA256为`6a9988a052c68ef4becb069364a7d2144f7f591d9cdd6c67587432419a3ae843`。最终CI、原生正常及向前恢复演练、实际生产结果仍分别记录。

财务、吧台、三屏业务代码和242条迁移保持rc.222。已通过的120人业务验收及首次失败记录见[完整记录](rc222-release-verification-20260923.md)。不将CI、模拟或正式包发布当作实际部署，也不覆盖rc.222不可变标签。


## 固定版本与候选验收

- PR280合并SHA：`a423934511ef289a3dbff90734f24242275b98b6`，与候选`044a49c6f4b3bb5aad101c6afabd92a77612b689`树一致。
- 候选CI 35824994681全部必需检查通过；原生候选完整升级35826790804通过，最终schema242，六类非空财务事实、重复回调单次入账、受限运行身份和资源清理均核验。候选构建镜像不代替正式镜像验收。
- 演练35825190644、35826070340保留为失败证据：LAB子进程继承上传文件清单stdin导致归档缺文件，正式校验正确拒绝；独立工具分支3378c009隔离stdin后通过，生产发布程序和检查未削弱。
- 标签CI 35827817235、发布35827817038及正式镜像正常/向前恢复结果分别后补；当前尚未切换生产。


## 2026-09-23 15:22 CST 正式包与上线准备

标签CI 35827817235第二次完整通过，数据库任务14分57秒、镜像及最终质量证据均通过；第一次2701项PG测试通过后HTTP流程被15分钟总时限取消，原失败保留。正式发布35827817038第二次通过。

镜像摘要`sha256:6f9406529a2331a8a9e6e119cae9ba0e91112c2c7ac882240f5c3452cfd1f729`，平台镜像配置摘要`sha256:dadb11abe431d6b935cff9cc8581e0df989f0495a6a916a528b57255b341559f`；本地核验归档、15脚本与2配置共18文件。独立发布工作树npm ci、真实浏览器启动及唯一正式入口dry-run通过。新计划`maintenance-20260923151550`绑定正式SHA/镜像与现场rc.221，旧撤回记录复验通过。尚未执行生产切换。

SYS-357的三处CI执行预算修复由PR281合并（`46f1b850`），全量CI 35829648903通过，数据库任务15分6秒；全部测试/阈值及失败拦截不变。此后续CI/文档提交不改变rc.223镜像、业务源或242条迁移。正式镜像原生正常及向前恢复演练35830617358运行中。


### 2026-09-23 16:09 CST rc.223原生演练通过及实际维护对账阻塞

正式镜像原生35830617358正常升级和故障后向前恢复均通过（22分16秒），六类非空财务表、重复回执单次入账、schema242、受限API/worker身份和清理均核验。随后实际生产正式入口已执行：备份、真实RDS恢复验证、OSS读回、迁移242与候选启动通过，但原有未决支付/未消费终态回执导致资金门禁拒绝切换。当前生产保持维护，旧版本不可接管242；不能引用15:22历史快照称线上仍正常运行224。

| 编号 | 优先级 / 负责人 / 目标 | 问题及处理 | 关闭条件 |
|---|---|---|---|---|
| SYS-358 | P1；Codex发布负责人；rc.223维护恢复 | 原预检已识别历史未决支付及未消费终态回执，自动worker对stopped或尚未到期记录不处理，维护期无法自动收敛。现有阻塞指纹与升级前预检一致；准备按原交易消费可信回执及主动查单，不增加收退款、不直接改业务SQL、不修改原资金基线 | 正式同SHA重入期间，以实际受限运行身份、现有验签/金额绑定/幂等命令完成原事实处理；资金与回调门禁清零、正式入口完成、公开ready与营业页面验证。渠道仍未知不得假定失败或入账 |

本地隔离真实PG/RSA历史查单、回执绑定及幂等专项3文件20项通过。受限恢复辅助程序固定SHA、冻结预检指纹、原始交易标识和正式控制器的可写候选身份，仅输出状态枚举与哈希；正在同SHA正式重入，尚未宣布恢复营业。

#### 变更记录

| 时间 | 变更 | 边界 |
|---|---|---|
| 2026-09-23 16:09 CST | 正式原生两场景通过；实际升级242后对账门禁失败，新增SYS-358并按原交易恢复 | 保持维护及原备份/epoch/日志；不伪造资金清零、不恢复旧库；生产完成证据待补 |
