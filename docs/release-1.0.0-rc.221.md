# 1.0.0-rc.221 schema224兼容修复

基线为正在营业的rc.216（5b9d929499b1d8cb0eb3a0c0668604e9a398f1fe），保留001–224迁移原字节，使用原deploy-release.sh。当前主线rc.220尚未上线，故本候选暂缓其升级内容；原标签、审计和发布记录完整保留。

| 本次纳入 | 验证重点 |
|---|---|
| 扫码共享门店锁及顾客串行锁、有限重试 | 120人扫描、同顾客跨桌、撤销二维码、受限数据库登录 |
| 支付操作和退款的父记录先加锁及终态重查 | 订单/支付/退款一致性、自审拒绝、并发请求 |
| 推荐修改重放前权限校验、移除不必要行锁 | 撤权后重复请求拒绝，受限角色可用 |
| 星驿查询商户绑定校验 | 错商户响应拒绝 |
| worker健康状态保留失败直到实际成功 | 跳过执行的tick不能清除失败 |

暂缓225–242全部迁移及依赖它们的积分冲回处理、损耗持久化凭据、权限版本、吧台制作取走三屏、财务欠款/补偿重收/消费结算投影、持久服务请求、新数据库运行账号和维护升级控制器。这些未上线内容仍需原审计验收，本次不称全部问题已解决。小程序保持生产基线，不上传；不变更Windows PrintBridge。

验证进度：npm run check、构建、发布系统测试、测试运行器9项及支付适配器37项通过；独立PostgreSQL为2354通过、1项既有跳过。浏览器与远端CI待完成；部署未执行。首轮PG的3项失败记录保留：移植测试误用了schema232标签权限和新版付款终态错误，已恢复schema224测试契约；合并身份只由模拟旧生产权限的管理员测试连接执行，扫码仍使用真正受限LOGIN，不称生产已完成权限分离。保留原维护epoch/journal及撤回凭据，发布前核验其无活动控制器且schema未迁移；不删除记录或伪造completed。
