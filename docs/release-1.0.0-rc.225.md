# 1.0.0-rc.225 重复支付尝试与历史审核发布

同一次收款在刷新页面或换窗口后使用新请求键，过去会建立新的支付ID；渠道动作的幂等保护只覆盖原ID。后端现在按既有锁序检查精确的订单分摊、金额、渠道、方式，复用未释放的线上尝试，且不重复发出payment.initiated事件。不同金额/方式保持独立，明确放开重试仍保留审计；不增加进程全局队列。

历史处置作为正式维护发布的显式计划字段historicalPaymentReview，包含原因、租户/门店/支付ID及资金指纹。控制器先验证原冻结预检身份、已关闭桌次、实际欠款为零、已收款及退款匹配账务流水、没有未消费渠道终态或正在外送的动作，再只追加不可变审计和停止自动查询。原支付、退款、账务流水和原发布基线保持不变。审核收据单独受root:0600及哈希日志保护；后续任何资金/动作变化、未关联新增尝试或终态未入账继续阻断。恢复运行后晚到回调沿原验签与幂等命令入账，多收继续进入财务提示。

财务核对页显示关联订单、当时欠款及正确收款，历史尝试保持待核，不伪造支付失败、平台关单或财务结案。已停用的旧付款不再提供二维码；人工查单结果不能重新启动自动重试。正确收退款不调整，顾客无需重付。

## 验证与范围

受限PostgreSQL专项覆盖单笔/合并12并发新请求键只建一次、不同金额/方式不混用、明确重试释放、欠款/未闭桌/缺账务流水拒绝、审核写入中断后重入和不重复审计、旧码停用、晚到成功及重复回调单次入账/多收提示。发布控制器回归覆盖固定审核证据、变更指纹、其他新尝试、原基线缺失继续拒绝。完整回归、固定SHA CI、原生发布演练与实际部署结果待追加。

本版不新增数据库迁移，仍为schema242。实际逐笔清单、业务金额、凭据和生产查询结果不进入版本库。仅候选测试不代表生产已恢复营业。

2026-09-23 18:37 CST：npm check通过（保留既有lint/打包警告）；完整PG2705通过、1既有跳过，最后增补后支付专项68通过；维护控制器12项通过；财务核对页手机1、厨房3、三屏1通过。新旧厨房视图需分别按开关测试，混用选择器的初次失败已复验。固定版本CI和正式镜像原生演练待执行，尚未部署。


### 2026-09-23 20:20 CST rc.225正式生产恢复结果

SYS-363的代码修复及本次生产部署已完成。PR #283合并3280275e450ffea48dce2eb70b01557ba1f5abbd，正式标签v1.0.0-rc.225，schema242不新增迁移。候选CI35852451744、正式标签CI35854682444、发布35854682439均通过；固定正式镜像向前恢复演练35857122048通过。初次演练仅因合成历史订单没有空闲桌台而失败，修正专用夹具后复验，业务源码及正式镜像未变。

官方deploy-release.sh完成；维护日志completed，公网ready/writeEnabled=true，基本营业及财务/吧台/后厨/取走入口为同一SHA；API与必要后台任务使用受限运行账号，配置开关已核对，原健康巡检与crond恢复。批准历史对象逐笔回读确认关联审计、停止重试、原状态保留、正确收退款及账务指纹不变；未要求顾客重付，未新增这些尝试的资金流水。备份、审核收据、发布与完成证据OSS读回通过。实际业务明细与原始查询保存在受限本地证据目录，未写入版本库。

按用户要求复用有效证据、取消同一SHA冗余主线CI，延期独立首次升级演练、追加全面页面和现场验收；不据此关闭其他商业化遗留项，也不声称完成现场实付或硬件验收。

| 时间 | 变更 | 证据与边界 |
|---|---|---|
| 2026-09-23 20:20 CST | SYS-363已按固定rc.225版本恢复生产营业；历史审核留痕及停止无效重试完成 | 正式SHA/镜像、CI、恢复演练、生产可写/权限/后台/入口及资金指纹均核对；全面现场验收延期 |
