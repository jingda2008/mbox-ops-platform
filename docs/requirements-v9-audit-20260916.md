# v9需求逐条开发审计（2026-09-16）

**当前结论：已形成可运行的本地开发候选，本次已补齐六项功能规则，仍有IP风控事故证据、编号差异与外部验收缺口，不能称为“全部需求完成”。** 已将原文拆成34个审计项；“可配置”要求单列复核，未实现部分不会用相邻功能代替。

- 工作目录：`/Users/jingda/mbox/mbox-requirements-v9-20260916`；分支：`feat/requirements-v9-20260916`。
- 基线：`6545919068ea390a00a7dce14dde2e9ba740bc3a`（获取时的origin/main，rc.204、schema204）。本地新增迁移205–220，运行就绪检查要求schema220。
- 原件：`需求整理-20260916-v9(1).docx`；SHA-256：`c420bc01ad11265142931290bcbd776988abc6dfb772f2443a067ceff55c6d8a`。原件不改写。提取结果：`artifacts/requirements-v9-20260916/source-extract.json`；下表段落号是OOXML原文顺序，不是页码。
- 操作范围：独立工作树、本地PostgreSQL与一次性浏览器测试库。没有提交、推送、合并、部署、小程序上传或真实群发。

## 逐条结果

“本地通过”指相关实现和本地检查；不代表真实微信送达、门店实物核验或生产验收。

| ID | 原文段落 | 要求 | 实现、证据与剩余项 |
|---|---|---|---|
| V9-01 | 14,19 | 从100001发六位会员号 | 本地通过。入会自动分配；数据库行锁串行发号，12路并发与跳过已有号验证；旧入会幂等指纹保留兼容。 |
| V9-02 | 15,20–22,128 | 单/双字母扩容、短号优先、老号不变 | 已实现数字→A10001…Z→双字母；默认999999后切换，双字母从AA1001起。原文AB1001为示例且第六节仍待定字典序；默认值未获业务确认。旧号不重编。 |
| V9-03 | 14,15,22 | 起始、位数、补零、前缀后台配置 | 本地通过。版本冲突、同请求重放、未登录和无权限拒绝已测；短号可生成二维码和查询。 |
| V9-04 | 16,36 | 存酒、订单、提醒、验证码关联会员 | 新存酒强关联会员；提醒/验证码关联同一原单和客户；可选消费订单ID须属于同一客户。旧桌次存酒未自动迁移到新会员寄存体系。 |
| V9-05 | 24,34 | 录入品类、数量、会员 | 本地通过。独立会员存酒表单，不依赖当前桌次；支持原购凭证、位置、备注，扩展字段另见V9-31。 |
| V9-06 | 25,35 | 日期-时间-会员唯一编号、格式可配 | 本地通过唯一性。为防同会员同秒多笔碰撞，候选默认额外加流水尾段，例20260916-193401-100001-1；与原文三段格式有差异，需确认后定版，当前不能删除防重流水占位符。 |
| V9-07 | 37 | 品类默认到期、单笔日期/天数覆盖 | 本地通过。品类新增/编辑/停用/排序；单笔日期优先于天数；后续改期使旧验证码版本失效并取消旧提醒。 |
| V9-08 | 38,124 | 打印、模板配置 | 本地浏览器通过。独立80mm凭证，可配标题、字段、页脚，会员号/编号保留；可浏览器打印或另存PDF。标签机/现有PrintBridge接入与实体出纸尚未验收，既有结账打印样式未改。 |
| V9-09 | 39–42 | 六档逐档提醒、短存期跳过失效档 | 本地通过。30/15/7/3/2/1各自建任务，以原单+到期快照+档位去重；重复调度、改期取消和短存期规则已测。 |
| V9-10 | 43–45,66,131 | 服务号、16–17时可配、文案/开关 | 本地适配与调度已实现，默认16:30，后台限定16:00–17:00（按分钟精度，包含17:00这一分钟）。模板资格、真实模板字段、平台限额、真实送达待外验。accepted仅表示平台受理。 |
| V9-11 | 48–51 | 报会员号查找、向对应会员发码 | 本地通过身份链路。只向经平台核验、UnionID唯一映射的本会员服务号身份发送；先关注后小程序登录可补关联。缺身份/账号/模板时保留失败原因；真实跨应用UnionID能力待外验。 |
| V9-12 | 52–55,129 | 默认4位、有效期、重发、错误限制 | 本地通过。默认5分钟、60秒重发、5次尝试，可配；加密保存，接口不返回明文；错误计数提交而非随异常回滚；未知发送结果不自动重发。 |
| V9-13 | 53,57 | 验证后另行确认已取走、操作人/时间/验证证据 | 本地通过。校验订单版本、送达受理状态、有效期、消费状态；并发取走只成功一次，事件不可改删。状态流转可配的缺口见V9-32。 |
| V9-14 | 56 | 会员/编号/日期/状态查询、筛选配置 | 本地通过。服务端过滤分页、日期按北京时间；可配置品类/状态/日期筛选入口；刷新和失败提示保留。 |
| V9-15 | 58,127 | 再存原单、部分取存与剩余量 | 本地通过。可配部分取存；再存数量不超过该次取走，重复请求不能复制数量。原单模式保留编号，新单模式重新算存期并保留后台来源，见V9-33。 |
| V9-16 | 59,60 | 不再存归档、防纠纷追溯 | 本地通过。归档不增加可售库存；每次确认不再存单独记录，剩余量为零且无待处理取酒后归档。支持自动/人工，见V9-34。 |
| V9-17 | 62 | 全量/品类单量、单笔金额、Excel | 已补齐并通过实际数据库及浏览器验证。范围可选存酒/消费/全部，按会员、品类、日期过滤并导出Excel。存酒登记价值选填，消费订单显示应付金额，两者分别汇总且不当作实收收入；同消费单关联多笔存酒只计一次。品类筛选消费单时取符合条件的整单金额。导出上限一万笔。 |
| V9-18 | 8–10,62 | 报表维度、权限配置、导出审计 | 本地通过。采用已有角色/员工权限配置；管理用bottle.manage.all，导出另需bottle.custody.export；新权限不自动授予真实人员。已补新门店缺失的瓶存权限目录。 |
| V9-19 | 65 | 所有关注者活动群发、内容/时间 | 本地草稿、安排、取消、定时提交及回执链路已实现。独立公众号群发渠道；安排时预览确认全关注者范围，结果不明不自动重发。账号资格、真实配额与送达待外验，未实际发送。 |
| V9-20 | 69 | 活动后台配置、优惠券+小甜点、亮点统计配置 | 已补齐简单组合方案。活动可加一份甜点，优惠券和甜点权益同事务发放，失败一起回滚；每人每活动一套，改变批次不能重复领取；预算计入两者成本。甜点走既有赠品核销。后台可选已发、甜点核销、余量、承诺成本四项统计。未配置或发放真实活动。 |
| V9-21 | 71,125 | 打开弹窗、今日推荐菜、频次内容 | 本地通过。首页/点单/预约/社区/我的五个主入口；默认关闭，可配每天首次/本次启动/每次回前台；最多8款公共推荐商品、顺序、标题与正文可配；异步失败不阻塞点单。微信真机待验。 |
| V9-22 | 73–77,97 | 多歌迷卡，名称/IP/图标/有效期 | 本地实现。沿用不限枚举的项目管理，补艺人/IP、站内图标配置与小程序展示；既有公开条款保留，新建API草稿必须完成双条件配置才能开放。 |
| V9-23 | 80–83,98 | 公共菜单保留、专属单品/套餐、增删改排序上下架 | 本地通过公共菜单与专属单品/套餐并集查询、订单报价；绑定、编辑、排序、上下架与移除接口/UI已实现。删除配置保留审计及历史订单快照。 |
| V9-24 | 83,90 | 不串卡、多卡增量并集 | 本地实际SQL通过：无卡仅公共、有多卡取并集、撤一卡只去对应增量；共享购物车及提交订单再次按客户身份检查，不能仅靠页面隐藏。 |
| V9-25 | 84,99 | 专属项价格、公共菜持卡价 | 已补齐。按卡、按商品配置价格，公共商品与专属项均支持；多卡取最低有效价，标准价更低时取标准价。菜单、购物车、下单一致；其他授权优惠替代本单持卡价，基于标准价计算，不叠加。成交快照不随改价/掉卡变化。真实SQL已验证。 |
| V9-26 | 91–94,100 | 企微+服务号同时满足、按卡绑定 | 本地通过。新API卡草稿开放门槛、申请/审核发卡/恢复均检查双条件；密钥加密，读取不回显；客户不能提交customerId伪造平台身份。历史非此机制项目不擅自改条款。真实配置待外验。 |
| V9-27 | 95,101 | 缺任一条件立即失效、收回菜单价格 | 本地回调、持卡及菜单/价格检查通过。验签后取关/删企微关系失效；旧回调不能覆盖新取关。掉卡后新的报价收回专属价，历史订单不重算。平台漏/迟回调及真实解绑仍待外验。 |
| V9-28 | 101,130 | 自动恢复/重新申请可配，人工撤销不自动恢复 | 本地通过。默认不自动恢复；自动机制冻结与人工冻结/撤销分开；双条件恢复只恢复自动冻结，人工撤销保持撤销。默认策略待业务定版。 |
| V9-29 | 102,103,126 | 境外支付方案、开关与失败文案 | 用户更正为IP风控。代码已传递可信代理链识别的客户IP、记录脱敏网络信息并分类渠道拒绝；本次补IPv4/IPv6校验及明确IP风控错误识别，不把一般异地交易直接断定为IP问题。用户不记得失败订单或时间，历史根因无法确认；不得称已修复，也不能以本地开关解除平台风控。 |
| V9-30 | 8–10,122 | 配置开关、存取/验证/推送/导出完整追溯 | 核心本地实现。命令幂等/权限、加密凭据、不可改存取事件、推送受理/未知分离、导出留痕；未知结果可查但不自动重复发。业务待定与外部验收项仍开放。 |
| V9-31 | 34 | 表单字段可扩展 | 本地通过。最多20个文字/数字/日期字段，可配名称、必填与顺序；服务端拒绝未知/缺失必填字段；单据保存字段定义快照，改配置不改旧单。 |
| V9-32 | 57 | 状态流转可配置 | 按用户要求采用简单配置并已实现：固定在存→验码→确认取走→再存/完结；后台配置部分取存、允许再存、自动/人工归档。验证码和数量检查不可绕过，不开发通用工作流编辑器。 |
| V9-33 | 58 | 是否强制关联原单可配 | 用户明确不强制，已实现。默认允许选择沿用原单或新建再存单，仍可在后台开启强制原单；新单保留不可改来源事件和取走批次关联，不复制数量。并发新建再存只成功一次已测。 |
| V9-34 | 59 | 归档规则可配 | 采用简单归档规则并已实现。默认自动归档，可配置人工归档；两者都要求无剩余量且所有取走批次已处理。保留历史记录，不自动删除、不直接重开旧单；再次寄存可建新单。 |

本次七项补充的完整规则见[补充规则与实现](requirements-v9-decisions-20260916.md)。用户确认项与助手建议项分别注明；历史支付问题继续开放。

## 用户后续新增存酒要求（不改写原文34项）

| ID | 来源 | 要求 | 实现与证据 |
|---|---|---|---|
| V9-35 | 用户后续消息 | 余量可选二分之一、五分之二等 | 瓶单位9个比例选项及手填数量；服务端按六位小数精确比较，拒绝比例与数量不一致。新存及再存入口均已补。 |
| V9-36 | 用户后续消息 | 调用摄像头拍一张照片存入，带时间水印 | 新存/原单再存/新单再存强制照片；前端调用摄像头，服务器生成北京时间、会员号与数量水印JPEG。照片按门店和存酒操作权限读取，禁止公开URL；缺照或非法图片拒绝，存入记录只增不改。 |
| V9-37 | 用户后续消息 | 存酒必留手机号，优先调用注册会员号码 | 从有效会员已验证联系记录获取手机号，加密保存当时快照；无登记则强制补填并标注“员工补录，未验证”。界面只显示掩码；多条有效号码拒绝歧义匹配。 |
| V9-38 | 用户明确回复“继续使用微信服务号验证码” | 取酒必须核验验证码，渠道维持微信服务号 | 服务号发送→验证→确认取走，无法跳过；过期/次数/重复及并发核验保留。手机号不作为短信发码入口，不降低身份要求。 |

补充截图：`artifacts/requirements-v9-20260916/deposit-evidence/index.html`（360px/1280px共6张，模拟摄像头）。水印是服务器收存时间，不是不可伪造的物理拍照时间；手机号手工补录不等于验证号码归属。真实设备及服务号送达尚待现场验收。照片每张最多512KiB，需将存储、备份及保留规则计入上线准备。旧单不补造历史照片，下一次再存执行新要求。

## 本地验证证据

最新schema220：完整数据库2252通过/1项容器恢复演练跳过；存酒专项15通过（与全量重叠）、图片/比例单测3通过；360px/1280px模拟摄像头浏览器2通过；类型、构建、架构新代码与diff检查通过。lint通过并保留警告，含相机动态stream ref清理提示和Fast Refresh导出提示。详见`deposit-*.log`及最新`verification-summary.json`。以下schema219记录作为历史基线保留，不能替代真机与上线验收。

详细日志保存在`artifacts/requirements-v9-20260916/`；验证汇总见同目录`verification-summary.json`；全部130个非空段落（含重复速览、标题和流程连线）均在`paragraph-coverage.json`登记。前轮schema216数据库全回归2240通过/1跳过。本次schema219专项73通过，单元模式1849通过、小程序286通过、浏览器2通过；最终数据库全回归2247通过/1项容器恢复演练跳过；售后与菜单专项209通过。类型检查、浏览器构建、静态及架构检查通过，lint保留既有警告。不同测试集存在重叠，不合计为独立验收数量。以下证据类型不可互换：

- PostgreSQL：真实迁移、事务、权限、幂等、并发取走、字段快照、提醒去重、卡资格和多卡菜单查询。
- 单元/契约：号段边界、验证码与消息结果分类、精确保存大整数微信回执ID、XLSX实读、打印文本转义、弹窗频次及既有回归。
- 浏览器：一次性数据库，360px/1280px录入→查单→Excel→打印预览→弹窗配置。截图及XLSX在`artifacts/normalized-browser/results/`。
- 尚无：真实服务号/企微资格与回调、真实模板与群发收据、小程序真机、物理打印、真实取酒操作、正式部署证据。

## 默认值与外部验收

默认候选为：100001起号、6位、补零、A–Z、最长双字母；4位验证码/5分钟/60秒重发/5次尝试；提醒16:30；整笔取存；条件恢复重新申请；弹窗默认关闭、启用后每天首次；存酒新建与提醒默认关闭。配置保存与正式启用必须按门店实际规则办理。

服务号/企微账户管理使用`/api/social-accounts/{id}/callback`。须由账号管理员配置正式域名、主体AppID/CorpID、Secret、回调Token/AESKey、模板及字段。两个账号与小程序需具备可用且一致的可信身份映射；无UnionID时不会根据手机号、会员号或员工口述强行拼接身份。当前旧的通用回调路径不等同于此新链路已接入。

门店应分别验收：操作与导出权限；老会员不换号；按真实会员发码且其他人不能取；部分取/再存实物数量；改期提醒；服务号取关/企微解除/恢复；不同卡菜单与套餐；打印设备；群发平台受理及最终回执。未知推送结果须核对平台，不能一键自动重发造成重复触达。

最终复核说明：首轮全回归中一项既有售后测试在刚授予权限后立即读取时遇到微秒/毫秒时间边界；将测试夹具设为已生效权限后，相关209项及最终2247项全回归通过。正式权限规则未改。菜单按会员资格计价，响应已设private/no-store，避免共享缓存串用。


## 详情与推荐弹窗后续修正

- V9-39（用户反馈）：华为X5详情关闭按钮不可见、套餐图被压缩。已改独立顶栏关闭、稳定图片高度、内部长内容滚动、组成名称换行；单品图片aspectFit及全屏预览调用保留。证据见SYS-234，不能把模拟器尺寸当成华为真机证据。
- V9-40（用户反馈）：推荐弹窗和按钮优化。深绿标题栏、暖白内容、编号推荐清单、固定“稍后再看/查看菜单”双按钮；标题、正文和最多8项产品在中间scroll-view滚动，关闭入口始终在顶栏。
- 本轮仅修改小程序界面及测试，不改存酒、优惠或价格规则。355项小程序回归、156文件静态检查、2项布局测试覆盖5种尺寸通过；原生隔离截图和限制见`artifacts/requirements-v9-20260916/detail-popup/index.html`。未上传发布；真实X5/iPhone、正式HTTPS图片全屏预览待验收。


## 推荐广告商品图片与价格

V9-41（用户后续明确）：推荐弹窗必须显示具体商品图片与价格。已补图片、名称、价格卡片，读取当前顾客菜单的有效价格（含可用会员专属价），不重复实现计价；仅查询配置的最多8个商品，保持后台推荐顺序，过滤已隐藏/无有效价格商品。图片沿用菜单安全地址和缩略图，失败重试原图后显示占位；不会用其他商品图替代。后台价格修改后，下次读取弹窗反映新价。图片/价格不代表实时库存承诺，暂不可点商品显示状态，最终以下单校验为准。

验证：11项会员/菜单数据库专项（含会员250分/普通500分与图片字段核对）、358项小程序回归、5种尺寸布局验证通过。最新截图见`artifacts/requirements-v9-20260916/detail-popup/native-product-images-prices.jpg`，其中商品素材是真实本地文件，价格为隔离测试数据，不代表正式售价。生产API和小程序尚未部署/上传，真实图片域名及真机仍待验收。


## 参考图后的简洁海报弹窗

V9-42（用户提供参考图）：替代V9-40/41的视觉排版，保留其商品与计价来源。去除品牌顶栏和底部双按钮；采用居中大图、简洁菜名与价格、底部独立圆形关闭入口。多道推荐使用手动左右滑动和分页点，不自动切换；点击图片或名称进入菜单。图片使用安全地址的原图并保持aspectFit，失败显示占位，不以其他商品图片替代。文案区独立滚动，长文案不压缩图片。

验证：358项小程序测试、156文件静态检查、git diff --check通过；布局覆盖320×568、360×640、390×844、540×720、720×540，检查图片固定高度、文案滚动、44px关闭触摸区与点击层级。微信开发者工具隔离Nexus5尺寸中已目视检查，手动滑至第二商品且分页变化，点击关闭后弹窗消失。截图`detail-popup/native-clean-poster.jpg`及`native-clean-poster-second.jpg`使用本地商品素材和测试售价。未上传、未发布；真实X5/iPhone及线上素材仍待验收。
