# 1.0.0-rc.222 财务、吧台、三屏完整交付

本候选恢复rc.220中未上线的财务及三屏完整实现、225–242原字节迁移，同时保留rc.221已上线修复。财务按实际退货商品冲回奖励；普通重收只收真实欠款，撤销补偿独立登记。吧台/后厨放好后进入共用取餐屏，取走视为送达，手机不二次确认，误触按原领取集合撤回。

生产PostgreSQL工具在容器中运行，主机/tmp不可见。恢复工具在受保护共享目录生成精确临时密码匹配和恢复清单；原凭据不修改。属主探针在隔离管理员库运行，verify成功/失败只删除本次创建的隔离库，不修改营业库。保留源属主、扩展、ACL、迁移及数据对比门槛。

旧维护只能通过新计划明确绑定的未迁移撤回证据接续；哈希链、原版本、schema、停止的守护服务和入口均须核实。原journal与epoch保留，不伪造completed。配置文件按签名校验后供node组只读。

当前已提交草稿PR279，尚未合并部署。真实RDS对9月21日冻结schema224备份的隔离恢复已通过，临时库已删除；这不替代正式切换时的新备份和OSS读回。最终同SHA CI、原生维护演练及生产结果完成后追加。小程序源码同步不等于平台上传；真实取餐屏、资金和打印验收另记。

120人首轮再次复现厨房/吧台看板超时。实际计划确认120单触发任务扫描864000次；来源查询改为受门店scope约束的逐层主键关联，避免低估后乘法扫描。新增真实PG回归在旧查询失败、修复后通过；完整并发验收结果另记。
