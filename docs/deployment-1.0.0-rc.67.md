# M-BOX 1.0.0-rc.67 阿里云验证环境部署记录

## 发布身份

- 版本：`1.0.0-rc.67`
- Git标签：`v1.0.0-rc.67`
- 主分支提交：`5d45211a547613c213ab61a848643306626dd607`
- 镜像：`mbox-ops:1.0.0-rc.67-5d45211`
- 发布镜像摘要：`sha256:aca764a723ec269b5cd11306d5d617c8f0bbbbeec51d6be68c1676e14b55446d`
- 迁移摘要：`sha256:61aa569cc09bc935ac3a0642d03e6ada71ff9025f0e774d210b23f6a3445ff55`
- 部署时间：`2026-08-09 02:47:37 CST`
- 环境：阿里云验证环境
- 地址：`https://139.224.254.60`

## 发布证据

- 发布流水线修复PR：`#60`，合并提交为`5d45211a547613c213ab61a848643306626dd607`。
- PR CI运行`31271975951`通过，覆盖代码质量、1103项测试、数据库和浏览器商业流程。
- 标签CI运行`31272234776`通过，镜像、质量、数据库、浏览器及总门禁全部成功。
- 发布运行`31272234778`通过，草稿预发布中的同一镜像包经提交SHA、镜像摘要和归档校验后公开。
- 发布资产包括112,512,165字节的不可变镜像归档、归档校验和、迁移清单和发布清单。
- 候选容器通过健康检查、PostgreSQL规范化读取路径和投影一致性检查后才切流。
- 部署后公网冒烟验证通过，线上修订为`9076`，提交SHA和镜像摘要与发布清单一致。
- 部署前数据库备份：`/opt/mbox/backups/mbox-20260809-024104.dump`。
- 回滚容器：`mbox-app-rollback-5d45211-20260809-024725`。
- 61张静态桌码均通过线上凭证交换：59桌返回可用会话，`S02`和`S05`因未开台返回预期的`TABLE_SESSION_NOT_OPEN`，无无效签名或错桌。

## 运行观察

- 部署加载双容器和执行迁移期间，小规格服务器出现约6分钟资源饱和，新SSH握手和公网请求短时超时；原子部署会话未中断，候选验证后正常切流。
- 新容器启动时出现一次阿里云RDS域名解析`EAI_AGAIN`，随后连接恢复；上线后未发现5xx、崩溃、未处理异常或数据库投影不一致。
- 61桌码并发分批验证时，冷启动请求约0.8至1.3秒，后续单请求约0.2秒。该结果说明当前规格可用于验证，但部署并发和高峰容量仍需持续观察，不能据此批准正式生产容量。

## 本次能力

- 完整部署`rc.66`已独立复审通过的现场履约、角色队列、库存权限和预约分流能力。
- 标签发布不再依赖已耗尽的GitHub Actions临时制品额度；镜像先进入草稿预发布，完整CI成功后才公开。
- 制作岗位一次点击完成，配送岗位一次点击送达，审计链仍保留自动接单、推定取货、送达和责任人证据。
- 调酒师、厨房、服务员按有效岗位、工位和责任范围隔离，经理保留监督视图。

## 保留边界

- 当前支付仍为模拟模式，真实微信、星驿支付、退款、物理POS和日结尚未完成生产联调。
- 当前硬件模式为未配置，真实打印机、耳机、平板弱网和连续高峰仍需门店验收。
- 213条经营TC为147条通过、50条未执行、16条阻塞，共66条发布阻塞项。
- 本版本只批准阿里云验证环境和员工演练，不能据此批准正式商业生产发布。
