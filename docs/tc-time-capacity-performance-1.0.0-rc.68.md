# M-BOX 时间、容量与响应性能专项TC 1.0.0-rc.68

本专项是历史213条经营TC之外的增量门禁，不改变原编号和既有状态。自动化只证明候选代码与隔离环境；阿里云资源、时间同步、部署后延迟必须以线上只读证据重新验收。受版本控制的状态以最新不可变提交证据为准；本地脏工作区或未提交复测只能记为诊断，不能把失败改成通过。

## 门店时钟与营业日

| TC | 级别 | 场景与步骤 | 预期结果 | 当前状态 | 证据 |
|---|---|---|---|---|---|
| TME-001 | P0 | 服务器、容器和测试进程分别使用UTC、Asia/Shanghai及其他主机时区 | 同一绝对时间得到相同M-BOX营业日，不读取主机默认时区 | 通过 | `venue-time.test.ts` |
| TME-002 | P0 | 北京时间05:59:59.999与06:00:00.000计算营业日 | 前者属于上一营业日，后者属于新营业日，边界无一秒重叠 | 通过 | `venue-time.test.ts`、`business-day-rollover.test.ts` |
| TME-003 | P1 | 跨UTC自然日、跨北京时间自然日和跨月 | `timestamptz`保存绝对时间，`business_date`按门店时区与06:00规则生成 | 通过 | `venue-time.test.ts`、数据库迁移结构 |
| TME-004 | P1 | 门店时区使用IANA规则并模拟冬夏偏移变化 | 不使用固定偏移推算；IANA规则可正确转换，非法/不存在时间被拒绝 | 通过 | `venue-time.test.ts` |
| TME-005 | P0 | 应用停机跨过一个或多个营业日后启动 | 逐日补切且幂等；历史资金、库存和审计仍归原营业日 | 通过 | `business-day-rollover.test.ts` |
| TME-006 | P0 | 浏览器时间快慢10分钟或手工修改日期 | 开台、支付、任务、SLA和营业日使用服务端时间；客户端时间只用于显示 | 通过 | 服务端权威时间测试及`live-time.test.ts`正负12小时设备偏移矩阵 |
| TME-007 | P1 | 阿里云ECS检查chrony/NTP、时区、系统时钟漂移和容器UTC | NTP同步正常，漂移小于1秒；容器可使用UTC且业务结果仍是门店时区 | 阻塞 | SSH已建立TCP但在服务端banner阶段超时，尚无线上时钟证据 |
| TME-008 | P0 | PostgreSQL会话时区为UTC与Asia/Shanghai分别执行时间矩阵 | `timestamptz`绝对时间和06:00营业日边界不受数据库会话时区影响 | 通过 | `npm run db:verify:time`、CI数据库作业 |
| TME-009 | P0 | 北京时间00:00至05:59发生收款、退款和交班复核 | 统一归入上一营业日；06:00起归入新营业日 | 通过 | `payment-domain.test.ts`、`business-day-rollover.test.ts` |
| TME-010 | P0 | 桌次、赠送额度、库存和经营分析跨06:00边界 | 全部读取同一门店时区和切日配置，不存在硬编码自然日 | 通过 | `gift-policy.test.ts`、`table-sessions.ts`、`profit-center.ts` |
| TME-011 | P1 | 把员工或客户设备时间调快/调慢12小时后浏览菜单、任务和出品 | 商品可售、SLA风险及倒计时使用服务端偏移；防重复与触感可使用本地单调时间 | 通过 | `live-time.test.ts`验证正负12小时仍恢复服务端时间；菜单、任务、出品统一使用`serverNow` |
| TME-012 | P0 | 应用服务器与PostgreSQL时钟偏差超过5秒 | 就绪探针失败并输出`mbox_database_clock_skew_ms`，禁止切流 | 通过 | `postgres-repository.test.ts`、`observability.test.ts` |

## 容量与扩容判定

| TC | 级别 | 场景与步骤 | 预期结果 | 当前状态 | 证据 |
|---|---|---|---|---|---|
| CAP-001 | P0 | 两个API实例、60读取并发、每条关键路由至少300样本 | 0个HTTP/流程失败；关键路由P95/P99全部达标 | 失败 | PR #62 CI `31306680108`：9条受测路由均有P95或P99超限；后续本地修改尚未形成不可变证据 |
| CAP-002 | P1 | 压测期间检查事件循环 | P95理想不超过50ms、硬上限75ms，P99不超过100ms | 失败 | PR #62 CI `31306680108`：两个实例P95约165.0/158.7ms，超过75ms硬上限 |
| CAP-003 | P1 | 压测期间检查数据库连接获取 | P95不超过50ms，失败/超时为0 | 通过 | PR #62 CI `31306680108`：连接池获取P95低于0.5ms，无等待、失败或超时；本项通过不抵消路由失败 |
| CAP-004 | P0 | 压测期间检查写队列与投影 | 队列高水位低于100，无拒绝/超时；规范化投影一致 | 通过 | PR #62 CI `31306680108`：队列高水位6，无拒绝/超时，投影ready；仍需结合单次写服务时间判断聚合写成本 |
| CAP-005 | P1 | 阿里云线上读取CPU、内存、磁盘、连接数、重启/OOM与请求P95 | 连续峰值证据达到阈值后才决定纵向或横向扩容 | 阻塞 | SSH已建立TCP但在服务端banner阶段超时，尚不能取得实时资源证据 |
| CAP-006 | P1 | 部署时双容器并存和迁移 | 不使线上CPU/内存持续饱和，不造成新SSH和客户请求超时 | 未通过 | rc.67部署记录曾出现约6分钟资源饱和 |
| CAP-007 | P2 | 20个聚合写请求并发冲击（高于12名员工的预期同时操作） | 记录退化点但不替代正常负载门禁；不得出现错单、丢单或5xx | 未执行 | 旧20并发观察只保留为历史容量信号，未在当前提交和新负载模型重跑 |
| CAP-008 | P0 | 提交级单路由回归，每条关键路由至少300个成功样本 | 每条路由独立满足SLO，权限拒绝和脚本错误不得混入成功性能样本 | 未执行 | 本地源码正在复测；必须由不可变提交CI重新生成证据 |
| CAP-009 | P0 | 固定规格候选环境运行30分钟300人代表性混合经营峰值 | 动作比例、到达率和角色符合模型；错误、重复、错状态为0，资源与SLO达标 | 未执行 | 负载定义见`performance-test-profiles-v1.0.md`，尚未在固定候选环境执行 |
| CAP-010 | P1 | 在隔离环境执行1/2/4/6/8次业务写每秒容量阶梯 | 每档连续3次，找到排队拐点、安全容量和恢复时间，不用极限结果冒充经营峰值 | 未执行 | 尚未执行 |
| CAP-011 | P1 | 校准压测机实际发起率和调度延迟 | 实际发起率不低于目标98%，漏发为0，调度P95/P99在门限内 | 未执行 | 新脚本已记录指标，待不可变CI验证 |
| CAP-012 | P1 | 同一提交、环境、数据和负载连续运行3次 | 三轮均达到门禁，禁止只挑最好结果 | 未执行 | 尚未执行 |
| CAP-013 | P0 | 性能失败或测试脚本失败后封存原始结果 | 报告、日志、指标、缺陷分类与SHA256清单均保留，修复后另起一轮 | 失败 | CI `31306680108`可追溯，但GitHub Artifact配额已满，原始制品尚未完成远端封存 |
| CAP-014 | P1 | 用同一请求ID对齐客户端、服务端、连接池、队列和数据库耗时 | 端到端差值可解释，不能仅凭总耗时推断需要扩容 | 未执行 | 已增加队列等待、写服务时间和状态体积指标，待完整请求链路复测 |
| CAP-015 | P0 | 合并七个独立路由阶段前核对环境指纹 | 提交SHA、脏工作区、依赖锁、迁移集、种子数据、运行时、实例数和连接池参数一致；任一不一致即拒绝 | 未执行 | 合并器及破坏性单测已实现，待不可变CI生成七阶段证据 |
| CAP-016 | P1 | KDS和任务写路由先准备300条数据再测量300次动作 | 日志按`setup/measured`分窗，动作SLO只统计测量样本；准备阶段5xx和连接故障仍阻断 | 未执行 | 本地KDS诊断300/300、P95 153.7ms，准备阶段与测量阶段均无5xx/连接故障；待不可变CI复测后才能改为通过 |
| CAP-017 | P1 | 连续3个营业日分别生成300单并完成KDS、库存、任务和06:00切日 | 活跃聚合状态不超过10MB；超过2MB后每次成功写入净增不高于5KB；历史数据归档可查且热路由P95/P99不逐日退化 | 未执行 | 单营业日本地诊断约916次写入净增约3.21MB、约3.5KB/写；尚未执行多营业日稳定性和归档有界性验证 |
| CAP-018 | P1 | 两实例、全新数据库下持续执行KDS动作5次/秒，共300次 | 记录容量档位、排队拐点和恢复时间；P95不超过800ms、写队列等待P95不超过100ms，状态和库存不丢不重 | 失败 | PR #62 CI `31316732711`：KDS开始P95 3469.7ms，聚合写队列等待P95约2599.7/2601.4ms，单次写服务P95约466.6/471.5ms；连接池获取P95约0.32/0.31ms，瓶颈是聚合写串行队列而非连接池。后续脏工作区本地5次/秒诊断P95 96.5ms、最大并发1，不是同一不可变构建，不能覆盖CI失败。5次/秒约为显式估算夜间均值的27.5倍，作为容量缺陷保留，待新提交CI裁决 |

## 响应与异步体验

| TC | 级别 | 场景与步骤 | 预期结果 | 当前状态 | 证据 |
|---|---|---|---|---|---|
| RPF-001 | P1 | 同一员工会话60个并发受保护请求 | 同一实例只执行一个在途租约校验，完成后不缓存授权结果 | 通过 | `keyed-single-flight.test.ts`及双实例混合负载 |
| RPF-002 | P0 | 员工退出与并发普通请求 | 退出请求不复用在途校验；数据库撤销后新请求拒绝 | 通过 | `verify-presence-leases.mjs`：跨实例立即撤销、拒绝复活并在签名会话到期后清理墓碑 |
| RPF-003 | P1 | 300位不同匿名客人首次扫码建会话 | 行为事件与访问次数不丢不重，P95不超过500ms | 失败 | PR #62 CI `31306680108`：P95 2611.9ms，超过500ms |
| RPF-004 | P1 | 12名员工同时冷启动bootstrap | P95不超过500ms、P99不超过800ms，响应权限隔离正确 | 失败 | PR #62 CI `31306680108`：P95 1012.2ms，超过500ms |
| RPF-005 | P1 | 已取得ETag的员工轮询 | 数据未变化返回304，不重复传输完整工作台 | 失败 | PR #62 CI `31306680108`：P95 284.4ms，超过150ms；304正确性与延迟必须分别判定 |
| RPF-006 | P0 | 前端乐观更新后服务端拒绝、超时或并发冲突 | 立即反馈，失败恢复原状态并说明原因；不得形成假完成 | 通过 | `optimistic-action.test.ts`、`optimistic-projections.test.ts`及客户情绪、预约失败恢复浏览器流程；任务/KDS共用同一事务协调器 |
| RPF-007 | P1 | 已签名员工执行下单、任务和库存等高频请求 | 人员目录与租约完成身份校验，业务接口仍按权威状态校验权限；不重复克隆整店状态 | 未执行 | 身份与权限代码测试已覆盖，但“高频路径不重复克隆整店状态”尚缺提交级性能证据 |
| RPF-008 | P0 | 客户端伪造采购、费用、券核销或打印回执时间 | 服务端忽略客户端操作时间并写入权威接收时间；业务归属日期使用独立字段 | 通过 | `commercial-ops-api.test.ts` |
| RPF-009 | P1 | 分别测完整Bootstrap 200和稳定ETag Bootstrap 304 | 两条路径独立计数；304不传完整工作台且各自达到SLO | 未执行 | 新负载脚本已分组，待不可变CI验证 |
| RPF-010 | P1 | 在空预约、正常预约和高容量预约数据集读取预约 | 数据量和响应体积有记录，三种规模均达到正确性与延迟门禁 | 未执行 | 尚未执行 |
| RPF-011 | P1 | 同一客户重复扫码及并发建立桌次会话 | 客户ID和访问记录幂等，不重复建会话、不丢行为事件 | 未执行 | 尚未执行 |
| RPF-012 | P1 | 12名员工持续心跳并跨两个实例读取 | 在线状态、离线时限和权限一致；脚本给所有模拟在线员工续租 | 未执行 | 已修正负载脚本续租范围，待不可变CI验证 |
| RPF-013 | P0 | 任务/KDS响应超时后客户端重试 | 不重复完成、重复制作、重复扣库存或状态回退 | 未执行 | 尚未执行故障注入 |
| RPF-014 | P0 | 实例A写入后立即从实例B读取任务、KDS、预约和桌次 | 跨实例读取与数据库权威状态一致，不返回旧状态或假成功 | 未执行 | 尚未执行 |
| RPF-015 | P1 | 使用生产构建和独立浏览器上下文，分别测员工进入岗位首页与固定桌码进入可浏览菜单 | 从导航开始到首屏可见且完成两帧绘制P95不超过500ms；API错误为0；不得用API或测试工具轮询耗时代替页面内绘制时间 | 失败 | PR #62 CI `31316732711`：员工页面内双帧P95 701.4ms，API旅程P95 101.6ms。现有入口已经预取员工工作区，问题仍需按API、资源、解析和绘制继续拆解；客户阶段通过不抵消员工失败 |
| RPF-016 | P1 | 将员工登录从整店聚合写迁移到规范化事务，并注入停用、切营业日和写入结果未知故障 | 租约、登录审计和未归属任务重分派原子一致；失败不签发令牌且不遗留幽灵租约，登录期间状态变化必须拒绝或重试 | 未执行 | 本轮曾验证只写规范化租约可降低队列，但独立审查发现会丢失聚合审计和任务重分派且补偿不足，相关实现已撤回；需设计事务性审计/outbox后再实施 |
| RPF-017 | P1 | 分别执行员工App/浏览器进程冷启动、新浏览器上下文首开和已有会话页面启动 | 三类指标独立命名并各自满足阈值；不得用预登录的新上下文首开替代完整冷启动 | 未执行 | 当前自动化只证明`fresh_browser_context_page_readiness`，浏览器进程启动与员工登录准备均在计时窗外；需补真实平板和手机冷启动证据 |

## 扩容门槛

当前只有`CAP-003/CAP-004`在最新不可变CI中通过，`CAP-001/CAP-002/RPF-003/RPF-004/RPF-005`失败。只有在同一不可变镜像部署后，阿里云峰值连续15分钟出现以下任一情况，才批准运行期扩容：

- ECS CPU持续超过70%，事件循环P95超过75ms或P99超过100ms；P95超过50ms先告警并分析阻塞来源。
- 可用内存低于20%，发生OOM或容器重启。
- 数据库连接获取P95超过50ms、等待队列持续大于0，或连接使用率超过70%。
- 核心读取P95超过500ms、核心写入P95超过800ms，且错误率超过1%。
- 部署双容器并存时资源不足，影响现网请求。

扩容不能替代慢SQL、过大bootstrap、同步分析写入、连接池往返或聚合JSON写入修复。

部署容量与营业容量必须分开判断：`CAP-006`已证明双容器部署有资源压力，即使日常请求尚未证明需要扩容，也应采用单候选容器、远程镜像分层缓存或临时升配完成部署。若当前ECS低于2核4GB，验证环境不应继续降低；商业上线建议4核8GB作为蓝绿切流和300人峰值的起始容量，最终仍以`CAP-005`线上15分钟窗口决定。
