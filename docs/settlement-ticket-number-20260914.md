# 整桌结账单商场识别号

2026-09-14 21:49 CST。用户最终范围为仅整桌结账单，做好后提交合并，不部署。当前开发基线为远端主线bc662a6（包含rc197交付），工作目录为 `/Users/jingda/mbox/mbox-release-consolidation-20260914`。

## 行为

仅 `table_settlement` 在首次形成打印快照时附加可选 `displayNumber`，例如 `20260914-213508-004271`。日期和时间来自桌次关桌时间，按北京时间转换；营业日独立保留。后六位从租户、门店、桌次的固定标识计算，保留前导零。同一票据的分页、多份副本和重试固定同号，已有快照及旧票重放沿用原号码。

此号码定位为OCR展示号，不保证跨票全局唯一，也不替代订单、支付、退款或桌次主键。如果商场以号码作为严格去重/核销依据，需要另行增加可靠号码分配和数据库唯一约束。

`ticketReference` 不修改。新版HTML和Windows打印桥仅对带展示号的整桌结账单在票头显示“单号”，并在票尾完整打印“原始追溯码”。其余九类票及旧结账票保留原输出。补打沿用既有快照复制和审计机制，时间、内容、展示号和追溯码不重新生成。

无数据库迁移（schema202）、业务订单号变更、收退款动作、权限变更或小程序改动。旧打印桥会忽略新字段，继续打印原号；后端与打印桥都升级后才显示新格式。

## 验证

- 模板、来源和打印桥19项通过；补充日期跨日、等价时区、前导零、非法时间和号码、序列化、分页、旧票兼容及九类其他票种断言。
- 34项隔离数据库测试通过，含真实快照保存、源事件重放返回原任务、连续两代补打原号码/时间/内容不变、旧快照不被补号。使用自动创建并清理的本地临时数据库。
- 完整 `npm run check` 通过：1791项通过，706项环境条件跳过；类型、lint、架构、元数据、两端小程序静态与构建检查通过。最终远端CI另记。
- Chromium检查58mm/80mm模板单号均保持一行，预览保存于 `artifacts/settlement-ticket-number-20260914/`。不代表商场AI真实识别通过。
- PowerShell 7.4隔离Linux检查升级基线、未知适配拒绝、停止期间变更拒绝、重复安装；原15项事务故障模拟和语法检查通过。新增58/80mm真实ESC/POS字节检查，单号不换行、追溯码不截短、仅末尾切纸通过。
- 原 `raw-ticket.tests.ps1` 的前三组GBK/宽度/控制字符检查通过，末段原始门店WinSpool比对因 `artifacts/venue-original-20260913/print-ticket.ps1` 缺失而失败；未补造历史文件或修改原测试。新增包独立核对PS打印及枚举文件与标准1.0.4包规范化字节哈希完全相同。

## 打印桥候选包

构建：`python3 scripts/build-print-bridge-105-upgrade.py`。

产物：`artifacts/print-bridge-one-click-1.0.5-r1/MBOX-PrintBridge-1.0.5-OneClick-r1.zip`，SHA256 `5445d150f8766e2abe84c33472bcb05231fb17ca289d0e55cd82c1a106d7c2a1`。包内7个程序/说明文件和manifest校验通过；固定归档时间便于重复构建核对。

升级仅接受标准1.0.4基线或完全相同的1.0.5文件；标准1.0.4基线由bc662a6下对应源码按原1.0.4构建规则（PS1 BOM/CRLF、JS UTF-8）计算。保留既有备份、忙队列退出、停服后复核及失败恢复流程，不改配对、路由、打印机配置和防重任务记录。

用户将来在收银电脑运行升级包；当前仅保存，不安装。Windows 5.1/UAC/实际服务恢复和实体打印尚未运行。SYS-224保持待验收。

## 提交与发布状态

本轮未部署、未创建发布标签、未上传小程序、未安装门店打印桥。线上只读核验仍为rc197 / `97e0cb89a8b3cbc2498773d91b15128630399def` / schema202。提交合并结果待实际完成后填写。
