# 2026-09-21 审计修复候选交付

基线为 `77744bc4e9ed308546b630590bdfa5dfe5ce5b7d`，工作分支 `fix/audit-remediation-20260921`。本候选处理 SYS-310—313、316—320；SYS-314/315 和三屏正式流程由独立任务处理，合并前按 [并行分工](parallel-development-20260921.md) 集成。本文不是生产发布或商业验收完成记录。

## 候选行为

| 编号 | 变更 | 仍需保留的边界 |
|---|---|---|
| SYS-310 | API、worker、ready及发布检查真实数据库LOGIN，运行与维护/备份凭据分离；回滚探针同时检查旧容器实际身份和身份契约；标准PG测试创建真实低权登录并清理 | 旧rc.216未取得首次切换的恢复资格，实际环境身份核对受控保存；必须建立可用的受限回滚基线和正式过渡流程，不能仅更换URL |
| SYS-311—313 | 奖账按订单关联不同付款的退款；区分实际多收退回与销售退款；扣除未付停止金额；补发使用原结清来源，并在订单锁下复核退款待核记录 | 混合计分/不计分商品退款跨过超收部分的政策仍待用户决定；待核记录没有完整关闭流程，尚未全闭环 |
| SYS-316 | 报损独立永久回执，回填过期但仍保留的旧回执；迁移锁及触发器覆盖旧应用滚动切换写入；恢复重新鉴权，不重复扣库存、审核或发事件 | 已物理删除原缓存的历史操作无法证明原请求哈希，返回明确409要求核对原记录，不自动重做 |
| SYS-317 | 权限发布携带稳定操作标识、永久回执及修订号，丢回复后恢复原结果；旧页面不能覆盖之后保存 | 最终三屏与权限集成、真实岗位验收另行执行 |
| SYS-318/319 | 两端服务状态补充已接单；顾客确认状态从服务端读回 | 只调整普通呼叫服务，不重定义取走流程 |
| SYS-320 | 原结账请求遇付款已成功、部分退款、全退、失败或关闭时读回原身份及最新状态；先恢复再做新付款预检；两端/Web保留原单恢复，不再次拉起支付 | 证明范围为原结账回执仍存在；恢复失败不会被当作付款成功，实际通道/真机仍待验 |

## 验证快照

证据保存在工作区父目录 `outputs/audit-remediation-20260921/`，测试集有交叉，不将各组数字相加：

- `standard-postgres-final-v2.log`：完整标准实库回归 **2398通过、1条件跳过**，退出0。包含真实受限LOGIN身份/API/worker用例。跳过项为要求独占Docker测试容器的数据库维护恢复演练；现已在另一独占容器补验1/1通过，资源已清理。因此2399项在两套隔离环境均已执行通过，不称一次运行2399。
- `check-frozen-v2.log`：`npm run check`退出0；包含两端小程序318通过、普通单元2010通过/799环境跳过、运行器9通过及正式构建。保留既有lint、500kB分包和动态导入警告，不宣称零警告。
- `financial/financial-tests-final.log`：88通过；补发审批与worker真实并发由第三连接持订单锁，确认两个竞争者均等待后释放，只记一次奖账/冲回。`payment-cashier-migration-isolation.log`：48通过，历史197迁移测试在同一事务内断言后回滚，不污染当前schema。
- `security/`：身份集成5通过；业务195通过，其中184为真实低权事务、11为原有单元；临时3库3登录已清理。空队列worker运行不能代替外部通道或峰值验收。
- `frontend/`：权限与普通服务62通过；运行器9通过覆盖信号取消、资源创建途中取消、子进程启动失败和拒绝退出的清理。使用替身SQL与独占进程，不影响其他任务。
- `root-inventory-final.log`：22通过，包含226真实迁移旧回执回填、旧应用完成捕获、缓存过期/删除后的并发重放。

此前全量失败已定位并修正：227增加只读元数据权限后的旧断言；197/154历史迁移测试持久化改写测试库版本。未修改已部署001—224迁移以掩盖失败。

恢复演练证据为 `frontend/isolated-restore-drill.log` 与 `isolated-restore-drill-result.json`，覆盖095备份恢复、保留096及改名/最终证据故障回退；专用容器和volume已精确清理，未触碰共享数据库。受限回滚候选的评估记录位于 `security/rollback-candidate/plan.md`：旧rc216加身份补丁虽82项单元及构建通过，但金融语义不兼容，明确不合格，未构建镜像或操作生产。

## 迁移依赖快照v1

225业务政策尚未最终冻结。三屏任务仅可将下列真实SQL用作本地未发布依赖；正式集成须核对最终哈希并从新库重跑全部迁移。

| 迁移 | SHA-256 |
|---|---|
| 225 | `141af8b1b846fdae8407f8ebd05d95e5363567ac9055d5d1d5a085373948a8ca` |
| 226 | `54e3a0cb71cb733f15241296722254ea6a3b98518551d8b4183619c344ab89c9` |
| 227 | `abc91642ab97bf341d6341df5799ab5367ead0053e11b30675a5558ddc39225d` |

## 合并和部署条件

1. 用户确定混合退款积分政策，补齐确定规则后的执行、审批关闭及历史事实边界，重新验证225和相关业务。
2. 三屏任务交付正式实现与228，合入同一候选，验证停用员工接班、多代重做、取走视为送达与撤回、年卡投影、库存、付款退款。
3. 建立受限回滚候选及首次身份转换流程，核验完整源码SHA/镜像、低权API/worker、schema与新金融事实兼容、实际故障回滚。旧版本可读DDL或ready并不表示业务可回滚。
4. 最终合并SHA通过完整CI和相应浏览器/实库验证，再从唯一正式入口发布，读取线上身份与版本、备份及完成证据。小程序上传、真机、打印与实物验收分别记录。

候选可以提交评审；以上条件未满足前保持草稿，不独立合并或部署，不把安全待核或代码通过标记为生产问题关闭。
