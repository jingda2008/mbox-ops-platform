# 微信会员二维码放大与关闭

基线：origin/main `62baa3e7af1c1aa84d242510cc3b4b81c76512b7`。分支：`fix/member-qr-preview-20260919`。

用户明确要求支付宝不改；支付宝试改已完全撤回。本轮仅修改微信会员中心三份页面文件，以及本记录和待处理清单。双端布局差异是本次明确范围，不通过改动支付宝或放宽一致性检查来掩盖。

- 点击会员二维码打开居中大图，显示会员号和出示说明。
- 可点击关闭按钮或遮罩关闭；点内容区域保持展示。
- 离页、重新加载收起；加载中、错误状态或缺少二维码时不打开。
- 直接展示当前会员二维码，不生成新会员身份、不调用新增业务接口。

验证：微信静态检查通过；现有顾客流程 31 项通过；页面逻辑实际加载验证打开/保持/关闭/离页/刷新/缺码保护及模板事件绑定通过。源模板/CSS 浏览器模拟 320/360/375/390/430px 无溢出、二维码为正方形、按钮和遮罩关闭通过。375px 模拟截图位于 `artifacts/member-qr-preview/wechat-qr-preview-375.png`，使用无业务效力的测试二维码，不是原生设备截图。

现有 refined-layout 套件 10 项通过、1 项失败：固定页数断言 23 与实际 25 不符。在未改动的 rc.210 发布工作树用同一测试复现，登记 SYS-253。工作树最初缺少 Playwright 依赖，确认 lockfile 一致后复用 rc.210 依赖完成上述验证。

本轮未提交、合并、上传、切换体验版或正式发布；iOS/Android 微信原生真机验收尚未完成。

## rc.211 整合审查

用户随后授权检查、提交、合并和部署。整合候选沿用已有微信专属差异登记机制，为本次会员中心模板和样式固定双方审核哈希；支付宝源文件不变，其语法、事件、包体及其余共享页面校验继续执行。SYS-253 更新为25页并确认两条存酒路由存在，11项完整布局回归通过。预览页面逻辑在发布工作树再次执行，打开、内容不关闭、按钮关闭、离页、重载、错误与缺码保护均通过。上传及正式运行状态见后续rc.211交付记录。
