# 发布后无干扰补验（2026-09-24）

核对时间：2026-09-24 01:10 CST。生产rc.228/7e0b457a8c96752126d5ac472d5e8a53ef29dfc4，schema242。代码工作区基线ba4762e0。生产仅低频只读事务、限时日志聚合与GET探测；未创建顾客订单/支付/退款、扣料、打印、发送通知、写业务数据、调整权限或重新部署。

## 证据台账

| 范围 | 本轮结果 | 仍不能据此认定 |
|---|---|---|
| 生产版本与后台 | 镜像身份匹配，ready可写，API/worker受限身份、三屏开关及巡检定时器已核对 | 瞬时健康不是长期无故障 |
| 客人登录/下单/收款 | 发布后的正常营业记录证明真实微信登录、客人扫码下单及线上确认收款；已补此前延期的证据 | 不代表所有设备、所有渠道和顾客到账争议均验收 |
| 财务一致性 | 发布后收款（含部分退款）及成功退款的金额/币种/账务唯一性核对无差异；没有新同订单同金额未释放重复待付组 | 未取得渠道清算文件，不替代渠道对账；未付订单不直接认定漏款 |
| 历史错误尝试 | 原批准清单的资金指纹、零欠款、审计、停止重试、原状态及无新增资金账务再次通过 | 保留待核状态与可信晚到回调，不伪造关单或结案 |
| 库存 | 数量余额与流水汇总一致，无负数/重复份数扣料样本 | 账实盘点、损耗原因和待核成本仍缺外部事实 |
| 积分成长 | 积分账户/会员余额与成长流水汇总无差异；无当前退款待核申请样本 | 空表不算混合退款现场验收，沿用隔离证据 |
| 三屏履约 | 功能开关开启；本窗口无新批量制作/取走记录 | 无实物出品/交接/误取撤回现场证据，不自动造单补样本 |
| 打印 | 软件版本与近期心跳、任务终态已只读核对，无待投递积压 | 终态不证明纸面内容、打印机字库或实物送达 |
| 推荐503 | 功能关闭，客户端有对应降级，符合配置 | 不擅自开启经营功能 |
| 通知授权/预约变更确认 | 新发现受限角色行锁权限缺陷，最小候选已修复；见SYS-368/369 | 生产仍旧版本，不能声称新修复已生效 |
| 性能 | 有限真实访问样本中部分后台聚合页面较慢，无当时长事务/锁等待 | 不在营业生产压测；短时样本不能代表峰值容量 |
| 发布与恢复 | 复用rc.228正式部署、备份/OSS及schema242恢复证据；补SYS-367环境前置检查 | 当前新候选还需固定CI及正式发布验证，停业/故障注入不在本轮范围 |
| 浏览器与残留维护入口 | 本机浏览器连接关闭，HTTP公网ready可达；发现仍运行的旧维护入口容器，无公开映射端口 | 不猜测可安全清理，不停止容器；不把本机网络错误当作全站宕机 |

## 最小修复与验证

通知授权保留现有事务级授权锁和会员/预约父行锁；政策身份/模板由准确外键绑定，发送前仍复核政策是否有效。预约变更确认在原预约父行串行化，影响表保持不可变，保留唯一追加确认记录。没有扩展运行权限或新增迁移。

三组原集成测试从管理员运行改为必须使用标准测试流程创建的独立受限LOGIN，并核验真实权限。原代码在实际运行搜索路径下10项失败、3项通过；修复及增补后15项通过，含政策撤销、并发授权与权限不得扩展。pg_catalog兼容实验另留历史日志，已确认不是当前生产根因，未纳入产品变更。

发布检查新增稳定公网TLS探测、远端Bash语义检查、精确公开配置权限及实际镜像默认用户读配置测试。定向发布检查25通过/1既有环境跳过；完整Linux发布脚本检查交CI，macOS缺GNU flock不能冒充通过。源码/测试/发布状态以本报告后续追加为准。

业务数量、具体原始日志和查询仅保存在受限本地目录`outputs/post-release-audit-20260924/`，不随Git推送。停止与旧120人任务交接，原定时启动已暂停，避免重复执行。

## 运行身份扩展复验

应用层同类修复覆盖成本更正、员工业绩登记/退款冲回、跨桌顾客移动、观察记录更正、联系信息保留策略及活动积分退款。不可变事实保持无UPDATE权限：更正使用事务锁，观察锁原输入，退款锁原商品/积分账户，转桌仍由限定数据库函数复核，联系策略沿用已有受控函数。五组36项通过；原本缺失的观察更正用并发请求验证一次成功、另一请求冲突、原事件不能改写。

另有数据库专项缺口SYS-371：叠加规则发布及会员配置影响审批的触发器权限失败已在受限LOGIN下实证，优惠报价/赠品允许商品/预约政策/通知配置另有静态候选待完整路径复现。这些不并入应用层删锁补丁，也不通过扩大营业账号权限处理。活动集成套件中配置准备仍管理员夹具，发奖和退款worker才使用真实受限LOGIN；此套件通过不能证明运行账号配置审批已修复。诊断转换与失败日志保留在本地受限证据目录。

当前没有新增迁移；任何新候选仍须固定SHA全量CI。后台正常运行、已确认资金与库存一致，不代表以上边缘配置功能均无缺陷。待核成本、实际纸面打印、三屏实物交接及无样本路径仍保留，不虚构现场结果。

## 后续CI结论修正

ff8f4a9完整CI数据库2700通过/14失败，证明通知旧哈希还有pgcrypto命名空间兼容缺口；这不属于已观察到的生产收款故障。已新增schema243候选，将三处身份校验函数与应用查询改为等价内建SHA256，保持原权限、属主及条件，并加1秒锁等待/5秒单语句上限。pg_catalog限定路径的15项通知/预约回归通过。以上更新取代前文对初始候选“无新增迁移”的描述；仍需最终CI与发布兼容证据，生产未切换。


## 第二轮CI与发布延续检查

CI35898042776质量、浏览器、性能通过，数据库2639通过/1失败/74跳过：四组套件准备阶段固定要求旧schema242，升级子套件由此连带失败。版本断言改为精确匹配仓库迁移清单末项，未降低权限或财务断言。另发现SYS-373旧维护计划与后续普通升级的延续缺口，已列为本次部署前条件；不删除历史证据放行。

修正后五组财务/升级回归75项通过；其中跨维护属主升级另运行完整七组129项全部通过，没有跳过。类型检查通过。最终完整CI待固定提交。
