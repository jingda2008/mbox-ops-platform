# 1.0.0-rc.210 系统审计修复

关联 SYS-242—252，逐项根因、修复和证据见 [修复核对表](system-audit-fixes-20260919.md)，原始只读审计见 [系统审计](system-audit-20260919.md)。用户已授权修复、提交、合并和部署。

支付查询完整身份哈希为不超过 128 字符的幂等键；验证观察应用失败和渠道查询失败分开记录，通用 worker 失败进入健康状态。资金未知状态、渠道核验、幂等账本、人工现金与渠道付款独立事实保持严格。

服务号固定入口以已验签 receiver 定位受信任启用账号并持久化；订阅页移除全部示例发送，保存带签名身份的客户端授权上报。上报不冒充平台配额或送达证明。bizsend 明确 errcode=0 即记录受理，缺失 msgid 保持 null；验证码与保管到期采用独立列。

图库按用途分页、缩略图、懒加载；私有缓存每次回源仍校验当前员工权限。慢请求分段记录认证、连接及事务准备、权限、元数据、字节及提交、转换、发送。推荐只读配置取消行锁，写入仍保留锁；微信与支付宝相同业务逻辑在关闭时明确降级。

迁移 221 只新增只追加授权上报表及图库分页索引，不修改既有财务或库存事实。发布前必须正常备份并读回，候选迁移及 readiness 要求 schema 221。旧应用不依赖新增对象，标准回退保留旧镜像与数据备份。

## 验证与交付边界

精确结果在核对表追加，CI 与正式发布证据绑定本次不可变 SHA/镜像。TC 登记继承历史领域证据，不代表本轮重新逐项现场执行。

后端发布与原生小程序上传、体验版、真实微信授权送达、门店打印桥安装、实物票和现场高峰为独立证据。本版后台和服务号页面随服务发布；原生推荐降级代码需对应平台候选上传后生效。历史已处理 20 元退款不重开；历史资金、成本和盘点差异不由代码修复猜测填平。
