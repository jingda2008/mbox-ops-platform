# M-Box `1.0.0-rc.54` 阿里云验证环境部署记录

部署时间：`2026-07-28 16:23 CST`
部署环境：阿里云验证环境
发布提交：`e0c389d531b7ff715a8098f280a68a82a2751b1b`
版本标签：`v1.0.0-rc.54`

## 本次范围

- 空桌点击后直接在桌位旁显示人数、销售归属和确认开台，不再跳到页面其他位置。
- 开台成功后自动收起选择区并留在全店现场，不再自动进入全屏点单。
- 现场桌位按员工后台配置的责任区域显示；李艳未负责W区时，现场操作台不显示W区桌位。
- 店长保留全店监督数据权限，但不因此获得未分配责任区的桌台操作权限。
- 手工开台接口和AI开台工具统一执行责任区校验，旧页面或AI命令均不能绕过后台配置。
- 平板和手机窄屏保持单列就地开台，不产生横向溢出。

## 不可变发布证据

- PR：`#35`
- PR CI：`30341297803`，通过
- main CI：`30341660899`，通过
- release CI：`30341996997`，通过
- GitHub预发布：`v1.0.0-rc.54`
- 发布提交：`e0c389d531b7ff715a8098f280a68a82a2751b1b`
- 线上镜像：`mbox-ops:1.0.0-rc.54-e0c389d`
- 线上镜像摘要：`sha256:af4c8939cb399227a302667a6113691f1bf76d008a47aa6a78a1c66077ad03c6`
- 数据库迁移清单摘要：`sha256:103fdcdc59457f1760428043949c86879f04ec6c07088452a44b758625771fdd`
- 数据库迁移无变化，按发布策略复用近期备份。
- 上一版本回滚容器：`mbox-app-rollback-e0c389d-20260728-162311`

## 验证结果

- 135个测试文件、1027项代码与业务测试通过。
- 40项浏览器端到端流程通过。
- 李艳责任区移除W区后，现场页面不显示W区；仍可查看其授权范围内的全店监督数据。
- L04空桌点击后在桌位旁显示人数和销售选择；开台后控件自动收起，页面仍停留在“全店现场”。
- 1024 x 768平板视口完成就地开台且无横向溢出。
- AI开台与手工开台均通过同一服务端责任区校验；未分配区域返回明确拒绝原因。
- PR、主分支和版本发布三层CI全部通过。
- 阿里云候选容器在零流量状态完成就绪、认证、PostgreSQL投影、版本SHA和镜像摘要核验后切换公网流量。
- 公网`/api/ready`返回`ready`，运行提交和镜像摘要与GitHub发布一致。
- PostgreSQL规范化运营读模型就绪，主投影和读模型修订均为3123且数量一致。
- 公网主页返回HTTP 200，旧版本保留为可回滚容器。

## 尚未完成

- 李艳需要在门店终端重新进入现场页面，确认W区隐藏，并在负责区域完成一次实际开台复验。
- 系统管理员需要复核每位员工的责任区域；未配置区域的员工不能执行该区开台。
- 真实微信/星驿支付、退款、物理POS和日结联调。
- 真实打印机、KDS、门店网络和全岗位真机验收。
- 66条P0/P1经营验收项。
- 真实硬件和语音转写接入。

因此本版本是可供门店继续模拟和验收的验证版，不得标记为商业生产完成。
