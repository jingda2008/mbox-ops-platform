# 员工会话与盘点复核后续修复

2026-09-14，侧会话按用户“继续解决”开展本地修复。独立工作区 `/Users/jingda/mbox/mbox-staff-session-review-followup-20260914`，分支 `fix/staff-session-review-followup-20260914`，基线 rc196 `e88aab4b8ebaf62c948ed4cf1606804e8e163ec0`。不修改主会话工作区或生产库存；本记录不是上线证明。

## 查明的边界

制作队列检查在线租约，员工登录本身可在在线租约到期后保持有效。手机回前台时，队列读取与独立心跳没有顺序保证；旧客户端收到 `actionSessionValid=false` 直接展示会话失效。客户端故障回归在旧代码上4失败/1通过，证明缺少主动续期与重读；不据此断言原截图唯一原因，撤销、硬过期、设备口令失效仍可能导致真实拒绝。

新客户端仅在服务端明确返回操作会话无效时调用既有认证心跳，成功后重新读取队列一次；不在本地放行操作、不重放制作命令、不延长登录硬过期时间。401保持重新登录，网络失败保持错误，再次读取仍无权限则保留拒绝。有效队列不增加心跳请求。

盘点不能自审、库存变动后禁止旧盘点覆盖均保留。退回单新增逐物料“重新盘点”入口，跳转已有单项盘点表单并清空数量；提交产生独立新单，旧单及退回原因保留。只有原有盘点权限可用，不允许从待复核单直接复制旧数量。物料当前不可用时提示刷新核对。无复核资格的用户收到明确的联系其他审批人指引。

## 验证

- Web和服务端类型检查通过；构建通过；lint通过，保留既有警告。
- 员工操作及认证相关常规测试93通过、45项因无数据库跳过；另启本次专用临时PostgreSQL，在独立数据库执行认证、KDS权限、制作队列及盘点接口57项全部通过。新数据库测试确认休眠后在线状态可恢复，登录ID及硬过期不变，已撤销登录不能续期。
- 手机浏览器2项通过：故障注入操作会话无效后的心跳与重读；真实隔离数据库的盘点提交、禁止自审、另一员工通过/退回、读失败重试、退回后重新实盘450毫升、新单审批及库存450毫升、旧单仍已退回。
- 浏览器首次启动因默认本地测试数据库55441未运行而失败；改用本次专用容器55446、独立随机数据库和18836页面端口后通过。未改既有数据库/服务。
- 原375px长单号和多商品历史布局独立复核1项通过；三个不同浏览器流程合计3项通过。首次长截图出现粘性栏位置采集异常，追加回顶部定位断言并改用实际视口截图，定向重跑通过；不将截图采集异常认定为线上错版。最终截图保留在 `artifacts/normalized-browser/results/normalized-all-roles-busin-18f17-我的已制作多商品长单号在窄屏逐项显示，保留商品处理入口/prepared-history-375.png`。本次专用临时数据库容器在测试后停止并自动移除。

## 现场还需完成

现有待办应由有审批权限且非提交人的员工核对。库存已变动的单据先退回，提交人重新实测并使用新入口提交，由另一审批人及时复核；选择对应物料没有出入库变动的核对时段。不能为了消除待办直接通过旧数量。上线时20条样本中的17条失效、19条本人提交只是当时该页快照，不是当前全店统计。

本轮不更改微信小程序包；修改的是员工网页。合并、生产部署、真实员工手机验收和历史盘点实际处理均另行留证，SYS-220/221及会话问题不记作商业关闭。

## rc197发布准备（2026-09-14 20:50 CST）

用户明确授权提交、合并、部署。已合并最新主线rc196交付文档并保留历史，完整本地检查1789通过/706环境条件跳过，发布元数据及构建通过。rc197不新增迁移；现网预检仍为健康rc196。远端不可变CI、合并标签与标准部署按实际结果续记。
