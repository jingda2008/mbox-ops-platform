# 微信小程序审计修复与交付边界

记录时间：2026-09-14 12:20 CST。源码目录：`/Users/jingda/mbox/mbox-system-audit-20260914`；分支`fix/wechat-slow-table-bill-20260914`；基线`3f645374e1337021e835789f9df931276c662b88`，本轮修复未提交。

## 本轮已实现

| 项目 | 修复后的行为 | 验证 |
|---|---|---|
| SYS-211 慢桌账阻塞加菜 | 菜单、购物车就绪即可加菜；提交仍核验桌账，复用读取，失败保留购物车；过期桌次、旧付款响应不得覆盖新状态 | 12项新增竞态/错误场景，原代码可复现失败；每场景30次、180商品VM测量 |
| SYS-213 协议读取依赖登录 | 隐私与服务协议均可匿名读取已发布正文；服务器固定门店范围，忽略伪造门店参数；个人资料和写入仍要求身份 | 真实主应用无身份读取、假身份与假门店、个人接口401 |
| SYS-213 发布后日期转换失败 | 隐私政策数据库日期显式转文本再规范化，避免真实已发布正文返回400 | 隔离PostgreSQL中先草稿不可见、发布后内容与摘要一致、撤下不可见；修前400已留存 |
| 协议与入会读取分离 | 仅浏览服务协议不读会员；身份慢/失败时正文仍显示，入会按钮禁用；读取失败可重试且不误报“未发布” | VM异步/重复请求/离页场景；原生条款可读、按钮禁用截图 |
| SYS-216 饮食说明单独同意 | 微信默认不勾选，修改说明后取消原选择；填写后必须单独同意；清空保存撤回；授权版本和决定与偏好同事务保存 | 未同意/旧版本/伪造元数据拒绝；数据库留时记录；旧授权重放不覆盖新撤回 |
| SYS-217 偏好读取失败覆盖资料 | 首次加载失败时禁止用空白默认值保存，提供重新读取 | 无任何写入的失败回归；原生页面和模板验证 |
| SYS-212 非法JSON误报500 | 统一400与REQUEST_JSON_INVALID，错误信息不反射请求内容；空PATCH/DELETE仍兼容，超体积413和内部500保留 | 三种写方法、无效体、空体、超体积、内部异常 |
| SYS-210 固定测试营业日失效 | 从数据库营业日建立当天及次日，不改变生产关账/日期校验 | 数量142项及全量数据库回归 |

支付宝小程序代码无修改。为允许微信三页有独立修复，双端一致性检查仅对这三份经审阅模板记录双方精确SHA；其他布局、事件实现、样式、语法与包检查保留，后续模板变化会重新触发检查。

## 测试证据

证据目录：`artifacts/wechat-all-remediation-20260914/`。

- `check-verified.log`：最终完整检查（元数据、架构、微信及支付宝检查、284项小程序发布测试、类型、静态检查、常规单元、构建）。常规计数以日志最终摘要为准；695项因无数据库条件跳过。
- `full-postgres-complete.log`：2149通过、1条件跳过；涵盖新增饮食授权/撤回。最后仅修正无条款服务时503的错误码/消息顺序并增加常规回归，该错误分支由最终完整检查覆盖，无数据库语义变更。
- `consent-postgres.log`：59项定向接口及真实数据库测试；与全量重叠不相加。
- `policy-client-final.log`：11项协议、授权及失败恢复行为测试，已纳入284项。
- `native/INDEX.md`：变更页面原生复核及六张截图，包含真实点击重试恢复；身份和正文为隔离夹具，未走真实微信授权、付款。
- `slow-bill-performance.json`：同基线、同菜单、固定API延迟，首次/重进/转桌/轻载各30次，每版本120次。首次可加菜p95 411→36ms，重进410→35ms，转桌410→35ms，轻载40→35ms；加菜p95 27–28ms；各阶段零失败。仅VM逻辑指标，不能作为现场高峰、原生渲染或支付渠道延迟。

保留中间失败日志：发布政策日期错误；测试初始化未设置RLS作用域；双端模板一致性需登记微信差异；偏好静态契约未适配第三参数；新增代码只读类型诊断。均有后续修正和通过结果，不以重试掩盖产品缺陷。现有构建分包与静态警告未当作已修复。

## 用户已确认的资料

运营主体、对外联系渠道及法人批准资料已按用户回复保留在本地核对包；原始会话记录不随公开仓库提交。这不等于已阅合同附件或已完成后台独立审批。

`protocol-review/`保留原稿、V3完整正文及摘要、用户确认记录、微信饮食单独授权文案、30日验证手机号和180日报名联系方式的策略草稿、发布批准参数草稿。正式审批仍使用真实授权员工身份及原有独立发布流程，不捏造另一员工或倒填生效时间。

## 尚未闭合的营业/发布项

1. 本轮未提交、推送、部署后台、上传微信或提交平台审核。需先交付后端新增公开条款与微信偏好接口，再上传匹配微信包；旧后台不支持新接口，不能只上传前端。
2. SYS-201仍开放：本轮生产只读查询确认隐私版本为空、联系方式保存策略为空、已发布会员条款仅269字。批准草稿不等于正式发布。需通过真实角色发布并回读版本/摘要，完成微信隐私保护指引。
3. 已实现的自动联系方式处置仅覆盖报名联系方式、验证手机号。其余已批准保存类别需要执行配置或人工处置记录，不能承诺已自动清理所有数据。不得仅为验收运行生产删除。
4. 正文“目前不向境外提供个人信息”尚需按实际外部服务的数据范围核验；服务器境内不等于数据处理链全部境内。
5. SYS-202/211仍需发布后的真实手机、门店网络及高峰混合负载验收。提交付款继续等权威校验是正确性要求，未用取消校验换取速度。

协议规则参考边界：电子商务平台的交易记录期限不能不加区分地套到所有单店应用；本包的3年为用户确认的经营保存基线。健康信息单独同意依据个人信息保护法第28—30条，代码测试不代替完整法律或跨境合规审查。参考官方[个人信息保护法](https://www.npc.gov.cn/WZWSREL25wYy9jMi9jMzA4MzQvMjAyMTA4L3QyMDIxMDgyMF8zMTMwODguaHRtbD9yZWY9aW1i)、[电子商务法](https://www.samr.gov.cn/zfjcj/tzgg/art/2023/art_d337c3291e8b40459ca03dea54395856.html)。

## rc194继续修复

用户于2026-09-14授权继续修复审计其他问题，检查后统一上传、合并和部署。SYS-209补账号/来源/门店的分层限流；SYS-218解决嵌套限流事务占用连接导致登录等待；SYS-214升级依赖并纳入开发依赖扫描；SYS-215修正扩展夹具单价与桌台前置条件；SYS-208补售后控件最低44px。最终提交/部署证据见release-1.0.0-rc.194.md的后续记录，前文12:20快照保留，不代表当前发布结论。
