# 1.0.0-rc.220 发布传输属主兼容修复

从普通用户的发布机使用rsync归档传包，会把本地UID/GID带到root接收的生产主机，导致维护计划无法通过root:0600校验，升级在停写前退出。传输现在显式忽略源属主和属组；重试时只将指定的普通维护计划文件设为root:0600，拒绝符号链接，原正式入口检查继续执行。

本版业务代码、财务规则和schema242与rc.219一致。首次失败、旧线上健康且未进入维护的证据保留。发布、原生传输和实际生产验收须分别记录，不代表小程序上传或现场验收。
