# M-BOX 1.0.0-rc.68 性能、反馈与质量证据升级

## 目标

本候选重点修复高峰时每次操作都读取或写回整店聚合数据造成的长尾延迟，并把运行指标、TC真实覆盖度和独立质量复审纳入发布门禁。它不改变支付审批、库存事实或责任权限。

## P1整改

- 员工心跳写入独立租约表，不再改写整店revision；登录、退出和真实在线状态变化仍保留审计。
- 心跳权限校验使用轻量员工目录与租约，不再为每次心跳读取并克隆约7MB整店状态。
- 进程启动一次性载入租约，避免逐员工连接数据库。
- 中国时间转换复用格式化器，消除工作区投影中的重复高成本对象创建。
- 预约接口复用已鉴权状态，避免同一请求重复读取聚合数据。
- 同一匿名客人、同一桌次的会话轮询只持久化一次到访事件；并发请求合并，数据库幂等约束继续兜底。
- 重复点击已完成任务返回权威终态，不因旧页面或网络重试退回原状态。
- 员工首屏先展示工作区，非关键初始心跳延后执行；关键按钮保留即时反馈和失败恢复。

## 可观测性与指标

- 增加低基数规范化路由请求计数和延迟直方图，不把业务ID或令牌写入标签。
- 就绪探针公开数据库延迟、连接池总数/空闲/等待和变更队列状态。
- 增加历史日志分析工具，输出每条规范化路由的样本量、P50、P95、P99、最大值、状态码和关键错误信号。
- 指标目标和样本门槛见`docs/runtime-quality-standards.md`；样本不足不得判定通过。

## TC质量管理

- 保留213条经营基线和原始通过条件。
- 将“验收状态”和“工程覆盖度”分开，明确外部依赖、代码支持待现场验证、部分实现和真实能力缺口。
- 当前登记有207条经营验收阻断和24条质量补充阻断，共231条未通过商业发布门禁；验证版本上线不能替代真实支付、打印、弱网、应急和连续营业验收。

## 数据库

- 新增迁移`023_staff_presence_leases.sql`，按租户和门店隔离员工在线租约并强制RLS。
- 不迁移旧测试业务数据；部署候选前必须执行迁移并验证表、策略和索引。

## 发布边界

- 当前文档描述候选目标，不等于已部署或已通过复审。
- 只有全量检查、浏览器与压力测试、独立质量复审、不可变镜像候选验证、切流后指标和回滚证据全部通过，才允许替换`rc.67`验证环境。
- 商业生产还需真实支付退款、物理打印、弱网和连续至少3个真实营业晚班达到SLA。

## 证据索引

- 运行指标：`docs/runtime-quality-standards.md`
- 时间与容量架构：`docs/time-and-capacity-architecture-1.0.0-rc.68.md`
- TC报告：`docs/tc-execution-report-1.0.0-rc.68.md`
- TC逐条登记：`docs/tc-execution-register-1.0.0-rc.68.csv`
- 发布阻断：`docs/tc-release-blockers-1.0.0-rc.68.csv`
- 商业化清单：`docs/commercialization-pending-checklist.md`

## 验证结果

- 当前本地`npm run check`通过：146个测试文件、1152项测试，以及lint、TC一致性、TypeScript、生产构建和确定性种子校验均通过；移动端关键浏览器冒烟3/3通过。
- 最新不可变CI为PR #62运行`31329190196`：质量、数据库、浏览器业务流程和七个独立性能阶段均完成，但性能门禁失败，发布结论为`DENY`。
- 员工浏览器双帧P95为701.7ms；KDS开始事件循环P99为104.53/104.07ms；KDS完成为155.98/148.24ms，一个实例写队列等待P95为131.45ms、写服务P95为550.93ms。
- 同一CI各实例连接池获取P95低于0.11ms，当前证据不支持通过扩大连接池或服务器扩容解决这些延迟。
- 当前本地候选在Bootstrap到达后直接显示真实岗位首页，30个新浏览器上下文双帧P95为92.1ms；完整运营工作台继续并行加载，点击请求会在加载后执行。该数字仍须新提交CI复现。
- KDS分段指标确认上一轮队列长尾来自调度器与前台写的相位冲突，不是连接池不足。将调度器改为在前台写后的早期空档执行后，原条件300/300成功，客户端P95 266.6ms、队列等待P95最高58.1ms、事件循环P99最高98.0ms；45次调度写仍执行。该数字仍须新提交CI复现。
- 压测启动器现在会在测量结束前再次核对源码指纹；运行中代码或测试工具变化会保留诊断数据但拒绝作为发布证据。通用TC登记分别记录执行状态和制品状态，避免`artifact_missing`被误写成未测试。
- 5次/秒KDS容量探针、三轮完整重复、多营业日增长、真实设备/微信弱网、星驿真实支付退款、物理打印和连续3个营业晚班仍未通过。
- 质量总账已在失败CI中正确生成并尝试上传，但GitHub Actions制品额度耗尽，运行与质量制品均未归档；`CAP-013`继续失败，需授权清理旧制品或配置替代不可变存储。

## 当前状态

- 版本：`1.0.0-rc.68`
- 状态：本地候选整改与测试中，独立质量复审结论为`DENY`，尚未部署。
- 已关闭：租约退出绕过、启动复活、跨实例KDS在线状态误判、PostgreSQL营业日时区偏移、并发就绪503误报。
- 性能状态：客户页面和关键API路由在最新CI达到对应门禁；员工浏览器、KDS事件循环、KDS完成队列等待和5次/秒容量探针仍失败。
- 当前候选诊断：员工浏览器和2次/秒KDS完成已在本地达到门槛，但不可变CI尚未裁决；不得写成正式通过。
- 云端容量阻断：无法取得阿里云实时CPU、内存、OOM、容器重启与连接指标；历史双容器部署曾资源饱和。
- 验收阻断：207条经营验收和24条质量补充，共231条发布阻断；真实支付、打印、弱网和连续3个营业晚班尚未验收。
- 供应链：生产与开发依赖审计均为0漏洞；因最新版`@capacitor/assets 3.0.5`仍携带无修复的critical/high传递依赖，本候选移除图标再生成工具并保留现有iOS资产，上游修复前不重新生成安装包资产。
- 发布动作：可以继续形成提交和CI候选证据，但不得替换阿里云`rc.67`或批准商业生产，直到全量测试、云端资源核验及P0/P1现场门禁关闭。
