# M-BOX 1.0.0-rc.193

rc192已部署并健康运行，但发布脚本的配置保留名单未登记数量售后开关，导致正式实例仍按默认关闭。rc193只补MBOX_QUANTITY_AFTER_SALES_ENABLED的规范化保留与回归；不改金额、库存、权限和小程序业务代码，不新增迁移，schema202及门店配置v24保持。

## 验证和发布

修复前新增规范化回归确定失败；修复后validation/production两环境各覆盖missing/true/false/typo，并重复规范化两次，已通过。缺省仍关闭，错误值原样交给运行配置校验拒绝；28项配置解析测试通过。rc192的完整CI/数据库/浏览器证据保留原提交身份，rc193仍执行自己的不可变标签检查与标准发布，不能借用旧镜像摘要。

生产部署前只恢复本店已授权的显式true，标准脚本完成后核对正式容器和已规范化文件均保留true，功能才能记为已启用。同版本false保留原申请恢复；没有新增业务事实前不需要回退健康的rc192。

微信rc192开发版已经上传，rc193小程序源码与其相同；后续是否重新上传必须记录实际包提交身份。支付宝编译通过但当前账号无法关联既有应用，未上传；两端审核/正式发布、真实手机及高峰验收不能由服务端部署代替。已验收Windows打印链路保持，积分用途/券扩展暂缓，协议V2仍为送审稿。

最终提交、CI、镜像、备份及线上回读在实际完成后追加。


### 2026-09-14 02:29 CST 分页测试隔离修正

首个rc193候选908d84a的CI34774248144数据库测试2145通过/1失败，失败仅为交班分页测试累计超过默认5000ms。原测试在共享门店中以每页2条遍历前面全部售后测试遗留记录，且依赖前置测试准备权限/多条记录，耗时和是否可独立执行都受运行顺序影响。

将该用例移到独立门店并显式建立三条申请和员工/审核权限：两页恰好三条、无重复遗漏、普通员工仅见自己、拒绝仍暂停可见、恢复后消失均直接断言。保持原5000ms上限和性能门禁，不改生产查询、索引或权限。拆分后原生命周期文件142项通过；独立分页在空库完成迁移后96ms通过，配置开关回归及28项配置检查仍保留。新候选重新CI，首轮失败/夹具初始化修订记录均保留在发布artifacts。
