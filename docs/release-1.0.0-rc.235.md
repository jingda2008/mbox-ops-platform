# 1.0.0-rc.235 后厨旧页面恢复与在制批次入口

rc.234部署后，现场仍向旧KDS完成接口提交新制作批次。旧页缺少准确恢复提示，页内刷新仅重读数据，不加载新的页面代码；另复现竖屏只有在制批次时初次仍显示空待制作列表。

旧队列的attentionMessages和被拒绝的旧完成请求均明确提示用浏览器整页刷新并进入制作中。保留409代码与原拒绝边界，不让旧接口越过批次、份数或库存校验。新屏刷新先读取当前HTML版本标识：有正式新版本时保存精确草稿并重载；版本检查失败或无更新时保留现有队列正常重读。待确认/执行中操作禁止重载，只能恢复原操作。竖屏首次没有待制作而有在制批次时进入制作中，员工手动选择后轮询不切换。

独立PostgreSQL21项通过，三屏联合浏览器17项通过；新增用例覆盖草稿保留、未知结果刷新禁用及不自动重发命令，补验版本检查503回退。前端类型、正式构建、lint通过。schema247不变，无数据回写或权限调整。

采用标准deploy/aliyun/deploy-release.sh发布，保留rc.234回退。SYS-379和SYS-383真实后厨设备验收保持开放；已加载旧程序的设备仍需一次实际整页刷新，不能声称服务端部署能远程替换平板内存中的程序。本记录建立时未部署。

## 发布结果

2026-09-25 00:36:36 CST已部署，后续复验另发现口令轮换授权冲突，见[发布记录](production-release-1.0.0-rc.235.md)。现场恢复继续开放。
