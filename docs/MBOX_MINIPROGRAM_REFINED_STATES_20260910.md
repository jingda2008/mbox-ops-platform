# 小程序精致化与全状态检查

时间：2026-09-10 16:44 CST。工作树：`/Users/jingda/mbox/mbox-member-growth-reliability-20260909`，分支 `fix/miniprogram-refined-states-20260910`，从远端最新主线 `71b4ba9` 开始。仅本地修改，未提交、部署或上传。

## 已修改的共用层

- 23 页使用同一精简样式层：主要标题由原有约 40–48rpx 统一到 34rpx，弹窗标题 32rpx；减少装饰间距和厚重阴影，维持原品牌颜色。
- 主/次/危险操作改为内容自适应行高，至少 44px 点击高度，长文案换行；关闭按钮的实际点击区保持 44px，不因窄屏 rpx 缩小而难点。
- 成功/警告/错误提示统一节奏。支付结果按钮等宽，小屏改为单列；确认订单底部使用可收缩列，保留原滚动容器和安全区。
- 服务三列按钮、服务反馈、表单、桌账订单标题/金额区域紧凑化，长金额不省略、不改精度。法律正文、支付状态、业务事件和所有请求逻辑不改。
- 加载/空态/错误组件使用独立样式隔离，已单独收紧图标和字体；发现并同步支付宝旧组件样式，补入组件一致性检查。授权事件、平台支付接口、默认许可状态不变。

## 验证及边界

`npm run test:miniprogram:layout`：5 个测试组通过。包含 23 个真实页面样式叠加环境 × 320/375/390/430px（92 组）、4 支付状态 × 4 宽度（16 组）、确认底部 4 组及长金额 1 组。**这些是实际 CSS 的组件/层叠夹具，不是 23 个完整原生页面截图或真实支付测试。**

- 完整 `npm run check` 最终通过：`/tmp/mbox-refined-check-recheck.log`，常规测试 1604 通过/506 条件跳过；双端静态检查及共享组件检查通过。
- 期间旧负载测试的短时 mock 门禁一次退出非零，未修改或放宽门禁；单独 9 项重新执行通过后，完整检查再通过。保留失败 `/tmp/mbox-refined-check-final.log` 和复验 `/tmp/mbox-refined-load-recheck.log`，不能据此断言根因已经确定。
- 支付宝官方编译通过 23 页：`/tmp/mbox-refined-alipay-compile.log`，产物 `/private/tmp/mbox-alipay-compile-3Eq011`；不是支付宝实机或上传证明。
- 组件前后对比位于 `artifacts/refined-layout-20260910/service-before.png` 与 `service-after.png`，图中标明非原生、没有发送服务/付款。
- 另建本地隔离 schema186/端口18806，不使用旧 schema181/18796 环境。微信开发者工具曾截图不可用，后恢复并观察到首页、服务页正常加载，服务页三列两行。随后控制台聚焦/粘贴反复超时，**原生全路由和全状态验收尚未完成**；中途工程观察不替代最终源码逐页验收。隔离拦截真实支付、订阅通知、电话和外部请求，不上传隔离工程。

## 23 页状态清单（最终原生逐项验收待补）

不是所有页面都有支付状态；支付状态必须在支付入口和消费方中核对，不能用一张成功弹窗代表完整链路。以下各页均需加载、空态、异常、长文案、小屏及返回恢复；共有样式已修改不等于状态已验收。

| 页面 | 额外需要核对的状态 |
|---|---|
| 首页 | 扫码前/后、会员/非会员、服务入口、入会弹层、演出/内容详情 |
| 品牌故事 | 无内容、长文章、图片加载失败、返回 |
| 预约 | 扫码前/后、多步表单、选择/提交/取消、演出变更、键盘 |
| 点单 | 扫码前浏览/后下单、空车/多行/多份套餐、推荐忽略/接受、券、备注、支付成功/取消/失败/未知/迟到确认 |
| 超嗨 | 活动为空/多项、会员入口、筛选/返回 |
| 活动详情 | 停止报名、已有报名、待付/成功/取消/未知/退款、底部操作 |
| 我的 | 未登录/已登录/会员、卡信息、授权弹层 |
| 口味偏好 | 多选、输入、保存中/失败、键盘 |
| 联系我们 | 有/无企业微信配置、长联系方式、授权取消 |
| 优惠券 | 空券/多券/过期/不可用、展开规则、返回定位 |
| 等级与兴趣卡 | 可申请/审核中/已拥有/拒绝、长名称 |
| 联系许可 | 未授权/多选/撤回/停止全部、保存失败 |
| 消息授权 | 未允许/拒绝/允许、真实授权能力不可用 |
| 演出 | 空排班、跨日分页、长艺人名、多场次、临时变更 |
| 积分 | 零余额、长金额/积分数、多流水、分页 |
| 会员中心 | 非会员/不同等级/权益缺失、说明展开 |
| 会员条款 | 长正文、不同意/同意、授权拒绝、键盘/安全区 |
| 订单与桌账 | 扫码前/后、未付/部分支付/已付/退款、个人历史/本桌、多行套餐与长金额 |
| 桌边服务 | 未扫码/已扫码、提交中/已存在/失败、订单选择、长备注 |
| 服务进展 | 空列表、处理中/完成/升级、反馈成功/失败 |
| 投诉 | 未扫码、输入/选择、提交中/成功/失败、键盘 |
| 点歌 | 未扫码、时段、点歌/延时、提交中/失败、条款 |
| 隐私 | 长正文、展开联系方式、电话更换/授权拒绝 |

后续原生验收必须绑定最终工作树内容，并分别标注真实交互与注入的布局状态。没有对生产订单、顾客、收退款和打印数据进行写入。
# 17:04 补充：用户截图与组合展示澄清

- 删除“我的夜晚”装饰头部；“我的专属身份 / 发现同好”改为会员卡同宽品牌绿导航，保留身份读取、重试及已有身份卡。
- 未扫码提示压为横条，320/375/390/430px组件测试均不超过100px高，扫码按钮保持44px可点击高度。
- “甄选组合”扫码前后均收齐全部套餐，按套餐类型归集，不受后台分类差异影响；搜索仍正常过滤。
- 扫码后独立显示“推荐组合”，沿用后台算法和最多三款限制，不替换菜单目录、不编造推荐；空推荐明确提示可查看全部套餐。
- 当前新增菜单专项36项、CSS布局6组、完整check通过。只做本地修改，未发布；真机扫码、推荐实际返回和最终页面截图仍需原生验收。
