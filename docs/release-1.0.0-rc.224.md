# 1.0.0-rc.224 已迁移维护重入修复

rc.223已将生产迁移到schema242，但因原有未决支付/未消费回执保持维护。正式同SHA重入发现旧撤回记录校验仍把当前库与撤回时schema224比较，误拒绝本次已完成的迁移。

本版本只修正旧撤回记录校验：首次进入仍要求原schema；重入时额外接受当前已验证哈希日志中schema-provisioned事件当时绑定的目标schema。仅声明新的forward-target、无实际完成事件、不同源绑定或其他schema均拒绝。旧撤回日志/凭据、停止的guard/ingress、原资金基线、备份、epoch和全部发布条件仍保留。财务、吧台、三屏代码及242条迁移不变，不覆盖rc.223标签。

回归覆盖首次进入、完成迁移后的同SHA重入、下一SHA向前恢复仍处于前次目标schema、未完成的新目标、未知中间schema及篡改源绑定。原代码在本轮完成迁移重入用例失败，修复后通过。

2026-09-23 16:12 CST：本地控制器20项通过；完整发布检查、CI、原生演练和实际恢复结果待追加，当前生产仍维护，不能宣称营业恢复。历史支付仍只按原渠道可信结果处理，不以发布修复替代财务事实。
