# rc194审计修复交付记录

最新状态（2026-09-14 13:45 CST）：代码已检查、上传、合并，后台已部署；用户已确认手动上传微信版本`1.0.0-rc.194`，微信开发版上传按用户确认记为完成。本次未独立回读平台上传记录或包身份；正式协议、平台正式发布和实际设备/门店验收仍独立开放。下文13:32的工具拒绝记录保留为历史。

## 修复与版本

本轮覆盖SYS-208—218：44px售后触控、跨设备员工登录限流、营业日测试、慢桌账与加菜解耦、非法JSON分类、匿名协议读取及日期转换、开发依赖漏洞、扩展浏览器夹具、微信饮食信息独立授权和撤回、资料读取失败禁止空值覆盖，以及真实小连接池下登录/切换的嵌套等待。具体行为及失败复现见[版本说明](release-1.0.0-rc.194.md)和[微信修复记录](wechat-remediation-20260914.md)。

| 项目 | 已核实结果 |
|---|---|
| 修复PR | [#223](https://github.com/jingda2008/mbox-ops-platform/pull/223)，已合并 |
| 审查提交 | `923a050b56aa6ae731aeb5d68b158938f4207262` |
| 发布标签/合并提交 | `v1.0.0-rc.194` / `1093206b7b656af95970bf297cf8b811bacf9cbd`，与审查提交文件树相同 |
| PR CI | [34807505575](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34807505575)，成功 |
| 标签CI | [34808133364](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34808133364)，成功 |
| 发布CI | [34808133357](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34808133357)，成功 |
| 镜像摘要 | `sha256:3053d9244c06459ba56d6eb329d781a1036a2241bad973373278219be085c1bb` |
| 数据库 | schema202，无新增迁移 |

## 验证

最终本地完整检查通过：常规1780通过/704数据库条件跳过、微信发布逻辑284通过；隔离PostgreSQL2160通过/1条件跳过；最终默认浏览器66通过/11条件跳过。会员、离店重做及停止新增后恢复同时开启的扩展浏览器77通过，该扩展快照早于最终SYS-218改动；最终认证由后续完整数据库、默认浏览器及不可变CI覆盖。发布配置28项及入口烟测通过，全依赖审计0项已知漏洞。各套测试有重叠，不相加为独立商业验收总数。

最初全库旧会员长流程超过原5000ms限制；独立诊断及最终全库通过，未修改该测试、断言或时限。真实单连接池并发原样复现超时，修复后及等待期间撤销权限的回归均通过。失败记录保留。微信30次每场景的VM慢桌账对比和原生截图仅证明对应模拟/开发工具环境，不证明真实高峰性能。

## 后台部署

使用标准`deploy/aliyun/deploy-release.sh`，最终部署时间`2026-09-14T05:23:31Z`（北京时间13:23:31）。不可变包、备份上传与回读、候选健康、切换后四入口HTTP/渲染及部署证据均通过。上一健康rc193保留为停止状态的回滚容器。

第一次已切换后，本地发布工作区缺少浏览器验证依赖，标准流程自动回滚，回读确认rc193健康。补入已测试且同锁文件的依赖后，本机预检又遇到源站访问超时。采用仅限本次命令的已验证源站解析，保持TLS校验、原断言和超时；旧版本预检通过后完整重跑部署成功。冻结代码与标签未改变。不能由这些检查推断期间所有真实客人操作均无影响。

最终主站与支付域名的`/api/ready`均为ready，SHA、镜像、schema和production身份匹配。`/`、`/guest?table=W01`、`/reserve`、`/staff/live`通过HTTP及渲染核验。数量售后开关在正式源配置、规范化发布配置、活动配置、容器环境及程序解析均为true。未认证微信偏好写入返回401；非法JSON返回400和`REQUEST_JSON_INVALID`。检查未创建真实资金、库存或打印交易。

## 微信与待办

从合并提交生成production候选149个文件，源码1,119,342字节（非微信压缩包大小）；文件哈希全部一致。正式API、身份验证和URL检查启用，无开发数据回退或默认身份。调用官方微信开发者工具上传版本`1.0.0-rc.194`后，任务返回`cancelled`，原因`user_denied`，并明确`User denied "Codex" to call "upload"`。因此没有微信上传成功证据；候选及任务结果保留，未再次发起写入。重新授权后才可重试。

截至13:32，微信前端修复已入主线但工具上传未完成。13:45续记：用户确认已手动上传`1.0.0-rc.194`，不再重复上传；证据为用户确认，未独立核对平台包。上传完成仍不等于提审、正式发布、体验版选择或真机验收。支付宝应用未修改和上传。

SYS-201仍缺正式正文和保存规则的实际员工流程、微信平台指引核对：线上隐私接口200但`published=false`，会员条款仍是原v1。主体/联系方式和法人批准的用户确认已私下存档，不能代替三名不同员工的后台操作或合同核验。SYS-202真实设备慢网、混合高峰和原现场项目仍待实际验收。

## 证据位置与保存

本地受控证据：审计工作区`artifacts/wechat-all-remediation-20260914/`保存本地检查、原失败和候选复核；独立发布工作区`artifacts/audit-rc194-delivery/`保存CI、首次回滚、最终部署、线上接口/开关和微信结果。部署清单在发布工作区`.runtime/deploy/v1.0.0-rc.194/deployment/deployment-manifest.json`。

完整证据和协议材料保留本地，公开仓库报告不包含生产财务统计、交易个案、凭据或私人确认原文。原有其他工作区未清理或合并。本交付记录为后续纯文档提交，不改变已部署标签及镜像。
