# M-BOX 1.0.0-rc.194

当前交付状态（2026-09-14 13:32 CST）：PR #223已合并，标签与发布CI通过，后台于13:23:31部署成功并回读验证。微信候选包已核验，但开发者工具上传确认返回`cancelled / user_denied`，未上传；需重新获得上传授权。完整记录见[rc194交付记录](audit-rc194-delivery-20260914.md)。以下冻结前记录保留为历史，不代表当前发布状态。

本版本整合系统/微信审计修复：SYS-208—217的本地可修复代码与回归。微信桌账后台读取不阻塞加菜，但提交等待真实付款状态；公开协议与个人认证分离，饮食信息单独明确授权及撤回；非法JSON返回400。员工登录除设备外，10分钟内员工账号最多8次、来源40次、门店200次失败/在途尝试，正常成功仅释放本次。门店共享出口仍有攻击触发限流的可用性权衡；已登录员工会话不受此新增登录计数影响。

Vitest升级4.1.11；扩展夹具以种子商品实际单价核对金额，明确重新进入桌台详情，售后区域控件最低44px。无新增迁移，schema202、门店配置v24和已授权数量售后开关保持。支付宝应用源码不变，不上传。

本地完整检查、数据库、默认及扩展浏览器、不可变提交CI、标准部署与微信上传证据在完成后追加。原TC历史/未验收状态保留，不将回归总数作为逐条商业验收结论。

协议主体和电话及曲双隆（法人）批准记录已存档；SYS-201的正式正文、保存规则后台审批发布、平台指引及真机版本读取单独核验。SYS-202现场高峰、真实渠道和原Windows打印验收不能由模拟测试/后台健康代替。采用唯一标准deploy-release入口，备份/OSS回读/候选健康通过后切换，保留上一健康版本。

新增SYS-218：登录和切换的限流事务移出持有连接的业务事务，解决连接池满时的嵌套等待；写入前重新核验原设备/会话。真实单连接池并发测试原样失败、修后通过；成功计数在业务提交后释放，失败不释放。

## 2026-09-14 12:50 CST 本地冻结前验证

最终完整检查通过：常规1780通过/704数据库条件跳过，微信发布逻辑284项；完整隔离PostgreSQL2160通过/1条件跳过。扩展浏览器77项首遍通过（会员、离店重做、停止新增后恢复同时开启）；该扩展快照在最终SYS-218改动前，最新认证已由完整数据库覆盖，默认浏览器及提交CI继续验证最终源码。发布系统28项配置测试及入口烟测通过，依赖审计0项。

第一次全库出现旧会员礼遇长流程5000ms超时，2156通过/1失败/1跳过；单独诊断通过，最终全库独立运行2160通过，未修改该测试或放宽时限。SYS-218第一次诊断误用复用库产生重复夹具，改用新建隔离库后复现真实连接超时；修后单连接并发及等待期间撤销拒绝均通过。原失败不抹除。

尚未合并或部署，最终线上身份以部署回读为准；公开报告已去除生产财务统计/个案，原完整证据仅在本地受控目录。
