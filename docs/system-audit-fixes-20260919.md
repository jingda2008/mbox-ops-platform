# 2026-09-19 系统审计修复核对

用户要求：修改审计发现的问题，逐条核对，完成后提交、合并和部署。基线 `5c283b3ced6c8ea8e410557a3ee2a451d9d5b5bc`，独立分支 `audit/system-20260919`，目标 rc.210 / schema 221。原始审计与本次修复证据分开保存。

| 问题 | 实际修复 | 验证与尚待证据 |
|---|---|---|
| SYS-242 | 正式部署入口先加载依赖、实际启动 Chromium 并渲染，失败早于下载和任何远端命令 | 缺包/缺浏览器/渲染失败故障注入通过；下一正式发布验证待执行 |
| SYS-243 | 完整查询身份 SHA256 绑定、命令键有界；通用回收错误汇总 worker 健康；查询与应用分段 | PostgreSQL 渠道失败/关闭/成功、重试、单次账本及观察消费；完整隔离回归通过，上线读回待执行 |
| SYS-244 | 固定回调接受验签 receiver 对应的启用账号并落库；取关同步撤权 | 主应用验签→handler 及真数据库去重/账号隔离/撤权通过；真实微信事件另验 |
| SYS-245 | bizsend 显式 errcode=0 受理，无 msgid 留 null；异常协议仍 unknown，拒绝不重试 | 官方协议契约、真实库发码→核验→取走一次通过，外部送达待真实账号 |
| SYS-246 | 授权后不发送示例；旧发送接口 410；签名身份/模板白名单/只追加去重上报；已有关注由官方只读信息刷新 | 授权不发消息、重放去重、查询失败不撤权、真实取关生效；客户端上报不等于平台配额 |
| SYS-247 | challenge_expires_at 与 custody_expires_at 分离 | 90 天保管显示 5 分钟码有效期；过期码不发送及错误/重复核验约束通过 |
| SYS-248 | 全局认证/权限异常稳定 401/403；通知写入先校验身份 | 实际注册的四类路由缺失/过期凭证、GET/POST 与真实服务错误边界通过 |
| SYS-249 | 用途过滤前置、12 条 keyset 分页、有界转换队列、JPEG 保格式的私有缩略图、懒加载、ETag 回源鉴权、过期 UI 请求丢弃、慢读分段 | 实库 130 条混合用途分页无遗漏；缩略图、304 不读字节、撤权不命中缓存通过；浏览器分页/缓存/撤权和同资产弱网对照通过，现场待验 |
| SYS-250 | 配置读取无锁，提交保留行锁；推荐关闭时不当作网络故障反复重试 | 真库 enabled/pilot/disabled 及两端正常点单降级通过；试点开启不在本次发布内 |
| SYS-251 | 保留安全错误类别、操作、代码和服务端栈位置，原始密钥/报文不写日志 | 超时/网络/解析/未知分类与敏感值不泄漏通过；现网新日志待观察 |
| SYS-252 | 从 b75237d 完整可读版本恢复历史，并保留 rc.208 后全部新增内容 | 原 240 个编号与新增共 252 个全部存在；正文及后续记录逐字保留；UTF-8、乱码、截断门禁通过 |
| 回归噪声 | 输出目录测试只验证 I/O 与业务结果，短采样调度波动仅允许对应性能失败；正式性能门槛不变 | 缺目录 CLI 报告与正式失败门槛回归通过 |

原始证据：`artifacts/system-audit-20260919/`；修复证据：`artifacts/system-audit-fixes-20260919/`。两者均为本地忽略目录，最终发布证据另外归档。

## 历史事项

SYS-239 真机服务号授权、收码与提醒，SYS-241 Windows 安装和纸票，SYS-202 现场混合高峰/弱网仍需对应环境证据。历史财务积压、成本来源和盘点差异不能以测试或软件发布作核销。已处理退款不重开。

## 执行记录

- 当前定向验证已通过：50 项接口/worker 回归、35 项小程序性能回归；五个 PostgreSQL 文件中社交/存酒/推荐/媒体通过，支付重复执行夹具调整后纳入完整隔离回归。
- 完整检查、PostgreSQL、浏览器、远端 CI、提交合并、正式部署及上线读回正在执行；此记录不宣称上线完成。

### 2026-09-19 17:34 CST 本地复核完成

- 最终 `npm run check` 退出 0：1,887 通过、754 条件跳过；完整隔离 PostgreSQL 回归 2,297 通过、1 条件跳过。统计有覆盖，不相加。
- 默认浏览器原有 78 项通过、13 条件跳过；新增图库对照暴露 WebP 对已压缩 JPEG 减量不足和并发回退，修复后定向通过。扩展会员/赠礼/营销/升级/券退款/v9 浏览器 12 项通过。
- 最终图库专项 17 项、支付专项 27 项通过；两端原生校验及 35 项点单性能回归通过，支付宝官方编译 23 页面通过。发布链 Node 38 项、Linux 锁/状态/配置脚本、实际 Chromium 前置预检通过。
- 同一 12 张 400×400 JPEG、Chromium、4 并发、禁用浏览器缓存：原图 1,013,590 字节，缩略图 490,614 字节，减少 51.6%；正常网 80.6→66.2 ms；120 ms 延迟/180 KiB/s 模拟弱网 6,030.1→3,193.3 ms。数据库读取后才转换，最多 2 个转换并行、24 个排队，超限安全回退。仅为隔离环境对照，不外推生产 p95 或现场高峰。
- 测量过程中发现脚本 `bytes += await ...` 并发丢计数，已改为收集各响应后求和，并要求两种网络的同资产字节总数一致；最终报告保存于 `media-network-comparison.json`。早期失败证据保留，不用作验收成绩。
- 已读取生产当前 rc.209 镜像/挂载/只读业务基线和公开 readiness，未发现应用源代码层热改；当前仍未部署 rc.210。后续以合并 SHA、tag CI、正式脚本和上线读回完成发布闭环。
