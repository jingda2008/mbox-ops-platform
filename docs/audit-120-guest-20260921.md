# 120人营业场景：顾客与服务闭环子审计

基线：`77744bc4e9ed308546b630590bdfa5dfe5ce5b7d`；目录 `mbox-120-guests-audit-20260921`。本报告是根审计的一部分，120人并发、后厨/调酒师、收银、退单和打印矩阵由其他子审计覆盖。本子审计不声称穷尽全部场景。

本轮发现4个可复现缺口：2个P1、2个P2，根报告编号为SIM120-04—07。实际测试使用本机专用PostgreSQL库、原Fastify路由/命令执行器、实际微信页面或网页服务提交函数；人员、桌台、商品、订单均为合成数据。没有生产写入、真实付款、退款、发消息、打印或源码修复。顾客鉴权上下文由测试夹具提供，不能把这些用例称作真实微信登录或实际岗位权限验收。服务员完成步骤通过真实服务仓库执行，使用系统测试actor，未冒称真人现场操作。

## 新问题

| 子编号 | 级别 | 场景与已证结果 | 定位 | 关闭标准 |
|---|---|---|---|---|
| 120-G01 | P1 | 同桌31笔各10元未付订单，DB应收310元；顾客API只回最近30笔，小程序“当前未结余额”显示300元，无当前桌翻页，第一笔旧单本地付款恢复记录被清除 | `server/normalized/guest-table-orders-query.ts:213-219`；`miniprogram/pages/account/index.js:177-183,225-227` | 当前未付及待核订单不得静默丢失；全桌应收由权威完整聚合提供；明细分页与总额分开；未出现于当前页不能视为不存在并删除恢复记录。覆盖30/31/60笔、早期未付与最近已付混合、两端分页及收银总额对照 |
| 120-G02 | P1 | 连续5次普通服务后提交新的投诉触发429；限流窗口已移到过去，重建小程序运行时仍从持久存储沿用原键，真实服务端重放原429，投诉任务仍为0；相同投诉用新键对照立即201且任务1 | `miniprogram/utils/recoverable-command.js:24-37`；`server/normalized/guest-commerce-service-api.ts:749-815` | 明确区分“本次未受理的限流”与“结果未知”；恢复原回执后按retryAt允许一次新的受理尝试；跨重开、同桌他人触发表限流、普通请求后新投诉均验证。不得统一丢弃所有未知结果的幂等键 |
| 120-G03 | P2 | 网页请求送消费单据已落库，员工完成后HTTP回执丢失；客人按网络错误提示重试，网页新建幂等键，同一原需求变成两条服务任务：completed + pending | `src/normalized-ui/guest/GuestApp.tsx:431-450` | 对同一未确认意图持久保存原键并恢复原结果；原任务已完成、刷新后、跨桌隔离及真正的新一次需求均验证；不能仅靠“活跃任务合并”防重复 |
| 120-G04 | P2 | 投诉订单选择固定截前8条；API按时间正序返回，因此多人多轮时最新订单无法关联。在31笔夹具中API返回2—31，选择器仅允许2—9，第31笔不可选 | `miniprogram/pages/service/index.js:62-73` | 可查找/分页选择本桌所有允许关联的订单，优先呈现近期订单；不超过8笔、9笔、31笔及跨桌禁止关联均验证。整桌投诉仍可提交，因此本项不等于投诉入口完全不可用 |

120-G01已证的是顾客端漏单、金额展示和恢复记录错误；数据库31笔及310元仍保留，没有据此断言实际资金损失、数据库漏账或自动关桌放行。31笔是边界用例；120人分20桌、每桌6人若多人各自多轮下单，单桌可能达到此阈值，但本报告不把“120人”自动等同于“每桌31笔”。

120-G02的窗口恢复采用隔离库将`window_started_at`与`expires_at`一起前移2分钟，保持约束和命令回执不变，避免等待墙钟；新键201证明限流条件已经解除。服务请求命令默认保留24小时，因此正常一分钟限流窗口过去不代表原键能重获受理；具体保留时间仍受命令缓存清理影响，不称无限期。

该429还有响应契约问题：服务端用`{data:{status,message,retryAt}}`回429，微信`request.js:228-233`仅解析`body.error || body`，最终得到`HTTP_ERROR`且丢失`retryAt`，顾客看见“服务暂时未能确认，请稍后重试”。服务器文案称“我们已经收到啦，伙伴正在赶来”，而新投诉并未生成任务；网页接受该data，可能呈现此误导。更改文案本身不能解除旧键重放阻断。支付宝存在相同恢复器逻辑，本轮真实请求/运行时故障注入只执行微信，支付宝不冒称同等动态证据。

120-G03通过原网页`requestService`回调、原`GuestApiClient`和真实路由/DB执行；React状态setter及震动为无副作用夹具。原错误展示函数逻辑保留，实际首次提示为“网络好像走神了，请检查网络后重试。”，没有将fallback“这次没有送达”误写成实际提示。模拟的是响应丢失期间服务已完成的交叉顺序，非所有普通重复点击都会重复创建：原任务仍活跃时后端合并可正常防重复。

## 回归与既有问题

执行 `scripts/miniprogram-table-scope-race.test.mjs`、`miniprogram-invalid-scan-recovery.test.mjs`、`miniprogram-cart-portions.test.mjs`、`miniprogram-customer-funnel.test.mjs`、`miniprogram-session-launch.test.mjs`：**149通过，0失败，0跳过**。覆盖跨桌旧响应不绑定新桌、无效新扫码不恢复旧凭证、共享购物车份次、未知结账原键恢复及服务/购物车写入与读轮询竞态。这些是VM/契约执行，非120部真机或真实弱网验收。

专用 `server/normalized/guest-120-guest-recovery.test.ts`：**3个审计用例通过**，表示准确复现当前缺陷，不表示已修复。第三个用例同时覆盖120-G01与120-G04。新增文件定向oxlint通过；未据此声称全仓类型、构建、全部数据库或浏览器重新通过。

本日此前审计的SYS-318（acknowledged被双端摘要漏掉）、SYS-319（顾客确认事件未投影读回）、SYS-317（权限发布未知回执）、SYS-320（购物车原支付终态重放500）沿用原编号，不重复登记为新发现。此前报告位于 `/Users/jingda/mbox/mbox-comprehensive-audit-20260921/docs/system-comprehensive-audit-20260921.md`；此处引用的是同冻结源码的审计事实，不声称另一整改任务已交付。

顾客“要单据”可以通过个性化服务任务提交，之后仍依赖员工完成具体打印操作；消费单据、退款单、收款小票和税务发票的职责不能混同。实体纸张、Windows桥、打印机缺纸/重打、微信正式版本和真机效果由根报告分别标明，不从服务任务completed推断纸票已送达。

## 证据

- 测试：`server/normalized/guest-120-guest-recovery.test.ts`。
- 完整执行日志：`artifacts/audit-120-guest-20260921/mbox120-guest-recovery.log`。
- 149项定向日志：`artifacts/audit-120-guest-20260921/mbox120-guest-mini-tests.log`。
- 隔离DB运行命令：`TEST_NORMALIZED_DATABASE_URL=<本机隔离库> PGOPTIONS='-c TimeZone=UTC' node node_modules/vitest/vitest.mjs run server/normalized/guest-120-guest-recovery.test.ts --reporter=verbose --hookTimeout=120000`。

根报告映射：120-G02 = SIM120-04，120-G03 = SIM120-05，120-G01 = SIM120-06，120-G04 = SIM120-07。新增问题的商业清单编号、负责人及统一目标版本由根审计统一登记，避免子任务并发分配相同SYS编号。

## 增补：SIM120-10 / P1，扫码并发事务冲突耗尽重试

根任务真实HTTP报告已核实为 `/Users/jingda/mbox/outputs/120-guests-audit-20260921/run-mbox120_http_1789933759786.json`，时间03:49:19—03:49:33 CST；120个独立设备向30桌同时扫码，28个200、92个500 / `GUEST_SESSION_INTERNAL_ERROR`。这一阶段还存在后续商品可售夹具问题，但扫码冲突发生在购物车之前，不以购物车409归因扫码。该百分比仅是该次本机受控突发的结果，不能外推实际门店或设备上的失败率。

在原专用库进行4组独立门店/顾客夹具对照，调用**真实GuestSessionService、CustomerRepository与Postgres事务runner**，不修改源码或重试策略；连接池12、每组12个独立设备：

| 对照 | 成功 | 失败 | 实际事务尝试总数 | 失败原因 |
|---|---:|---:|---:|---|
| 12人同一桌并发，匿名身份首次进入 | 3 | 9 | 33 | 40001，concurrent update |
| 12人各不同桌并发，匿名身份首次进入 | 3 | 9 | 33 | 40001，read/write dependencies |
| 12人各不同桌并发，12个不同顾客身份预先创建 | 3 | 9 | 33 | 40001，read/write dependencies |
| 12人各不同桌顺序，匿名身份首次进入 | 12 | 0 | 12 | 无 |

并发失败的每个请求均已经尝试3次；不能写成“系统没有重试”。`guest-session-repository.ts:677-748`将设备/桌限流、身份解析、位置登记及会话签发放在serializable事务，指定`retryOnConflict:2`。`transaction-runner.ts:193-199`对40001/40P01立即重试，无退避；最多3次仍冲突就抛出。`guest-session-api.ts:243-246`最终落入通用500。顺序对照与已知身份对照表明，问题不限于同桌抢同一限流行，也不限于新建匿名顾客。

同桌明确共享桌限流计数行，且`issueTableSession`会`FOR UPDATE`锁同一桌次；其advisory锁包含deviceHash，因此不同设备不会在该锁处提前排队。跨桌冲突的日志包含`ensure_scanned_table_customer_position`、`canonical_customer_id`、`capture_table_session_customer_participation`以及guest_sessions查询/更新和COMMIT阶段的SSI冲突。迁移191:68—84、112—126的顾客规范身份/当前位置校验扫描同店顾客或活跃位置，再按`canonical_customer_id`筛选；串行化谓词读与并发写入形成广于单桌的读写依赖是**有源码和日志支持的解释**，但本轮未采集每一条SIREAD锁或执行计划，不能把某个索引、单行SQL或某个PostgreSQL优化器决策独断为唯一根因。

建议关闭标准：保持原身份、转桌和旧会话撤销不变量，收敛实际冲突范围，并对可重试冲突采用有界退避或明确可恢复响应；覆盖同桌不同设备、异桌新/旧身份、重扫及等待开台→扫码，保留首次失败和恢复耗时。不应仅提高重试次数或盲目降低隔离级别后宣布修复。复验需要120并发HTTP、渐进到店和真实设备弱网分别记录。当前证据未发现重复身份/错误桌归属或资金账错误，确认的是扫码入口高并发可用性缺口。

增补脚本 `scripts/audit-120-guest-scan-contention.ts`；记录 `artifacts/audit-120-guest-20260921/scan-contention.json` 与 `.log`。这些是48次真实服务调用的观察记录，3组包含明确失败，不能统计为“48项功能通过”。根任务原120次HTTP与此48次服务级对照分开报告。
