# 1.0.0-rc.231 权限时序与出品闭环说明

默认员工鉴权使用数据库微秒时间，避免应用毫秒时钟落后于刚提交的权限撤回；角色审批权限使用同一数据库时钟，显式历史时刻仍可查询。没有放宽员工、租户门店、设备或支付权限。

历史跨日关闭的旧整行商品，仅当有原始出品记录、没有送达审计、没有按份记录且桌次确有自动截止事件时，显示“有出品记录；送达未登记；桌次已跨日结束”，历史列表与导出一致。该说明不推断取消原因，不回写制作/送达，不修改正确收退款、应收和库存。

三屏功能已启用但无启用设备时，服务端单独报告设备未配置，页面提示在实际门店取餐平板设置。保留“取走即送达、手机不二次确认”；不把开发电脑伪装为门店取餐设备。

无新迁移，schema245；使用标准不可变产物发布程序。17组隔离PostgreSQL回归363项通过，含真实受限LOGIN时钟偏差/微秒/立即撤销50次及原交接重放失败用例；常规2140项通过，数据库环境相关1125项在常规命令中跳过，不能算已测。最终源码CI、正式发布和现场设备/实物验收分别留证，不以候选代替上线。


## 生产发布结果 2026-09-24 11:13 CST

PR294及标签v1.0.0-rc.231，SHA d55187768ad2680da1db39b436e5dc347e5ec28d；完整标签CI与发布工作流成功，2026-09-24 11:10 CST标准部署完成。schema245、无新迁移；四项OSS回读、四公网入口、浏览器及实际受限账号代码查询验证通过，原4笔订单和收退款事实指纹不变。运行healthy且写入开放。

操作环境沿用rc.230已验证配置：主机已有`/opt/mbox/tools/bash-5.2-runtime/bin`作为本次SSH命令PATH前缀；公网检查通过临时本机回环隧道访问同一139.196.99.138:443，保持mbox.shmbox.com主机名与TLS证书校验。初次默认Bash4.2预检失败日志保留，未改系统shell、远端发布程序或准入条件。操作端脚本在受限本地证据目录deploy-rc231.sh。

SYS-377关闭，SYS-376现场设备绑定及真实实物闭环继续开放。
