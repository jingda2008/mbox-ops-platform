# 问题5.0：生产数据、日志与线上代码根因核查

核查时间：2026-09-13 10:04 CST。业务窗口：2026-09-12 12:00至2026-09-13 08:00（北京时间，结束时刻不含）。用户已说明不记得具体桌号/时间，故按整晚系统活动核查；日志中的请求次数不是客人数或独立事故数，也未将所有测试/员工操作当作顾客消费。

## 结论先行

这9项反馈包含不同性质的问题，不能都归因“服务器人多卡顿”，也不能以“已有代码”关闭。

- **直接证实的实现问题：** 出品前8项截取、完成与配送待办混列、桌台详情缺整桌金额区域、手机桌卡被长文本撑宽、扎账默认逐单展开、盘点必须经独立复核且旧盘点审批会覆盖期间库存变化。
- **直接证实的营业异常：** 全窗口4次死锁、9笔小程序支付被归类为渠道IP风控拒绝、4次收/退款唯一约束错误、6次扫码限流、107次出品动作被拒绝。每类都有不同修复路径，不能通过加大服务器或统一刷新解决。
- **仍未完全定因：** 哪一次“转桌后仍显示旧桌”对应哪条请求、每次出品403的具体拒绝分支、现金收款两次23505究竟命中哪一条唯一索引、慢图片的网络瓶颈位置。缺少相应记录，不能杜撰唯一根因。

新证据补充[统一需求第15.3节](/Users/jingda/mbox/MBOX-桌台售后库存打印扎账开发需求-20260912.md)，并更新[开发台账](requirements-implementation-20260913.md)及[商业化清单](commercialization-pending-checklist.md)。本轮未修改业务程序、权限、订单、库存或生产配置。

## 证据来源与边界

1. 通过既有SSH连接确认主机为`chaohai-app`；当前生产容器`mbox-app`为`1.0.0-rc.191 / 2dc1067`，2026-09-12 19:48启动。核对其实际编译代码与该提交源码；没有用未部署的schema200候选解释现场。
2. 生产数据库为`mbox_mini_rc87`，schema197，读取事务明确为`READ ONLY`，设置门店/租户作用域，单条查询设8秒以内超时。不是此前中转validation库。各次查询时间保存在JSON中；不同只读事务不是同一整库快照。
3. Caddy入口日志覆盖完整窗口，共125,655条HTTP访问记录；另读取该时段运行过的5个应用容器日志，避免漏掉19:48升级前记录。入口耗时包括响应传输，不等同数据库执行时间。HTTP状态0不当成业务失败或成功。
4. 数据证据含当晚132个付款记录、213个新建出品任务、制作/配送审计、扫码及正式转桌记录、打印快照、库存盘点与后续流水。成功付款/退款的内部入账检查没有发现缺对应账务流水；这不等同对所有外部现金实物或支付渠道做过独立对账。
5. 证据目录：`artifacts/operating-issues-5-backend-20260913/`。其中`access-summary.json`、`app-errors-all.json`、`business.json`、`followup.json`、`deeper.json`、`closure.json`、`final-data.json`、`crosscheck.json`保存只读结果，派生统计另存。未导出密码、付款码、客人姓名或原始扫码凭据；客户端地址仅作哈希分组，不能据其识别员工或顾客。
6. RDS运行账号不能读取`pg_current_logfile`，查询返回42501并回滚该只读子查询。未绕过权限；因此未获得数据库原始错误日志中可能包含的约束名/完整等待SQL。应用日志未完整记录某些失败动作的输入和拒绝分支，这些部分保留待证边界。

## 1. 高峰买单卡顿：必须拆开处理

### 已证实的四条异常链

| 事实 | 现场证据 | 因果判断与修复方向 |
|---|---|---|
| 4次数据库死锁 | 19:46:20、21:24:39两次、22:39:58；日志`40P01`，涉及`guest_shared_carts`和`table_session_customer_participations`，入口购物车/服务状态读请求对应500 | 并发鉴权、购物车和结账锁路径存在竞争；不能说全部付款失败都由死锁造成。按现有REQ-16核对完整锁顺序、必要锁级别，定向复现后验证；本地锁序候选尚未部署 |
| 9笔JSAPI支付拒绝 | 19:05—22:29，W06 4笔、W05 4笔、W01 1笔；支付动作及付款快照均记录`POSTAR_CREATE_JSAPI_IP_RISK_REJECTED_111111` | 已确定拒绝类别是渠道IP风控，尚未证明是某个代理/门店网络配置错误。网络来源记录为`trusted_request_ip`；继续核可信转发链及渠道原始诊断，不能伪造IP或关闭风控。明确显示未成功原因，按原单提供合法替代收款，不让客人反复重下商品 |
| 2次现金记账唯一约束错误 | 22:39:42、22:39:44，`POST /api/payments/manual`返回500；应用`23505`定位到`payments`插入 | 这是重复记录冲突，不是耗时卡住。22:39:35以凭证“123”记录VIP1 ¥108，22:39:50以“1234”记录VIP2 ¥108。现金页面要求手填凭证、库内凭证有唯一限制，重复凭证是有依据的候选解释；失败请求未留具体凭证/约束名，不能认定已百分之百重建事故输入 |
| 2次现金退款唯一约束错误 | 01:08:03、01:08:06，同一¥160退款的`manual-result`返回500，`23505`指向退款记录更新；01:08:10同笔退款以新凭证成功 | 证明“动作报错后再次操作”不等于应再退一笔款。原退款随后已成功，保留原编号查结果；现金内部凭证自动生成并复用，备注与唯一编号分开，约束冲突给出可恢复提示。外部POS/线上真实凭证去重不能删除 |

**不能把9笔拒绝金额相加作为营业损失。** 它们对应9个订单/付款尝试，包含短时间反复下单；原尝试无成功记录，不排除客人后来另下单或员工代收。没有按顾客整段交易核完前，不推断应补收或已流失金额。

### 性能证据不支持盲目扩容

- 员工付款订单读取59次，p95约78.9ms；付款工作台592次，p95约137.1ms；历史查询480次全部200，p95约112.5ms。实际交易路径并非整晚普遍变慢。
- 共享购物车结账92次HTTP均201，入口p95约963ms、最大2614ms；HTTP201仅代表接口创建结果，9笔后续渠道拒绝仍须看付款状态。
- 收款创建36次中27次201、9次400，最大约2418ms；现金记账17次中13次201、2次400、2次500。未将400都归类为服务故障，也未把201当渠道实收。
- 图片传输是另一条显著慢路径：菜单图通常约194KB（库内191个菜单素材），14,134次图片请求超过1.5秒，115次超过100秒，多张在同一客户端成批结束。入口耗时可能包括慢链路/下载取消及并发传输；缺上游分段耗时，不能定为数据库慢或服务器带宽不足。应先核首屏图片数量、懒加载/尺寸及客户网络，确保图片不阻塞收款交互；不是立即加大服务器或清缓存。

现有REQ-15-A/16继续，补REQ-15-B现金凭证与冲突恢复。将错误恢复与性能问题分开验收。

## 2. 换桌扫码仍旧桌：已排除“所有扫码换桌都失效”

- 正式整桌转桌/拆并桌事件均为0，旧转桌表也为0；16条带员工离席字段的记录实际属于关桌，不等于完成了正式转桌。不能证明用户没有操作，只能确认数据库没有成功的正式转桌记录。
- 有6次本人扫码跨桌成功落库：21:24 S3→S5，21:51 S7→L07，22:34—22:36三位不同顾客BAR2→W02，23:29 W02→C01。旧参与记录已标记`guest_rescan`离开。不能用这6次成功证明用户报告的失败不存在，也不能把本人位置变化当成历史订单整体迁移。
- 930次扫码接口请求中921次HTTP200、6次429、3次状态0。HTTP200内有742条“桌台未开”业务记录，177条会话签发和5条合并扫码记录；不同层记录数量不要求机械相等，不把200全算成功入桌。
- 有效二维码未开台会进入等待页；与线上服务提交对应的微信小程序源码每6秒再次调用扫码接口（当前用户手机安装的小程序版本未独立核实），而扫码限额为单设备同码10次/分钟、整桌同码30次/分钟。手动刷新、重新进入或多人等待会叠加调用。6次429已由数据库`SCAN_RATE_LIMITED`佐证，其中4次集中21:55:45—58。拒绝事件未保存具体命中维度/桌台，所以不能把这4次强行全归到邻近的A08等待记录。
- 同一提交的客户端`loadGuestSession`在`TABLE_QR_INVALID`时会恢复旧桌缓存，但本窗口未查到`INVALID_TABLE_QR`业务事件或扫码400；该代码分支存在，不是本次失败已证实根因。客户端到底扫码何种启动参数、显示哪个桌号没有记录，用户也无具体桌号，无法完整还原某一次旧桌显示。

**方案：** 真实扫码与等待开台轮询区分处理；同一页面请求合并、尊重`retryAt`、网络恢复后自动继续查，不让自动轮询耗尽人工扫码额度；目标未开台显示目标桌与待开台状态。正式转桌成功与本人扫码绑定分别回读，下单前权威归属保护沿用。无需强制重启整个小程序，也不默认调大/关闭全局限流。REQ-03-B补此有证据的专项，旧桌显示本身仍保留待复现标记。

## 3. 查历史繁琐：主要是入口和筛选问题

生产历史查询480次全部200，最大约175ms，没有查到该入口卡死证据。线上组件把多项筛选、结果及分页放在展开区，默认日期/返回状态缺少用户所需快捷流程。

按REQ-11-A改常用营业日、桌号/单号快速检索与返回位置保留；不为这个反馈重建历史数据库或更改既有历史权限。日志没有记录每位员工找单所用的点击数，改善幅度要在常用任务操作对比中测。

## 4. 桌台没有总金额：线上展示缺口，后台金额存在

W17照片可定位到9月12日桌次：4笔订单分别¥514、¥68、¥126、¥160，合计**¥868**；4笔对应内部收款流水合计也是¥868，未查到这些付款的退款流水。这里是数据库整单金额相加，不是从照片裁剪内容猜总价。

线上`TableOrderStatusPanel`只渲染已上/未上数量及逐品项金额，没有整桌金额区域。本地09月13日候选已补消费合计，但未部署，不能用本地代码反驳截图。实际收款弹窗已有已选待收、本次收款值，位于订单列表后，需按REQ-01-A同时改善可见性。**已收¥868不能因此再催收¥868。**

## 5. 出品只看8项：线上硬截取与实际单量直接冲突

线上`prioritizeActionFact`默认截前8项，没有直接浏览后续待办入口。由当晚任务创建、制作完成及取消时间重建“尚未制作完”的任务数（不是商品份数）：吧台峰值**23项，21:50:12**；后厨峰值**10项，21:38:04**。这不是假设的极端场景，实际营业已经超过显示上限。

列表查询本身12,248次，p95约102ms；“找不到后面单”主要是列表策略问题，不是接口一律只返回8条。按REQ-17-A增加全部可达、筛选与有界加载，岗位范围保持。

## 6. 制作完成仍停留：队列语义与操作失败要分别修

- 线上主标签为“待制作 / 待取送”，读模型包含`ready`。`runFulfillmentAction`虽在成功后移除，但重新加载会让仍待配送的项目回到合并列表；缺少制作与配送职责分开的视图。这是代码层的稳定解释，不能简单删除`ready`任务。
- 当晚新建213个KDS任务，202个后来有完成及送达记录、11个取消。已制作至送达的中位间隔约67.5秒，p95约10.4分钟，最大约34.2分钟。不能把“制作完成”冒充“已送达”，也不能从等待间隔断言员工偷懒或都由界面造成。
- **另有107次KDS动作403，涉及20个任务。** 48次发生在完成之前、59次发生在已制作但尚未送达期间；不是全部“已经送达又点旧单”。共515次KDS动作中406次200、107次403、2次409。403覆盖权限、岗位、会话在线租约等多个分支，日志未保留具体动作/拒绝码与账号，不能直接指责权限配置错误或擅自给全部岗位开权限。
- 列表`canPrepare/canDeliver`主要依岗位和任务状态计算，而实际动作还校验设备授权、会话和在线租约；这是需要复核的前后端可执行性差异，不等于已经证明107次都来自租约过期。

按REQ-17-B分制作/配送列表，成功回读并移入对应历史；同时补403具体原因和同入口恢复，按钮与服务端能力判定一致。否则只改“完成后隐藏”会掩盖真实失败。

## 7. 直接输库存：存在未闭环盘点和陈旧数量覆盖风险

- 系统已有实盘数量入口，不是只能进货。现有14张盘点单均为`submitted`，没有已审批盘点；时间跨度8月27日至9月10日，涉及5种库存项，全部14条盘点行此后都有库存流水。
- 审批权限在当前吧台/收银/店长等角色已配置，创建人不能审批本人盘点；这是现有流程设计，不能由“待审核”直接推断无人有权。
- **源码确认的危险点：** `approveStockCount`读取当前余额后计算`counted_quantity - current.onHandQuantity`，最终将余额设成旧`counted_quantity`，没有拒绝过期盘点或把盘点之后的销售/退库纳入语义。14条既有待审记录均具备“已有后续变动”这个触发条件；尚未发生审批，不宣称已经导致错库。

REQ-18应优先复用快捷盘点、明确“当前实盘”与“进货增量”，同时增加陈旧盘点检测及重新盘点/核对入口。**不要批量批准这14张旧单。** 这不是新增多级审批，而是避免把几天前的库存数覆盖到今天。当前库存没有被本轮查询修改。

## 8. 手机布局：已在隔离浏览器复现并定位

使用线上`2dc1067`的基础/工作区/桌台样式，加上照片同类金额文本重建最小页面，未连接业务接口。

390px宽度时每个外层桌卡格宽86.25px，但C03按钮被长文字撑至153.28px，超出约67px；BAR1撑至132.66px，超出约46px。原因是内层grid的隐式自动列宽与按钮`min-width:auto`继承最小内容宽度，退款说明又是单行不换行；只对子文字加省略样式不足以限制父卡片。

单独在诊断页面设置按钮`min-width:0`，或把内层列明确为`minmax(0,1fr)`，就使这两张按钮回到86.25px。320/375/390/430宽度均已记录基线尺寸。尚未把样式写入业务代码；最终布局还应让主要金额完整可读，不能靠全部截断文字交付。

[线上样式隔离复现图](/Users/jingda/mbox/mbox-member-growth-reliability-20260909/artifacts/operating-issues-5-backend-20260913/layout-390.png)。基线、单因素诊断和测量分别保存为`layout-repro.html`、`layout-metrics.json`、`layout-cause-check.json`；这不是用户手机实机验收。

## 9. 扎账太长：真实快照已证实打印粒度错误

02:02:29发起一次9月12日手动扎账。数据库固化了**373条逻辑内容行，拆为7页**（前6页各60条，末页13条），7个任务各尝试1次并收到`printed`回执。不是查到7次用户重复打印，也不是单凭照片判断切纸失灵。

快照前几行就是“桌号＋订单编号＋订单金额”，然后逐件商品单价/小计；每个订单重复展开。线上`materializeManualDailyReport`正是这一逻辑，源请求只带营业日，没有汇总/明细模式。373条逻辑行在物理纸上还会因备注/字号多次换行。

REQ-10-A改为默认紧凑汇总，分类/商品/套餐汇总按需选择，逐单明细另选；点菜单大字样式与扎账汇总分开。后台快照已足以确认模板粒度是长票的实际原因，无需重装驱动或更换已验证打印传输。物理切纸结果仍不能由`printed`推定。

## 额外交叉核查

- 当晚后厨制作票56条已回执、1条待打印，目标设备均为`chufang`；吧台制作票90条已回执，目标均为`batai`。未查到这批任务把后厨制作票路由到吧台，不建议重写正常路由。收银汇总票包含后厨商品属于不同票种。
- 打印桥最新存储心跳为02:13:58，版本已经是**1.0.4**；这修正此前“尚无升级版本回读”的信息，但不单独证明安装包是哪一修订版或所有正式票物理验收通过。03:14:55后厨另有1条`pending`、尚未尝试，已晚于最后心跳；可能门店电脑关闭/断网，不能仅凭此定硬件故障或自动补打。
- 旧付款核对后台在该窗口留下15,061条`OnlinePaymentUnknownError`日志，涉及419个付款ID；已在原REQ-15历史核查范围内。不能仅因重复查询失败批量判未付、删账或再收款，也未证明这些日志造成前台卡顿。
- 推荐配置/推荐接口另有553/517次503，平均响应并不慢；应核对功能启用条件与前端降级，不能把这些503统计成收款接口故障。本轮没有据此新增促销规则或开启尚未配置的推荐能力。

## 实施建议与证据不足项

先做有直接证据、影响面小的修复：桌卡宽度、整桌金额、全部出品待办入口、制作/配送分视图、扎账默认汇总；同时把现金内部凭证改为自动且幂等，保留真实外部支付凭证去重。库存旧盘点风险先保护，再简化入口。

死锁按实际4次路径复现并覆盖；扫码等待/限流按已有6次事件做定向修正；出品403必须补可解释原因与恢复，不批量放宽权限。IP风控需核可信网络来源及渠道证据；图片慢链路先测分段，不先扩容。

仍无法凭现有历史数据确定的内容：某一位客人的原桌/目标桌及客户端显示、每次KDS拒绝的具体分支、现金插入冲突的精确索引/失败输入、旧图片下载慢发生在上游还是客户端。这些保留待证，不用“全部根因已查清”描述本轮。

完成验证：所有生产操作为只读；已核日志/数据库时间范围、线上版本、付款入账边界；隔离布局实验只写本地诊断文件。没有提交、部署、改权限、审批盘点、收退款或触发打印。完整证据校验值见[证据目录SHA256SUMS](/Users/jingda/mbox/mbox-member-growth-reliability-20260909/artifacts/operating-issues-5-backend-20260913/SHA256SUMS)，不把旧测试通过计作本次修复验收。
