# rc197员工会话与退回盘点交付记录

2026-09-14 21:26 CST核验，用户授权的提交、合并、部署已完成。

- 代码：[PR #230](https://github.com/jingda2008/mbox-ops-platform/pull/230)合并，发布标签 `v1.0.0-rc.197`，提交 `97e0cb89a8b3cbc2498773d91b15128630399def`。合并前后文件树一致。
- [PR CI](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34845749336)、[标签CI](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34846781881)、[发布CI](https://github.com/jingda2008/mbox-ops-platform/actions/runs/34846781896)均成功。完整质量、数据库事务与HTTP业务、手机浏览器、持续负载和镜像门禁通过；PR手机回归69通过。
- 独立发布工作区 `/Users/jingda/mbox/mbox-staff-session-release-20260914`，标准 `deploy/aliyun/deploy-release.sh` 最终退出0，输出 `deployment=complete`。部署清单时间为北京时间21:24:46，schema202，无迁移变化。
- 镜像 `sha256:a59423f1ee5829e3bcbea3b2bf27fc61b50163646401f207b18ae7dc8b78ea8b`。线上ready的提交、镜像、production及healthy一致。首页、桌台点单、预约、员工现场四入口HTTP与浏览器渲染均通过。
- 发布证据、镜像、数据库备份及部署/完成证据均经OSS上传和回读验证。rc196作为回滚版本保留。
- 数量售后开关在受控源配置、发布配置、活动配置、容器均为true且一致；程序解析为true。服务号回调三项配置在上述层级存在且一致，运行时已加载。未改变凭据。未登录盘点列表401，无签名回调403。

## 首次回滚与运行环境核验

第一次标准部署在切换后，本机Node26发布检查对预约页请求超时，自动回滚至健康rc196。回滚后的旧版在本机Node26 fetch也能复现超时，服务器及本机普通HTTPS请求正常；仅指定公共源站解析仍未解决。使用已安装的Node24.19.0执行相同检查，旧版四入口HTTP和浏览器检查均通过。随后仅对本次命令选择Node24，与CI主版本一致，完整重跑标准部署成功。没有改发布脚本、业务源码、TLS校验、断言或超时，没有永久修改系统DNS/代理。

这证明本次发布机器运行环境存在可复现的访问问题，不据此推断所有Node26环境都有缺陷。另一次补充curl探测曾出现瞬时TLS连接失败，有限重试后取得401/403及正确ready；原错误保留，不能据此认定生产应用故障。后续本机发布使用已验证的Node24，运行环境回归仍记录为SYS-223。

原失败日志、首次部署清单及状态、Node24预检、第二次成功部署日志和配置/公网回读保存在发布工作区 `artifacts/staff-session-release-20260914/`。最终部署清单位于 `.runtime/deploy/v1.0.0-rc.197/deployment/deployment-manifest.json`。

## 使用与验收

重新打开员工网页加载rc197。制作队列操作资格暂时失效时，通过认证心跳续期并再次核验；真实失效登录仍需重新登录。退回盘点在“已处理”中点击“重新盘点”，重新填写实测数量、生成新单，由另一名有权限员工审核。

旧库存已变化的盘点、本人不得自审均继续保护，未替员工审核或修改任何生产库存。原截图会话失效的唯一原因尚未确定，实际员工手机恢复、两名岗位盘点复核及历史待办处理仍待现场确认。SYS-220/221/222不因本次部署直接商业关闭。微信原生小程序包未改变。
