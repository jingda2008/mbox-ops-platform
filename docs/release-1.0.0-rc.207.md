# 1.0.0-rc.207 微信我的存酒

在rc.205最新主线基础上新增微信会员本人的存酒列表、完整业务单据、历史取存、完整手机号和存酒照片预览。个人页服务卡替换为“我的存酒”，使用现有品牌绿；仅认证本人可读取，照片逐次归属验证，完整手机号服务端校验后解密。支付宝源码不变。实现与本地/原生证据见customer-custody-20260916.md。

用户已授权提交合并、后台部署，并在后台成功后上传最新微信小程序并设为体验版。保留业务配置及默认关闭规则；生产schema从204升级至主线已有220，本次本人读取功能无新增迁移。现场资格、服务号验证码送达、真实本人记录、照片和真机验收仍需独立证据。

## 旧热修复替换

标准deploy-release入口增加可选MBOX_PREVIOUS_RUNTIME_ATTESTATION核对文件。显式参数、目标SHA、旧SHA、原归档镜像、实际运行镜像、容器ID、授权种类和观察时间必须匹配；无授权仍严格比对原镜像。原发布清单保持不变，新部署清单同时记录归档/实际镜像和授权核对信息，旧容器含可写层保留回滚。发布证据包含该核对文件。备份、迁移、健康、外部访问、OSS回读门禁不变。

## 验证与发布状态

本人存酒完整check通过，常规1864通过/741条件跳过，小程序292通过，存酒API与隔离数据库23通过，原生模板样式编译及iPhone模拟器通过。发布策略16项通过，包含授权缺失、关闭、各身份错配拒绝与精确匹配通过。

本文件建立时尚未部署或上传；PR/tag CI、不可变包、OSS备份、生产切换、微信上传及体验版结果按实际执行补记。

## rc.206后续修正

rc.206标签创建后，部署前证据扫描发现操作决定字段authorization被按认证凭据拒绝，未部署。改为operatorDecision，并在发布策略回归中直接调用敏感附件检查器验证真实核对JSON；保持凭据扫描规则完整。使用新rc.207标签，不重写旧标签。

## 2026-09-17 01:00 CST 实际交付

- 功能PR247、发布准备PR248及证据修正PR249均经完整CI后合并。最终标签 `v1.0.0-rc.207` / `3c6304f117d642ec1ce6c3f42f69a8f3c180fc1b`；标签CI `35122625876` 和发布流水线 `35122625860` 成功。rc.206未部署，保留原标签。
- 标准 `deploy/aliyun/deploy-release.sh` 首次部署退出0，生产部署清单时间 `2026-09-16T16:51:37Z`（北京时间00:51:37），完成状态00:52:02。镜像 `sha256:9795adaaac3ca375e1f7cadbb84f3da1e8e164174211147c3e7f50feceaf2f54`，平台镜像 `sha256:150552d5d5a3bd2e89177b6c8834a65a4ee35a9d82e43dde9ec52e2445a662a4`，schema220。OSS镜像/证据/备份及部署完成回读通过。
- 最终健康响应ready、上述提交/镜像、production；首页、点单、预约、员工四路径和实际浏览器渲染验证通过。新会员存酒接口未登录返回401及 `CUSTOMER_AUTHENTICATION_REQUIRED`。未创建真实业务或资金交易用于测试。
- 支付、微信、支付宝及联系方式保护环境配置摘要与发布前一致；商品、价格、套餐、岗位权限分配和员工覆盖摘要一致。审批额度表完整行摘要因provision刷新updated_at而变化；从首次发布前备份只读提取11行，与线上逐项比较，业务值、创建时间和记录身份一致。
- 官方微信开发者工具CLI上传 `1.0.0-rc.207` 成功，退出0且显示 `✔ upload`。AppID `wxdb9f2dc413484f2d`，包体1,011,553字节，164文件。候选绑定最终发布提交，正式API、微信身份启用、开发数据兜底关闭；上传前后全部文件摘要复核通过。候选清单SHA256 `1a8643b43dd597abca691130ea21b5d6ae29d7a1137ea2716a9fa9e3b171ea0e`。CLI仅返回包体信息，无独立上传编号，不编造平台编号。
- **体验版设置尚未核实**：公众平台页面被浏览器安全策略禁止访问，不能改用其他浏览器方式绕过。已请用户在版本管理将rc.207设为体验版并确认。没有提审、正式发布或真机验收结论。

### 额外配置保留尝试及边界

旧热修复内置额外服务号接收AppID，canonical环境本来没有该键；rc.207主线改为显式可选配置。曾尝试保留原有效值并通过标准入口重发同一标签，但重试在worker-adapters源目录与目标目录相同的cp检查处失败，未产生第二次迁移或切流。运行容器仍为首次部署时创建（16:51:17Z）的同一健康容器。失败报告的写入/切换布尔值受首次尝试遗留标记影响，不能解释为第二次切流的证据。

遵守用户“旧热修复不用管”的边界，不扩展修复同版本重发流程；额外canonical及release环境键均已撤回，原配置和当前运行态一致，旧内置额外接收AppID未继续带入。首次旧容器 `mbox-app-rollback-3c6304f-20260917-005134` 保留，镜像与容器ID均与最初核对记录一致。失败重试不影响已完成的rc.207首次部署和小程序上传。

本机证据在独立工作目录 `artifacts/rc207-release/`：首次部署日志/清单、最终健康及四路径验证、配置及审批业务值比对、各PR/tag流水线、微信上传日志/包体回执和上传后完整性记录。原生视觉验证仍为离线夹具，真实会员记录、取存实物、验证码送达和真机权限继续由门店验收。
