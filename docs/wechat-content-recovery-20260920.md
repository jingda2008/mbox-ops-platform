# 微信分类与超嗨修复恢复

## 原因与范围

2026-09-19 19:13 上传的 `rc.210.3` 在独立克隆包含 `84c8959` 分类修复和 `1dbaf8c` 超嗨修复，但未合并主线。rc.211 仅纳入会员二维码，后续 scan.1/scan.2 从主线打包，因此丢失这两项修复。对比实际候选包，rc.211 与 scan.2 的 order/index.js、community/index.js 与 community/index.wxml 均为修复前内容。

此次从 `e71c8ef` 建立隔离分支恢复两项修复及测试，不覆盖后续扫码和桌号状态修复，不改支付宝、后台接口或数据库。

- 分类：有父级或明确名称时遵从后台配置，不再另造“小食与果盘”入口；旧接口缺配置时仍兼容。
- 超嗨：只保留活动列表与会员报名，移除演出/预约/菜单卡片及演出加载依赖，消除无 Intl 设备的加载错误。读取失败可重试，无活动显示空态。
- 相关回归并入现有 `release:miniprogram:test`，覆盖真实父子分类筛选、无 Intl 活动加载、错误重试和禁止无关入口。

## 验证及交付

本地检查、远端 CI、主线合并、最终 SHA 候选构建及上传结果随执行补充；目标开发版本 `1.0.0-rc.212-ui.1`。后端无需部署；开发版本上传不等于体验版切换、送审或正式发布。

本地：小程序回归 308/308，通过食品父子分类/超嗨无 Intl 与错误重试/扫码恢复/会员二维码等检查；布局及支付宝平台约束 24/24；微信静态 166 文件、支付宝 23 页通过。支付宝源码无改动，仅更新已复核微信超嗨模板/样式的精确差异指纹。首次测试因新工作树尚未安装 typescript 依赖失败，npm ci 后完整重跑通过，失败日志保留在 artifacts/content-recovery。


## 2026-09-20 11:37 CST 实际交付

- [PR #263](https://github.com/jingda2008/mbox-ops-platform/pull/263) 已合并主线，源码 `1d1fe65f52d866f0ce3c94cb53a6b8b8048faf13`。
- [PR CI 35486366829](https://github.com/jingda2008/mbox-ops-platform/actions/runs/35486366829) 全部成功，覆盖质量、小程序、数据库/HTTP、浏览器、性能。PR 与最终合并提交的 Git 树完全一致。
- 主线 CI 首轮数据库 2304 通过、1 条既有重新制作测试超时 5000ms；未修改其代码或超时。相同代码的 PR 数据库成功，本机新隔离 PostgreSQL 完整复验 2304 通过/1 条环境跳过，100.87 秒；临时容器已移除。主线失败项已发起重跑，原始失败记录保留，不把首次失败写成通过。
- 官方微信开发者工具 CLI 上传 `1.0.0-rc.212-ui.1` 成功，退出 0、`✔ upload`，AppID `wxdb9f2dc413484f2d`，包 1,016,927 字节。
- 候选绑定最终合并 SHA；162 个包内源码文件与该提交逐字核对相同，生成的项目/生产身份配置单独验证；上传前后 candidate 完整性均 ready。清单 SHA256 `8edb996d7fa83b3c2e191d7863adf5f5b9085cc304de2c1f6e45b09cb70fecf8`。二维码、App/Home/会话六个文件与原主线逐字一致，后续扫码及桌号修复保留。
- 支付宝源码不变、未上传；后端没有修改或部署。此次仅上传开发版本，未设置体验版、送审或正式发布，真机分类/超嗨/二维码/扫码复验待实际新版本验证。

本地证据：`artifacts/content-recovery/`，包含两次小程序测试、平台布局检查、PR 源码保留核对、162 文件源包核验、不可变候选清单、上传回执、前后完整性、本机数据库复验及远端初次超时日志。
