# 1.0.0-rc.208 服务号编辑与群发操作面

仅修复员工端「服务号、企业微信与专属卡」保存后无法展开编辑，以及「服务号活动群发」服务号下拉、日期时间选择与安排发送入口。不改支付、库存、模板消息通道或默认经营开关。关联 SYS-240；SYS-239 微信类目模板库无验证码模板仍开放。

用户已授权合并 main 并发布生产。无新增数据库迁移；沿用 schema220。真实群发回执、验证码模板审核与门店送达仍需独立证据。

## 验证与发布状态

本文件建立时本地以合并提交为基线准备标签；完整 `npm run check`、标签 CI、不可变包、OSS 备份与 `deploy-release.sh` 生产切换结果按实际执行补记。不以本机缺完整 Node 工具链代替 CI。
