# 微信桌码进入慢排查与修复

更新时间：2026-09-19 21:22 CST。基线 origin/main `becf9dcd3415fb0ecce7332ef2ef3ea4a724c1b5`；线上只读 ready 确认为 rc.211 / `69cacece38e7f542c81e27b5b46433ed5db3d8c0` / schema 221、workers healthy。独立工作树 `mbox-wechat-table-scan-latency-20260919`。当前为本地修复，尚未上传本轮小程序改动。

## 已确认的代码阻塞

| 编号 | 原行为 | 修复及保护 |
|---|---|---|
| SYS-262 | 冷启动先等挑战请求，再调用 wx.login；两项没有结果依赖却串行等待 | 并行发起挑战和微信登录，仍等两者齐全后发送 code/state/nonce 认证，不跳过认证 |
| SYS-263 | 桌码扫描 await 整个顾客/预约会话初始化，微信身份已验证后仍等与桌码无关的预约 cookie | 抽出并共享身份初始化 promise，桌码只等真实微信身份；预约会话由启动/会员流程继续办理。保持原凭证域、会员退出行为、失败兜底和桌台请求作用域防串桌 |
| SYS-264 | 首页先 await 会员、预约、演出、权益，再连接桌台，任一非关键内容慢都会延迟桌台显示 | 桌台连接与首页内容同时启动，桌台验证完成即解除该加载等待；保留切桌/离页/晚响应保护 |

本次只改微信端启动/会话依赖；支付宝源码不变，没有改动身份服务、数据库结构、菜单价格和支付业务规则。默认生成桌码指向 pages/order/index，SYS-264 覆盖进入首页及已有首页链接的关联路径，不假设所有现场桌码都走首页。

## 线上只读证据

Caddy 日志窗口北京时间约 2026-09-18 21:16:54 至 2026-09-19 21:16:53，涵盖多个部署阶段；仅按固定 API 路径汇总，不导出顾客身份、请求参数或凭据。

| 路径 | 样本数 | 服务端 p95 | 最大值 |
|---|---:|---:|---:|
| 微信挑战 | 163 | 29.18 ms | 474.97 ms |
| 微信 code 认证 | 140 | 345.20 ms | 404.71 ms |
| 预约会话初始化 | 215 | 173.21 ms | 281.55 ms |
| 桌码连接 | 372 | 148.06 ms | 202.89 ms |
| 桌台菜单 | 744 | 100.75 ms | 122.71 ms |
| 公共菜单 | 430 | 65.13 ms | 78.41 ms |
| 共享购物车 | 3793 | 78.12 ms | 1141.33 ms |

这批路径未见 HTTP 5xx，存在少量401及 status=0；无法只凭这些日志将其关联到本次手机投诉。服务器耗时不包括微信扫码识别、代码包下载、wx.login、手机网络往返及渲染；不能将不同接口 p95 相加作为用户扫码耗时，也不能用当前健康状态证明现场没有卡顿。

## 验证与待确认

修复前控制请求完成顺序的回归复现：微信登录未并行、预约接口未返回则桌码请求不发起；修复后新增身份/扫码测试及切桌、身份凭证隔离、付款恢复等定向60项通过。微信静态与支付宝23页完整校验通过；补齐依赖后完整小程序回归 301 项全部通过。首次完整回归因新工作树缺少 TypeScript 依赖失败，保留日志并用 npm ci 补齐依赖后重跑，不归因于业务故障。

已询问用户具体慢的阶段、等待秒数、异常时间和桌号，尚无现场样本；目前只能确认并修复上述依赖阻塞，不能宣称它们解释每次现场等待。此前 rc.211 是开发版本上传成功，不等于用户手机当前使用了该版；本轮修复仍需上传并确认实际体验版/正式版，再以真实手机冷启动、二次扫码、弱网及未开台场景验收。

证据目录：`artifacts/wechat-scan-latency/`。

后续进展（2026-09-19 21:23 CST）：沿用当前会话已授权的提交合并流程推进微信修复。微信候选版本使用 `1.0.0-rc.211-scan.1`，与后端 rc.211 分开记录；本轮无服务端代码变化，不重新切换后端。候选将绑定最终合并提交；平台实际体验版/正式版和真机仍待核验。
