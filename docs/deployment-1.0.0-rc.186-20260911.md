# rc.186生产部署与历史状态修复

- 用户明确授权提交、合并、部署。PR208已合并，运行源码/标签：`7d789f5a633e6e8a0aeb2e7f636b9981a33077ae` / `v1.0.0-rc.186`。
- PR CI34511835633、标签CI34512846329、发布34512846537全部通过；本地完整check和1869项隔离数据库回归通过（1跳过）。
- 使用正式`deploy/aliyun/deploy-release.sh`，经既有SSH跳板部署chaohai-app；公网mbox.shmbox.com，从服务器DNS独立核实139.196.99.138用于单次公网检查。未修改系统DNS、代理或校验脚本。
- 首次候选/服务器切换检查通过，但Mac侧/reserve与/staff/live出现请求超时，正式脚本自动回滚至6ebd544；数据库保留189。随后相同外网及真实浏览器检查通过，沿同一正式流程重试，没有降低门槛或跳过检查。精确网络根因未证明，不将间歇超时确定归因于DNS。
- 最终部署北京时间2026-09-11 02:31:19；schema189、生产正常角色、writeEnabled=true、workers healthy；独立公网readiness为7d789f5，首页/顾客W01/预约/员工现场HTML及真实浏览器全部通过。
- 镜像摘要`sha256:97a75dbb9f9d62e0393f75f813a1755c560ba36fd9b5720e378c40520b5185e5`；平台摘要`sha256:0e63095dce81666afd9bb957fc369be8cf5000f0eaee22efe165ad895221c0fa`。
- 首次备份`/opt/mbox/backups/mbox-20260910T182522Z-HOCG2R.dump`；重试备份`/opt/mbox/backups/mbox-20260910T182957Z-r8QTjv.dump`；OSS备份、发布材料、切换及完成证据上传/回读通过。
- 保留应用回滚容器`mbox-app-rollback-7d789f5-20260911-023116`，前版6ebd544；应用回滚不等于数据库回退。

## 上线后闭环

1. 运行版本中的历史白名单修复先事务预演并回滚，再提交；限定VIP1退款测试品项A—D，全部有原经理取消事件、全退款和关闭桌次证据。四条submitted→cancelled，对应订单fulfillment_state→cancelled。
2. 独立连接回读四条商品及订单全部匹配，金额与数量与发布前一致；4条唯一审计；再次预演changed=[]，幂等性成立。未改资金、库存、历史退款或发起出纸。
3. 9类打印策略enabled/copies逐项与发布前完全相同。Windows未升级，仍上报1.0.0；末次心跳02:13:49，不能从配置active推断核验时仍在线，实体出纸没有在本轮测试。
4. 旧W06投诉由另一路操作已取消，本轮独立回读cancelled、旧升级125条保持；不伪造完成、也不复开。防重复升级新逻辑已经发布，未创建线上测试投诉。
5. 过期授权回读expired；周期收尾和消费时到期校验随本版生效。¥20按用户确认已处理关闭，不进行退款、补账或渠道查询。

后端部署不包含微信/支付宝上传，不代表Windows安装或实体纸票验收。发布原始证据位于`.runtime/deploy/v1.0.0-rc.186`及受控临时日志；不提交数据库、密钥或原始运维日志。
