# 1.0.0-rc.234 三屏入口、取餐登录及平板操作修复

解决后厨/酒水岗位仍进入旧出品页、旧按钮不能完成已绑定制作批次，以及仅有kds.deliver的取餐账号因普通看板403反复退出登录的问题。默认入口按受权岗位进入专用屏；已有批次从旧页进入正确制作屏完成；权限拒绝不再等同登录过期，不新增权限或设备绑定。

压缩三屏重复顶栏及空白面板。横屏并列待制作/制作中，竖屏工序切换；旋转保留批次及份数。取餐详情内部滚动、确认按钮留在可见区域；三屏适配键盘可见视口、保留特殊备注与待核原操作。进入后台或离线先禁用操作，恢复后读取最新状态再放行，不自动重发业务命令。

最低schema247，无新数据库迁移，无账目调整。发布范围是规范化员工网页和服务端，微信小程序与Windows打印桥不在本次发布内。保留rc.233自动回退能力，按deploy/aliyun/deploy-release.sh执行不可变产物核验、备份/OSS读回、候选检查、流量切换和公开浏览器验证。

本地16项联合浏览器、95专项单测、类型/正式构建通过，前序已完成取餐权限、SQL与原20桌80份完整流程回归。CI新增独立开关环境的三屏测试；经营TC仍保留原现场未执行边界。SYS-379/380/381/382真实Android设备、qucan绑定与实物验收保持开放。本文件建立时尚未部署。

## 生产发布结果

2026-09-24 22:45:55 CST正式发布完成，标准入口退出0，线上SHA/镜像/健康及公开页面一致。详见[发布记录](production-release-1.0.0-rc.234.md)；现场实物验收继续开放。
