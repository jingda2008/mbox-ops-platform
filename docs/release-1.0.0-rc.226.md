# 1.0.0-rc.226 微信登录与客人下单恢复

rc.225线上多桌加购成功、下单连续409；微信认证503。真实运行账号只读EXPLAIN和隔离数据库完整登录API均确认：身份保存的ON CONFLICT更新包含受保护的principal_id，运行账号因此拒绝整条语句，包括新客首次插入。

本版移除主体字段更新，冲突时必须匹配已有主体及外部身份ID，未保存则明确失败。保留受限权限、原加密身份和顾客关联，不新增迁移，schema仍为242，不需要重新上传微信小程序。

修复前隔离受限账号登录返回相同503；修复后新客、老客、会话读取、付款身份解析、跨门店与主体冲突拒绝共4项真实PostgreSQL测试通过，相关回归共98项通过。类型、构建、架构约束、lint及清单通过，保留既有lint/构建告警。微信上游测试替身不能代替真实微信及实际支付验收。

用户已授权紧急上线。固定SHA CI、正式镜像发布与现场恢复结果另记。上线后原匿名会话需要重新扫码绑定微信身份，保留同桌购物车；未上线前可由员工代客点单。详见[故障记录](guest-checkout-incident-20260923.md)。
