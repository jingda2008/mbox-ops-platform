# 1.0.0-rc.236 三屏已签发会话与口令轮换一致性

rc.235发布后只读复验发现：后厨会话和设备租约仍有效，绑定口令却在00:35:50的发布配置同步时被轮换撤销。既有StaffAuthCommandService测试明确六小时登录会话独立于门店口令轮换，KDS及取餐SQL却要求旧口令仍有效，造成正常登录和心跳不能恢复制作授权。

KDS读取/命令与取餐receipt/undo SQL统一按已签发会话处理。保留tenant/store、员工、原会话/设备绑定、会话与设备撤销/到期、在线租约、取餐设备绑定和实时岗位权限。新登录/切换员工仍要求当前门店口令授权，不延长任何会话，不恢复撤销的口令，不改历史绑定。schema248仅CREATE OR REPLACE取餐触发函数；无账目或履约数据回写。

隔离数据库先复现轮换后的false拒绝；68项校验涵盖原批次完成、口令轮换后取餐/撤回、直接SQL仍拒绝撤销设备/会话、在线过期及六小时硬过期。SYS-384正式发布和现场恢复结果分别留证。rc.235已上线，详情见production-release-1.0.0-rc.235.md；本版本建立时尚未部署。

## 正式发布结果

2026-09-25 01:36:20 CST已完成标准发布，schema248。后厨原会话与设备在线有效、旧口令仍撤销时，实际后厨板actionSessionValid/canPrepare/canStart均为true。完整CI、备份/OSS读回、公网浏览器复验通过，详见[生产发布记录](production-release-1.0.0-rc.236.md)。现场实物制作及取餐验收仍保持独立，不代替员工操作。
