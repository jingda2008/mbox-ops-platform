# 全系统审计整改实施与验收矩阵

2026-09-08；目标 rc.182。工作树：`/Users/jingda/mbox/mbox-cashier-terminal-history-fix-20260908`；基线 bc515baa。用户已授权实施、GitHub 合并、后端部署和微信体验版上传。此文件记录实施事实，不把历史审计的复现状态伪装为当前线上状态。

## 风险到实现的追溯

| 风险 | 本轮实现 | 验证与剩余边界 |
| --- | --- | --- |
| SYS-088 | 付款码绑定订单；写入立即互斥；查询与写入独立；代次防止旧结果污染新订单 | 真实组件处理器：写中切单拒绝、查询中切单允许、旧成功不覆盖、新码可释放；渠道真款另验 |
| SYS-089 | 本桌和协助点单新建 QR/付款码均可本地释放；失败后轮询恢复；保留财务追踪 | 隔离业务回归保护旧支付/晚到处理；不伪造关单，不取消后端结清约束 |
| SYS-090 | 协助点单重试保留编号，刷新授权凭证不改变稳定业务指纹；仍绑定员工与桌次 | API 丢响应与新凭证重试同编号，换桌独立；旧授权仍须服务端核验 |
| SYS-091 | 费用/更正/券指纹不含自动生成的随机编号；页面重试恢复原命令 | 独立数据库验证原金额单笔、改正文冲突、有意新增允许；历史疑似重复不自动清理 |
| SYS-092 | 读取代次和写入归属分离；离页/轮询不遗留 service/cart/feedback busy | 实际页面 VM、独立写入生命周期测试；换桌归属独立 |
| SYS-093 | 同周期草稿追加多人、显式编辑移除、版本锁、确认后不再改历史 | 三人、重复、旧版本、编辑移除、整批入账数据库测试；工资规则不自行猜测 |
| SYS-094 | 费用和工资最小查看/管理/入账权限贯通入口、面板、读取及按钮 | 服务端按权限裁剪；不授予服务员工资查看权限 |
| SYS-095 | 费用按日期加载、关键词搜索、每页50条；只读账号可切换工资周期 | 历史查询与更正保留；读取反序受保护；大量真实经营数据的性能需现场继续观察 |
| SYS-096 | 偏好多选保存并读回数组；旧 scalar 保持兼容 | cocktail + wine VM 往返；推荐使用其独立强类型偏好事实，不声称此展示字段已新增算法权重 |
| SYS-097 | 消息双接口部分失败分开显示；优惠券错误/空态互斥、可重试 | 页面 VM 双失败；真实微信订阅/送达另验 |
| SYS-098 | 预约日期/人数/请求代次校验，无效人数终止 checking | A慢B快页面处理器测试；服务端容量约束保留 |
| SYS-099 | 已登记40/42px员工操作提高到44px，保留信息层级 | 浏览器39项含触控、小屏、键盘、角色与业务链路 |
| SYS-100 | Web验收不再注入扩大页面高度/解除滚动限制的CSS | 保留真实视口截图；Web、原生、真机证据明确分开 |
| SYS-101 | 演出新增/导入/修改/场次仅写成功清空，刷新失败不称未保存；恢复命令编号 | 原有浏览器演出业务回归；原输入在异常时保留 |
| SYS-102 | 演员和歌单读取代次/归属校验，切换关闭旧编辑，修改停用核对原目标 | 静态及业务回归；不会用新演员名覆盖旧歌曲归属 |
| SYS-103 | 权益同次提交即时互斥，丢响应持久化恢复同一编号 | 同次并发只执行一次、重进复用、成功后有意再次操作新编号测试 |
| SYS-104 | 公共按钮尺寸约束与各页面网格/头像/表单/选项修正 | R01—R08、R10—R14、R17—R18共15处；原生逐页复查，不以隐藏溢出掩盖 |
| SYS-105 | 缺有效会员条款不弹邀请；非会员活动入口先读取公开活动确认存在 | 无效入口不会先诱导授权；不默认勾选会员或消息同意 |
| SYS-106 | 演员无图/坏图时文案使用完整宽度 | R09；缺图与正常图分支 |
| SYS-107 | 权益及积分兑换横滚卡内恢复正常换行，长文可读 | R15—R16；横滑轨道保留，卡片内容不继承不换行 |
| SYS-108 | 优惠券读取互斥空/错态、请求代次防旧数据 | 接口异常显示重试，不伪装无权益 |
| SYS-109（新增P1） | 套餐编辑改为现有 sharedCartView / refreshSharedCart，移除不存在的调用 | 发现于关联检查；服务端成功不再被界面缺函数中断；逐份编辑仍须原生读回 |

## 视觉建议实施范围

V01 无推荐时压缩短状态；V02购物车结算去固定大块留白；V03品牌故事去重复导语；V04个人服务奇数项完整占行；V05点歌备注自适应高度；V06积分入会返回纳入次要按钮；V07年度礼遇说明统一次级字号；V08小巧关闭图形与背景层分离；V09从加购进入优先选项，商品图文可展开。大量选项允许滚动，不为强行一屏而把字体压得过小。

## 已取得与不能替代的证据

- 最终独立 PostgreSQL 回归 1,405 通过、1项既有跳过；包含新增三人员工工资与100元费用丢响应重试（同编号重放200、改金额409、明确再次新增201）测试。
- rc.182完整 `npm run check` 通过；普通 Vitest 1,255通过，数据库专属用例另由上述独立数据库运行。小程序发布专项103项通过，支付宝平台13项通过。
- 浏览器端到端 39 项通过：多岗位、顾客/员工点单、退款权限、桌台、预约、弱网、小屏、大字、键盘和启动性能。测试用隔离数据库，非生产资金。
- 21个小程序注册页的事件绑定已检查；真实页面VM覆盖忙碌恢复、多选偏好、消息失败、预约反序。新增持久化提交恢复测试覆盖丢响应/重进/身份与桌次隔离。
- 首轮微信原生21页已获取截图；最终源副本 `/private/tmp/mbox-native-audit-rzqugS/miniprogram` 的129受保护文件逐项哈希一致，继续复验。隔离配置阻断生产请求与平台资金动作，明确禁止上传该项目。
- 同构支付宝业务与样式同步，仍保留支付宝授权/支付/订阅差异；静态21页5tab及13项平台测试通过不等于支付宝发布。
- 不关闭真实iOS/Android系统大字体、真实手机号/订阅、真实支付退款、实际打印和营业签字等既有外部验收项。不能用“所有功能全通过”替代分项证据。

## 发布状态

准备阶段：尚未提交、合并、部署或上传；最终提交/CI/部署/微信回执需后续追加。原审计报告保留原始记录，此文和商业化清单追加实施结果，不倒改历史。

### 22:24追加

已提交254e59e并推送PR #195。21页原生路由复查完成；顶部查看进展的点击区域再次测得184px超出网格，已补width/max-width/box-sizing，并实测点单页可见节点越屏0。此细节增加双端契约测试，尚待补提交。

原生3款4份、同款两份不同选择、重进保持、仅编辑第一份、第二份不变、第3份需要重选、取消不加购均通过；SYS-109缺函数链路已在真实页面编辑后读回正确。未点击付款，未创建真实订单或进行真实资金操作。首轮编辑选择器超时发生在工具热编译回到其他页面，恢复点单/购物车后同样例完整执行通过，保留失败记录不隐藏。

### 22:45追加

PR #195 的 cd9add6 质量、独立数据库、浏览器、性能检查全部通过。原生补验发现并修复菜单默认加购来源为空导致V09不收起图文，以及年度礼遇申请按钮缺显式样式；双端同步，菜单实际点击后 source=menu_add、介绍收起、选项与确认区可见、可见节点越屏0，取消保持购物车4份。新增实际页面回归通过，微信133文件静态检查及支付宝21页/13项测试通过；最终补提交仍须重新通过CI。

原生服务提交201响应延迟至离页后才释放，返回服务页 submittingId 为空，随后加水提交完成，证明SYS-092原生恢复链路可执行。会员丰富数据下权益、年度礼遇、核销码关闭、积分/成长值页面已逐项截图读回，相关区域可见节点越屏0。优惠券注入网络失败显示错误而非空权益，恢复网络重新进入后错误清除。隔离副本129受保护文件与当前源内容再次逐项哈希一致，不将隔离项目用于上传。

公众平台页面被当前浏览器安全策略拒绝访问，不绕过；官方开发者工具代码上传与设为体验版分开，后者未取得可执行的官方通道前需用户在公众平台完成。线上部署与真机/资金验收仍分别记录。
