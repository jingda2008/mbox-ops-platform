# 2026-09-25 桌台名单调整

## 业务输入

用户要求使用以下68张桌；VIP大小写统一为大写，W01/A01/B01/C01等前导零改为用户给出的W1/A1/B1/C1。用户明确新增普通桌暂按4人、VIP暂按6人；D区归室内，其余沿用同类区域。已有桌容量、区域和布局不改变，新增桌布局留空，不臆造现场坐标。

- VIP（7）：VIP1、VIP2、VIP3、VIP5、VIP6、VIP7、VIP8
- W（18）：W1、W2、W3、W5、W6、W7、W8、W9、W10、W11、W12、W13、W15、W16、W17、W18、W19、W20
- D（8）：D1、D2、D3、D5、D6、D7、D8、D9
- C（14）：C1、C2、C3、C5、C6、C7、C8、C9、C10、C11、C12、C13、C15、C16
- B（10）：B1、B2、B3、B5、B6、B7、B8、B9、B10、B11
- A（9）：A1、A2、A3、A5、A6、A7、A8、A9、A10
- G（2）：G1、G2

## 已执行和边界

2026-09-25 02:01 CST生产配置事务已提交并只读回验：68张目标桌均available；新增27、原位改名28、无开放桌次及有效未来预约的旧桌停用15。11张旧桌（S3、666、BAR1、S7、S5、888、L01、BAR3、L03、S6、BAR2）仍有未结束桌次，保持原状态和扫码入口；不自行建立旧→新移单关系，不替员工关台。故目前可用桌包含目标68张及临时保留的11张旧桌，不能把生产总可用数写成68。旧→新对应关系尚未收到回复；如门店有对应，应按明确映射另行处理，不能猜测。

所有原桌ID、二维码数量、开放桌次数、人数容量及区域保持一致；没有更新订单、支付、退款或桌次。原二维码按桌ID解析，改名后仍对应同一桌。新27桌及原有G1/G2目前无固定桌码，未擅自轮换已有二维码；新增桌码制作/张贴和新增桌位图坐标属于现场待办。

生产通过现有runtime最小权限连接执行，审计actor_type=support，逐桌记录before/after及原因；不冒充员工。预演整笔回滚，实际执行原子提交，重复运行变化为0，锁等待上限1秒、单SQL上限8秒。当前应用仍rc.236，数据配置调整无需重启或发布新镜像。版本化门店配置更新为2026.09.25-v26并追加哈希台账，防止下次部署恢复补零旧桌号；源配置只含68张目标桌，不包含临时保留旧桌。

## 验证

14项真实PostgreSQL桌台管理、二维码及名单回归通过，包括runtime权限执行、改名保留原桌次和扫码解析、未结束旧桌保留、空桌退出以及幂等重试。期间测试夹具UUID显式类型、哈希长度及新增桌审计null处理的失败日志保留，修正后通过；生产预演与实际事务均成功。配置解析及版本哈希验证通过。

私有证据：outputs/table-roster-20260925/下before.json、preview.json、applied.json、after.json、verification.json；不包含客户身份或凭据。脚本scripts/reconcile-table-roster.mjs默认回滚，必须显式--apply才提交；以后重跑会在旧桌无未结束桌次/未来预约时停用，不能未经现场对应核对就批量关台。

## 2026-09-25 02:29 CST 旧网页二维码兼容补查与保护

上述02:01记录是首次事务结果。后续发现H5会严格比较URL旧编号与服务器当前编号，仅DB二维码ID不变不足以证明网页可用。已审计恢复28个原内部code，保留新display_name（证据legacy-codes-preserved.json）；新27桌、停用15空旧桌及11未结束旧桌保护仍有效。实际内部编号现在尚未全部改成新名单。rc.237候选补齐H5明确别名校验和配置原位改名/冲突回滚；配置v27追加不可变哈希，取代尚未部署的v26。部署完成后另行读回和记录，不提前声称旧网页码已恢复。

发布顺序：配置同步识别renameFrom并保留旧code，不提前创建或改号；兼容前端正式切换且全部发布检查成功后，用审计名单脚本原位改号。切换前失败时旧程序和旧编号保持兼容。改号后若人工回退旧镜像，须先审计恢复旧内部编号。
